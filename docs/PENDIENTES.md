# Pendientes

> Lo que está abierto, con **condición de cierre** para cada punto. Sin condición de cierre un
> pendiente no se puede dar por resuelto, así que no entra sin ella.
>
> Cuando algo se cierra **se borra de acá**, no se tacha. Si era una afirmación de estado, pasa a
> `docs/ALCANCE.md` con archivo y renglón.

---

## Seguridad

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 24 | **La evaluación de muestra se puede aprobar adivinando, y las preguntas que lo arreglan están propuestas y sin convalidar.** El mecanismo ya está bien: la corrige la base, la respuesta correcta no sale de ahí y el intento no se puede escribir a mano (migración 0008, comprobado en `scripts/probar_aislamiento.mjs`, puntos 21 a 27). Lo que falla es el contenido. Son **2 preguntas de 2 opciones cada una**, hay que acertar las dos y se dan **3 intentos**: quien no sepa nada acierta una vuelta con probabilidad 1 de 4, y en tres vueltas aprueba con probabilidad 37 de 64, **más de la mitad de las veces**. Un certificado que sale así sigue sin decir nada sobre quién va a entrar a una casa a cuidar a una persona. El 31 de agosto de 2026 el Desarrollador decidió quién redacta: *«siguiendo los precedentes de los 2 puntos anteriores, de momento y hasta que todo esté finalizado, nos conformaremos con las preguntas que tú propongas y yo convalide»*. Con eso se escribió `docs/PREGUNTAS_PARA_CONVALIDAR.md`: veinte preguntas de cuatro opciones —diez por cada una de las dos evaluaciones que hoy existen—, con la correcta repartida entre las cuatro posiciones y ninguna sobre tratamientos. **Y aparecieron dos cosas más para convalidar**: las cuatro preguntas sembradas hoy sí preguntan por tratamiento —posición de alimentación y Trendelenburg en `supabase/migrations/0008_cursos_y_evaluaciones.sql:373`, frecuencia de compresiones y qué hacer ante un paro en `supabase/migrations/0048_presdemo_arma_su_propio_curso.sql:68`—, que es justo la línea que la pantalla del Asistente dice no cruzar (`pwa-asistente/index.html:799`), así que la migración tiene que **reemplazarlas y no sumarse a ellas**; y las dos evaluaciones piden hoy **100% para aprobar** (`0008:370` y `0048:60`), cuando el valor por defecto de la columna ya es 70 (`0008:89`) | El Desarrollador haya convalidado las preguntas y una migración las cargue, con 70% para aprobar, de modo que acertar de casualidad baje a menos de 1 en 300. Como las guías y los documentos legales, las preguntas son documento vivo: se agregan, se corrigen y se sacan, y al concluir el proyecto van a revisión |
| 45 | **El proyecto alojado manda los correos con el servicio de fábrica, y ese servicio tiene un tope bajo.** Cada alta de Asistente o de Familia dispara un correo de confirmación, y a los pocos seguidos el servidor contesta *«email rate limit exceeded»* a todos por igual, incluso a `gmail.com`. No es un problema de pruebas solamente: un día con varias altas de verdad deja gente afuera, y lo que esa gente ve es que el alta falló. Comprobado el 24 y el 25 de agosto de 2026 intentando registrar cuentas ficticias contra el servidor alojado. **Y por un tiempo pareció que se llevaba puesta la prueba de aislamiento, pero eso ya no es cierto.** `scripts/probar_aislamiento.mjs` arranca creando dos cuentas ficticias, y contra el servidor alojado el tope la corta en la primera. **Contra la base de esta máquina corre entera**: el 1 de septiembre de 2026 pasó sus 129 comprobaciones con `node scripts/probar_aislamiento.mjs --local`, incluidas las cinco que sólo se pueden hacer ahí —las que ascienden a alguien a coordinador para ver si el personal lee los papeles de su Prestadora y no los de la otra, y si no lee ninguna de las tres tablas de la modalidad—, porque ascender pide la clave de administración y ésa existe únicamente en el entorno local. Así que **la comprobación de seguridad más importante del producto se puede correr hoy**, y lo que este pendiente traba es sólo correrla contra la base publicada. **No es lo mismo y conviene no confundirlo**: local prueba las migraciones de `supabase/migrations/`, que son las mismas de los dos lados y el guion se niega a arrancar si a la base le falta alguna; lo que no alcanza a ver es una diferencia de configuración del proyecto alojado, que no viaja en ninguna migración. El tope además se disfraza: la primera vez contesta *«Email address … is invalid»*, que manda a buscar el problema en el dominio del correo, y recién al reintentar dice *«email rate limit exceeded»*. **Esto lo abre el Desarrollador**: es configuración del proyecto alojado, `supabase/config.toml` no tiene llave para eso y `supabase config push` no la alcanza — se carga un servidor de correo propio en Authentication → Emails → SMTP Settings. **El 28 de agosto de 2026 la traba cambió de forma, y la nueva es peor de leer**: la prueba ya no se corta por el tope, se corta después, con *«La cuenta A se creó pero no devolvió sesión. Probablemente la base pide confirmar el correo»*. O sea que la cuenta **sí** se crea y lo que falta es la sesión, así que cada intento dejaba una cuenta creada y sin dueño en el proyecto alojado. **Eso ya no pasa, y se arregló el 1 de septiembre de 2026.** Eran dos cosas y ninguna era el correo. La primera: la prueba salía por `process.exit` en ese mismo renglón, **antes** de llegar a su limpieza del final, así que la cuenta recién creada se iba sin borrar; ahora los dos cortes borran lo que crearon (`scripts/probar_aislamiento.mjs`). La segunda: se creía que contra el proyecto alojado no había con qué borrar, porque la clave de servicio vive en la caja fuerte — y no es cierto, el CLI la sabe dar con `supabase projects api-keys`, que es lo que hace `scripts/probar_alta_y_baja.mjs` desde siempre. Ahora la pide igual, y **sin ella la prueba ni arranca**: crear lo que no se va a poder borrar es el defecto, y avisarlo no lo arregla. Las tres cuentas que habían quedado —las tres «Persona Ficticia A», que es la firma de tres corridas cortadas en el mismo renglón— se sacaron con la migración `0058_la_basura_que_dejaron_las_pruebas.sql`, junto con la Prestadora que había dejado la prueba de la puerta por lo mismo. **Lo que sigue abierto acá es sólo el correo** | El proyecto alojado mande los correos por un servidor propio, y varias altas seguidas dejen de fallar por el tope |
| 47 | **No hay ninguna cuenta de Asistente con la que se pueda entrar.** Este pendiente eran dos problemas y queda uno. **El cerrado**: las pantallas de acceso de las dos aplicaciones del teléfono traían un correo y una contraseña ya escritos en el HTML, cosa que no sobrevive a la primera persona real; los cuatro campos se vaciaron el 25 de agosto de 2026 y hoy arrancan con su indicación adentro (`pwa-asistente/index.html:329` y `:333`, `pwa-familia/index.html:591` y `:594`). **El abierto**: esa cuenta tampoco existía —contra el servidor alojado contesta «Invalid login credentials», comprobado el 25 de agosto de 2026—, así que nada de lo que vive detrás de una sesión se puede probar de punta a punta: «Mis Capacitaciones» se cerró sin haber podido ver la lista con los cursos de verdad. Crear una cuenta nueva choca con el tope de correos del pendiente 45 | Exista una cuenta de Asistente ficticia con la que se pueda entrar, lo que depende del pendiente 45 |
| 49 | **Los dos documentos legales ya no se contradicen con las pantallas, y falta que las pantallas dejen de contradecirse entre sí.** El 31 de agosto de 2026 el Desarrollador decidió: *«cuando el proyecto ya esté concluido se los daré a un abogado para que los revise, mientras tanto tomaremos esos como válidos»*. Con eso, ese mismo día se reescribieron enteros `docs/terminos_y_condiciones_asistentes.md` y `docs/terminos_y_condiciones_familias.md`: se sacó el aviso de borrador que decía que no se mostraban a nadie —que era la contradicción original—, los dos abren declarando que son **documentos vivos** y que la revisión profesional pasa al cierre del proyecto, el vocabulario se llevó al glosario, salió el nombre de una Prestadora ficticia que figuraba como ejemplo, y cada uno lleva su registro de cambios. **Y se declaró por primera vez lo que el software hace y ningún documento decía**: la geolocalización al fichar, los datos de salud del Reporte diario, los datos de terceros que el Asistente carga como referencias, y que el directorio se ve sin iniciar sesión. Del documento de Familias se sacó además el capítulo entero de Packs de Contactos, Suscripciones Premium, renovación automática y reembolsos, **porque nada de eso existe en el software**: no hay tarjeta, no hay cobro y no hay suscripción, y hacía aceptar condiciones de un cobro inexistente. **Lo que queda abierto no es de los documentos sino de las pantallas**, y está medido en la fila 124: el pie de `registrar-asistente.html` enlaza los términos de Familias en una pantalla de Asistentes, la aplicación del Asistente hace aceptar sin enlace que muestre el texto, la de la Familia no los nombra, y «Privacidad» sigue prometiendo un documento que nadie escribió | Los dos documentos hayan pasado por un abogado al cerrar el proyecto y lo que esa revisión corrija esté escrito en ellos. **Hasta entonces la versión vigente es la válida y esta fila no bloquea nada**; lo de las pantallas se cierra con la fila 124 |
| 62 | **La tercera puerta está cerrada del lado del navegador y abierta del lado del servidor.** Desde el 26 de agosto de 2026 el chat reconoce un teléfono, un correo o un domicilio y no manda el mensaje (`js/contacto.js`, con las reglas en `data/patrones-contacto.json` y la prueba en `scripts/verificar_contacto.mjs`). **Eso no es un control**: corre en la máquina de quien escribe, así que quien quiera saltearlo abre la consola del navegador y lo saltea — y el que quiere saltearlo es exactamente la persona contra la que existe, porque llevarse la conversación afuera es lo que le conviene. La regla de la empresa «se valida en el servidor lo que entra» pide la otra mitad, y hoy **no se puede escribir**: no hay dónde ponerla. La tabla `messages` que hay en la base es del modelo equivocado y todavía no se decidió cuál queda (`docs/TABLAS_QUE_FALTAN.md`, puntos 2 y 3, y pendiente 6). Mientras tanto no hay daño posible, porque el chat de hoy no guarda nada: `messages` contesta 401 sin sesión. **Y hay algo que decidir de paso**: la regla nombra teléfono, correo y domicilio, pero un nombre de usuario de otra aplicación —«buscame en Instagram como…»— abre la misma puerta y bloquearlo puede cortar conversaciones legítimas. Es decisión del Desarrollador | La tabla del chat exista y su migración traiga, en el mismo archivo, el control que revisa el contenido antes de guardarlo — con las mismas reglas que usa la pantalla y no una segunda copia de ellas |
| 68 | **No hay ningún registro de auditoría, y la regla de la empresa lo pide por nombre.** `celtatech\CLAUDE.md`, en «Seguridad, privacidad y auditoría», dice: *«Se audita toda acción sensible, y el registro tiene que permitir reconstruir quién, cuándo, sobre qué y qué cambió»*, y enumera el mínimo: entrada administrativa, cambios de permisos o de membresía, borrado de datos, modificaciones críticas, cualquier acción con consecuencia económica, y toda acción hecha durante una sesión de soporte. **Comprobado el 26 de agosto de 2026 contra la base en vivo:** ninguna de las 24 tablas es un registro de auditoría, ninguna migración crea uno, y la única «bitácora» que hubo era el cuaderno de cuidados, que la migración 0019 renombró a `reportes` — es del negocio, no de la seguridad. **Lo que sí existe, y conviene decirlo para no exagerar el agujero:** la plataforma trae `auth.audit_log_entries`, que está viva, es de `supabase_auth_admin` y no se alcanza desde el producto porque PostgREST no publica el esquema `auth`. Registra entradas, altas y cambios de clave, o sea **una astilla de «entrada administrativa» y nada de lo demás**. **El agujero más claro es la misma columna que fue el pendiente 66** —cerrado el 31 de agosto de 2026 por la migración 0047, que impidió escribirla desde una sesión cualquiera; lo que sigue faltando es el rastro de quién la escribió—: `caregivers.verification_status` es el veredicto que publica a una persona en el directorio público como comprobada, y la tabla no tiene `validado_por`, ni `validado_el`, ni siquiera `updated_at` — las columnas se leyeron del esquema vivo, no de las migraciones. La escribe un `PATCH` pelado (`js/apiClient.js:432`). Así que hoy, cuando alguien aparece en el directorio como comprobado, **no hay forma de saber quién lo validó ni cuándo**, y hasta el 31 de agosto de 2026 podía además no haberlo validado nadie, porque la persona se ponía el sello sola. Eso lo cerró la migración 0047; el rastro no. **Y otra vez se nota que es un olvido y no una decisión:** `verificaciones_asistente` —la evidencia de cada control— sí tiene `verificado_por` y `verificado_el`. Se pensó el rastro de cada papel y no el del dictamen que los resume. **Con una salvedad medida, no supuesta:** esas dos columnas tampoco las escribe nadie, porque `verificaciones_asistente` no aparece en un solo `.html` ni `.js` del proyecto — la pantalla que carga verificaciones no está construida. El rastro está diseñado y vacío. De ahí sale además que la lista `comprobaciones` del directorio (`supabase/migrations/0026_el_directorio_dice_que_se_comprobo.sql:66` a `:80`) hoy sólo puede salir vacía o con `curso_aprobado`. **Del borrado, la medición da tranquilidad a medias:** no hay un solo `DELETE` en `js/`, así que el producto no borra; pero `authenticated` tiene el permiso y las políticas de `caregivers` son `for all`, así que un pedido directo a la API borra la fila propia y no deja rastro. No es un agujero de aislamiento —son datos de esa persona— sino de reconstrucción. **Y la sesión de soporte todavía no existe**, así que su parte de la regla no está incumplida; pero construirla sin el rastro sería nacer incumpliendo, porque la regla pide que **todo** lo que se haga adentro quede auditado. **Por qué no se arregla solo:** hay que decidir dónde vive el rastro. Si es una tabla de este producto, se escribe acá; si es del módulo compartido de identificación —el primero de los dos que se decidieron el 26 de agosto de 2026— no se puede escribir antes que el módulo. Y hay una trampa de diseño que conviene dejar escrita: **un registro que el rol auditado pueda modificar no es un rastro**, así que la tabla tiene que poder recibir altas y no aceptar modificaciones ni bajas de `authenticated`, cosa que el permiso por omisión de fábrica no dejaba hacer, porque le concedía todo a `authenticated` sobre cada tabla nueva. Eso era el pendiente 67, y lo cerró la migración 0032 el 26 de agosto de 2026: hoy la tabla nace cerrada y cada migración declara sus permisos, así que la tabla del rastro puede aceptar altas y nada más. **Va con plan y aprobación antes de código.** **El plan está escrito y sin aprobar en `docs/PLAN_AUDITORIA.md`** (27 de agosto de 2026). Resuelve la traba sin decidir nada nuevo: la pregunta de `docs/MODULOS.md` —*¿esto seguiría teniendo sentido en prestación directa?*— pone el rastro del lado compartido, y el `CLAUDE.md` de este producto (§3) dice que hasta que exista el módulo lo compartido se construye acá; así que se escribe acá, sin el prefijo de la modalidad y sin una sola palabra de esta modalidad adentro. **Y lo escribe la base, no el navegador**, por cuatro motivos medidos el 27 de agosto de 2026: un rastro que el auditado puede omitir no es un rastro; no hay un punto único por donde pasen todas las escrituras —lo REST sí embuda en `js/apiClient.js:1075`, pero la autenticación y los archivos hablan derecho con la plataforma desde `js/auth.js` y ahí están tres de las seis categorías de la regla—; `js/apiClient.js` y `js/auth.js` están triplicados byte a byte, así que cada gancho habría que escribirlo tres veces (pendiente 13), y `window._sb` está expuesto en global (`js/auth.js:305`); y el navegador ni siquiera manda quién es, porque `cambiarEstadoAspirante` (`js/apiClient.js:431`) recibe el legajo, el estado y una nota, y ninguna de las tres es el actor. **Lo que el plan deja escrito y no cierra** son los pendientes 89 y 90, más las acciones de cuenta —entrar y cambiar la clave viven en el esquema `auth`, fuera del alcance del producto—. **Espera aprobación del Desarrollador**, y tiene cuatro puntos para decidir en su sección 12. **Y desde el 31 de agosto de 2026 hay un documento que promete lo que no existe**: `docs/terminos_y_condiciones_asistentes.md` §4.5 le dice a la persona que suprimir un dato no borra «los registros de auditoría», que hoy no hay. O se construyen, o esa cláusula se corrige | Exista un rastro que permita reconstruir quién, cuándo, sobre qué y qué cambió, para al menos: el cambio de `verification_status`, los cambios de rol o de membresía, el borrado de datos y todo lo que se haga en una sesión de soporte. Que el rol auditado pueda insertar en él y no pueda modificarlo ni borrarlo. Y una prueba con dos Organizaciones que valide un legajo y encuentre exactamente una fila con el autor correcto — **que tiene que fallar con el rastro sacado**, porque una comprobación que sólo lee la tabla pasa igual mientras esté vacía |
| 83 | **Hay una segunda dirección pública que sirve una versión vieja del producto y habla con la misma base.** `careonys-marketplace-nu.vercel.app` —la que nombraba el `README.md` hasta el 26 de agosto de 2026— no es un alias de la que despliega `main`: es **otro despliegue, congelado**. Comprobado ese día pidiéndolo con `curl`: contesta `200`, su portada se titula «Careonys — Buscá y encontrá al cuidador que necesitás» —tuteando y diciendo «cuidador», las dos cosas que los chequeos `trato` y `vocabulario` prohíben—, `data/catalogo-frases.json` le contesta `404`, y el directorio abre. **Y lo que lo vuelve un problema de seguridad y no de prolijidad:** su `js/apiClient.js` nombra **el mismo proyecto de base de datos** que el despliegue vivo. O sea que hay un cliente viejo, público y sin vigilancia, escribiendo y leyendo la base de verdad, sin ninguna de las correcciones posteriores —el filtro de contacto del pendiente 62, el escapado, los cuatro estados— y sin que nadie mire qué hace. Hoy no hay datos reales, así que el daño posible es cero; el día que los haya deja de serlo. **La dirección viva y correcta es `careonys-marketplace.vercel.app`**, que sí sirve lo que hay en `main` —comprobado el mismo día: trae las 242 frases del catálogo recién subidas—.  **Vuelto a medir el 31 de agosto de 2026, cinco días después: sigue igual.** Contesta `200`, su portada sigue diciendo «cuidador» —21 veces— y su `js/apiClient.js` sigue nombrando **el mismo proyecto de base** que el despliegue vivo, comprobado buscando la referencia del proyecto adentro del archivo que sirve. Lo único bueno que apareció al volver a mirarlo: **ese despliegue no publica la documentación interna**. Se le pidieron `docs/PENDIENTES.md`, `docs/ALCANCE.md` y una migración, y contesta `404` en las tres, así que el agujero que se cerró el 31 de agosto en el despliegue vivo —el repositorio entero servido por HTTP— no está abierto acá. Y contesta `404` a una dirección inventada, o sea que la medición distingue. **Esto no lo puede cerrar la línea de comandos**: borrar o proteger un proyecto de Vercel se hace desde la cuenta de CeltaTech, y la línea de comandos no entra ahí | Esa segunda dirección deje de contestar, o quede detrás de una clave. Y se compruebe con `curl` que ya no sirve el producto. **De paso conviene revisar la lista entera de proyectos de la cuenta**, porque si había dos sin que nadie lo supiera puede haber un tercero |
| 89 | **Subir dos veces el mismo documento destruye el primero, y no queda nada.** `Sesion.uploadFile` sube con `upsert: true` (`js/auth.js:245`), y el camino del archivo es determinístico: se arma con la cuenta y el tipo de documento (`registrar-asistente.html:1013`, `js/fichas-legajo.js:307`). O sea que subir de nuevo el mismo papel **pisa el anterior**, que deja de existir. Comprobado el 27 de agosto de 2026 leyendo las dos puntas. **No es un `DELETE`** —en los 46 archivos del proyecto no hay uno solo—, pero el efecto sobre el dato es el mismo, y por eso ningún rastro que busque borrados lo va a ver: `docs/PLAN_AUDITORIA.md` §9 lo deja anotado como lo que ese plan no cierra. **Por qué importa más de lo que parece:** el papel que se pisa puede ser el que la Prestadora miró para validar el legajo, así que el sello queda apoyado en un archivo que ya no está. **Y no hace falta subir dos veces el mismo papel para que pase.** `js/fichas-legajo.js:307` numera los archivos por su posición en la lista (`${clave}_${i}`), así que sacar una matrícula del medio y guardar escribe el papel de la fila siguiente encima del de la anterior, y deja un archivo suelto que ya no nombra ninguna fila. Comprobado el 31 de agosto de 2026 con `scripts/probar_pisado_de_archivos.mjs`. **Eso cambia la elección:** prohibir pisar, solo, no alcanza —rechazaría ese guardado, que es legítimo—; hay que dejar de numerar por posición igual. **Las tres salidas son distintas y hay que elegir:** que el camino lleve la fecha y no se pise nunca; que el depósito guarde versiones; o que pisar quede prohibido y reemplazar un papel sea una operación con confirmación, que es lo que pide la regla de la empresa para toda operación destructiva | El archivo anterior deje de desaparecer sin aviso, con la salida que se elija, y **probado subiendo dos veces el mismo documento** y comprobando que el primero se sigue pudiendo bajar o que la segunda subida se rechazó. La prueba tiene que poder fallar: contra el código de hoy tiene que dar en rojo. **La prueba ya está escrita**: `scripts/probar_pisado_de_archivos.mjs`, que hoy da rojo y entra en `scripts/probar_todo.mjs` anotada como roja esperada. Mide las dos causas por separado —el `upsert` contra la base y el camino leído del código— para no depender de cuál salida se elija, y las dos se comprobaron en verde antes de dejarla. La tercera salida, que el depósito guarde versiones, no se mide desde ahí y el guion lo dice: si se elige ésa, la prueba hay que reescribirla |
| 90 | **El motivo por el que se aprueba o se rechaza un legajo se pierde en el camino.** La pantalla lo pide: `panel-prestadora.html:614` y `:407` toman la nota de la entrevista y hasta le ponen un texto por omisión si está vacía. `cambiarEstadoAspirante` la manda como `notaPrestadora` (`js/apiClient.js:432`). Y ahí se termina: **`_mapToDatabase` la descarta**, porque `notaPrestadora` no está en la lista de campos de `caregivers` (`js/apiClient.js:1223` a `:1244`), así que se va con un aviso en la consola y nada más. Comprobado el 27 de agosto de 2026 leyendo las dos listas enteras. **Y no hay dónde ponerla**: `caregivers` no tiene ninguna columna para eso, en ninguna de las 34 migraciones. **La consecuencia dicha corta:** la decisión más crítica del producto —la que publica a una persona en el directorio como comprobada, o la que le cierra la puerta— se guarda hoy **sin el porqué**, y la pantalla le hace creer a quien la escribió que quedó guardado. **Dónde va la nota es la decisión, y está planteada en `docs/PLAN_AUDITORIA.md` §12.c** con las dos opciones y su recomendación: en la fila del rastro, no en el legajo, porque el motivo es parte de la decisión y no de la persona | La nota llegue a la base y se pueda leer junto con la resolución que la explica, y que no se pueda resolver un legajo sin dejarla. Probado resolviendo un legajo ficticio con una nota y volviéndola a leer; **contra el código de hoy la prueba tiene que fallar**, porque hoy la nota no llega a ninguna parte |
| 115 | **En el depósito de archivos el camino empieza por la cuenta y no por la Organización, y lo que separa a una Prestadora de otra está en otra migración y no lo dice nadie.** La regla de la empresa es literal: «Los archivos se guardan privados por defecto… **La ruta empieza por la Organización, y la política lo exige**» (`celtatech\CLAUDE.md`, «Seguridad, privacidad y auditoría»). Acá el camino es `<cuenta>/<archivo>` y las dos políticas propias del depósito comparan la primera carpeta contra `auth.uid()` (`supabase/migrations/0006_archivos_del_legajo.sql:52` y `:63`): cada cuenta llega a su carpeta y a ninguna otra, así que **hacia afuera no hay agujero hoy**. Lo que no está escrito en ningún lado es qué separa a una Prestadora de otra, y es esto: **una cuenta tiene un solo legajo**, por el índice único `idx_caregivers_user_unico` (`supabase/migrations/0005_acceso_por_sesion.sql:40`). La tercera política, la que deja mirar al personal de la Prestadora (`:78`), llega a la carpeta **por ese legajo**; el día que una misma cuenta tenga legajo en dos Prestadoras —que es justo a donde apunta un mercado— las dos ven la carpeta entera, con los papeles que la persona subió para la otra. Es latente, no explotable hoy, y **silencioso**: el acoplamiento vive en una migración distinta de la que crea el depósito. Desde el 31 de agosto de 2026 al menos queda escrito donde se lo va a leer: la séptima regla de `scripts/verificar_esquema.mjs` obliga a que toda política del depósito nombre la Organización, y la que no puede tiene que declarar en `SIN_ORGANIZACION_EN_EL_DEPOSITO` el motivo **y qué sostiene el aislamiento en su lugar**. Mover el camino a `<Organización>/<cuenta>/<archivo>` no es sólo una migración: hay que mudar los archivos ya subidos, y eso toca datos publicados. | Se decide si el camino pasa a empezar por la Organización. **Si sí**: hay una migración que reescribe las tres políticas contra `public.prestadora_actual()`, un guion que muda los archivos ya subidos con su registro, `js/apiClient.js` arma el camino nuevo, las dos exenciones de `SIN_ORGANIZACION_EN_EL_DEPOSITO` se borran y el chequeo queda verde sin ninguna. **Si no**: queda escrito acá mismo por qué no, y el índice `idx_caregivers_user_unico` lleva en su migración un comentario que diga que el aislamiento del depósito depende de él, porque hoy quien lo saque no tiene cómo enterarse. |
| 119 | **La pantalla de acceso puede estar diciendo cuáles direcciones tienen cuenta.** La regla de la empresa, en «Seguridad, privacidad y auditoría», dice: *«En la entrada, el error no debe permitir distinguir "esa persona no existe" de "la clave está mal"»*. Y hoy hay tres respuestas distintas. `js/texto.js:215` clasifica lo que contesta el servidor, y cuando contesta «email not confirmed» devuelve la clave `error.correo_sin_confirmar`, que en `data/catalogo-frases.json:155` empieza literalmente por **«La cuenta existe»**. `acceso.html:231` la muestra en la pantalla de entrada, y `supabase/config.toml:253` tiene la confirmación por correo encendida, así que cuentas sin confirmar hay. **Lo que falta medir, y decide si esto es grave o menor:** si el servidor de cuentas revisa la confirmación *antes* o *después* de la contraseña. Si la revisa después, sólo se entera quien ya sabía la contraseña y no hay filtración; si la revisa antes, alcanza con escribir cualquier cosa y la entrada se vuelve un buscador de direcciones registradas. Leyendo los archivos no se puede saber cuál de las dos hace: hay que preguntárselo. Está escrito el guion que lo pregunta, `scripts/medir_que_dice_la_entrada.mjs`, que no escribe nada, no imprime ninguna clave ni ninguna dirección, y necesita la base de esta máquina levantada. **Medido entero el 31 de agosto de 2026, y es el caso menor.** Con la contraseña equivocada las tres clases de dirección contestan lo mismo letra por letra —`400 · invalid_credentials · Invalid login credentials`—: una dirección sin cuenta, una cuenta con el correo confirmado y una cuenta sin confirmar. O sea que **el servidor mira la confirmación después de la contraseña**, y de «email not confirmed» sólo se entera quien ya sabía la clave. Desde la entrada no se puede averiguar qué direcciones tienen cuenta. Conseguir la tercera clase costó una vuelta y **la explicación que estaba escrita acá era falsa**: decía que el servidor de esta máquina confirma solo porque se levantó con otra configuración, se bajó y se volvió a levantar la base, y siguió confirmando igual. La causa medida es otra: el contenedor de cuentas **se creó el 2026-08-24** y `supabase/config.toml` cambió el 2026-08-25, y las variables de un contenedor quedan fijadas cuando se lo crea, no cuando se lo enciende; por eso `/auth/v1/settings` sigue contestando `mailer_autoconfirm: true`. Recrearlo pondría de acuerdo al archivo con el servidor, y ese trabajo es el 123 y no éste, porque apagaría la confirmación automática para todas las demás pruebas, que hoy se apoyan en ella para entrar con las cuentas que crean. Así que el guion le saca la confirmación a la cuenta que acaba de crear, escribiendo en la base de esta máquina —`scripts/medir_que_dice_la_entrada.mjs:140`—, y eso no es simulacro: el servidor decide qué contestar mirando esa misma columna. **Y hay un segundo caso, de la misma familia y que decide el Desarrollador:** `error.ya_registrado` (`data/catalogo-frases.json:160`) dice «Ya hay una cuenta con ese correo» en la pantalla de alta, no en la de entrada, y la regla nombra la entrada. | La medición ya está hecha y da verde —las tres respuestas son iguales—, así que lo único que falta es que quede escrito qué se decidió sobre el mensaje de la pantalla de alta. |
| 131 | **La foto de un Asistente queda al alcance de cualquiera para siempre, aunque despublique el perfil o se dé de baja.** El depósito `avatares` nace público (`supabase/migrations/0006_archivos_del_legajo.sql:39`), y eso está decidido a propósito y escrito en la propia migración (`:13`): sin eso el directorio no tiene fotos. Lo que nadie decidió es **qué pasa después**. La dirección se arma con `getPublicUrl` (`js/auth.js:260`), **no vence nunca** —al revés de la del depósito privado, que dura 300 segundos (`js/auth.js:265`)— y **nada borra el archivo**: ninguna de las 51 migraciones ni ninguna pantalla elimina objetos del depósito, así que dar de baja el perfil saca la foto del directorio y la deja servida en la misma dirección de siempre. Adivinar esa dirección no es el riesgo, porque la carpeta es el `uuid` de la cuenta; el riesgo es que **una vez que se vio, se vio para siempre**. Se declaró en `docs/terminos_y_condiciones_asistentes.md` §4.9 el 31 de agosto de 2026, que es lo que corresponde mientras siga siendo así, pero declararlo no lo arregla | Se haya decidido las dos cosas que faltan: si al despublicar o al dar de baja el archivo se borra o se muda a un depósito privado, y quién lo ejecuta. **Es decisión del Desarrollador**, porque cerrar el depósito deja al directorio sin fotos y eso es una decisión de producto, no de código |

---
| 107 | **Nadie decidió nunca qué pasa con las personas si alguna vez se borra una Prestadora, y el esquema hoy se contradice solo.** Este renglón decía otra cosa —«la puerta de baja de CeltaTech ya no puede borrar ninguna Prestadora»— y **era falso**: la puerta no borra ni borró nunca, marca `activo`, `suspendido` o `cancelado` (`supabase/functions/alta-y-baja/index.ts:102`). El `409` venía de la limpieza de la propia prueba, que sí borra con la llave de administración para no dejar basura, y chocaba contra las seis filas de puntaje que la migración 0046 le cuelga a toda Prestadora al nacer. **Eso ya está arreglado** borrando en orden, en `scripts/probar_alta_y_baja.mjs:209` a `:217`, comprobado contra la base de esta máquina. Y el estado protege de verdad: medido por el camino real, `directorio_de()` devuelve 1 persona con la Prestadora activa y **0 suspendida y 0 cancelada**, y la vista cruda `directorio` no la alcanzan ni `anon` ni `authenticated`. **Lo que queda abierto es el inventario que apareció buscando esto**, escrito en `docs/PLAN_BAJA_DE_PRESTADORA.md`, sin aprobar: veinticinco tablas cuelgan de `tenants` con tres criterios distintos, y **`caregivers.tenant_id` es `set null` mientras sus diez tablas de papeles son obligatorias y sin cascada**, o sea que el legajo sobreviviría a su Prestadora y sus papeles no podrían. Hoy no se nota porque nada borra Prestadoras; se despierta el día que alguien escriba el primer borrado, y ese día el legajo quedaría además con el sello de una Prestadora inexistente que **sólo CeltaTech puede bajar**. **No urge, y por eso conviene decidirlo ahora** y no cuando alguien lo necesite | El Desarrollador conteste la pregunta de `docs/PLAN_BAJA_DE_PRESTADORA.md` sección 6 —**cuando se da de baja una Prestadora, qué pasa con las personas que ella validó**—, y con esa respuesta se escriba la migración y la prueba de la sección 8, que tiene que dar rojo contra la base de hoy en sus dos primeros puntos. **Y que se corra `node scripts/probar_alta_y_baja.mjs` contra la base publicada**, que con la limpieza arreglada saca sola la Prestadora ficticia «prestadora-de-prueba-de-la-puerta» que quedó de una corrida anterior y que hace fallar por arrastre las comprobaciones 7, 8 y 9. Correrla borra datos publicados, así que se consulta |

## Antes de portar la primera pantalla

Se hacen una sola vez, y si no se hacen, la decisión la termina tomando quien porta.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 6 | **`mockup-app.html` es el único lugar del proyecto donde existe un chat** (`screen-chat`, con suscripción en tiempo real y confirmación optimista), y ninguna de las dos PWAs tiene chat. **Hasta el 31 de agosto de 2026 era además la única pantalla de alta que servía a una Familia; esa mitad ya está afuera**, en `registrar-familia.html`, que crea la cuenta con el rol `familiar` y la Prestadora del enlace, así que de este modelo queda por reescribir sólo el chat. Su código **no se copia**: lee `messages?order=created_at.asc&limit=50` sin ningún filtro, se suscribe a la tabla entera sin filtro, evita `apiClient.js` y, sin sesión, intenta escribir con la clave pública —y no puede: la tabla `messages` contesta 401 a quien no inió sesión, así que **ningún mensaje del chat llegó nunca a guardarse**. Hasta el 25 de agosto de 2026 la pantalla no lo decía, porque `fetch` no falla cuando el servidor rechaza el pedido; ahora la burbuja avisa que no se pudo enviar. El mensaje recibido lo mete con `innerHTML`, pero **sí lo escapa** con `Texto.escapar()` (`mockup-app.html:790`), así que por ahí no entra una etiqueta; lo que no tiene es filtro ni sesión. **Para qué existe el chat** (lo recordó el Desarrollador el 24 de agosto de 2026): es la forma en que la Familia y el Asistente se comunican **antes** de la contratación. De ahí sale una condición que no estaba escrita en ningún lado: **adentro del chat no se pueden filtrar datos de contacto**. Si se filtran, la conversación sigue por afuera y la plataforma cobra una vez y nunca más, que es exactamente la razón por la que el teléfono no se muestra (`docs/CATALOGO.md`). Anotada ahí como la tercera puerta, y **cerrada del lado de la pantalla el 26 de agosto de 2026**: el chat reconoce un teléfono, un correo o un domicilio y no manda el mensaje, dice por qué, y deja el texto en el campo para que se pueda corregir. Las reglas están en `data/patrones-contacto.json` y no adentro del guion, porque son una regla operativa; el reconocedor está en `js/contacto.js`; y `scripts/verificar_contacto.mjs` lo corre contra dos listas —trece mensajes que no pueden pasar y trece que no pueden quedar bloqueados—, que es lo que hace que la prueba pueda fallar. **Lo que falta es la mitad que importa**: el control del lado del servidor, que hoy no tiene dónde ir porque la tabla del chat no existe. Es el pendiente 62. Y el diseño de las tablas ya existe en `docs/TABLAS_QUE_FALTAN.md` —`conversaciones` y `mensajes`, atadas a Familia, Asistente y aviso—, que **no coincide** con la tabla `messages` que hay hoy en la base, colgada de una búsqueda de cuidado y no del vínculo entre dos personas | El chat —la capacidad que todavía vive únicamente acá— esté anotado como requisito para las PWAs y reescrito contra el esquema nuevo, y `css/mockup-app.css` haya pasado al sistema de diseño. Recién entonces se borran él y `mockup-app.html`. Y el chat no deje pasar un teléfono, un correo ni una dirección. **Mientras tanto la pantalla no se arregla más**: el 26 de agosto de 2026 el Desarrollador recordó que es un modelo estético y que «una vez usado pierde su valor», así que `scripts/verificar_estados.mjs` la sacó de lo que mira —está en su lista `PANTALLAS_QUE_SE_VAN`, con el motivo escrito y avisado en cada corrida— y sus ocho puntos de carga se arreglan en las pantallas que la reemplacen. Ya estaba afuera del i18n por el pendiente 65; ahora también de los cuatro estados |
| 46 | **Un perfil publicado no tiene todavía con qué empezar una conversación con esa persona.** `perfil.html:161` dice la verdad —sólo una Familia registrada puede comunicarse, y por la plataforma— y ofrece las dos puertas que existen: entrar como Familia (`acceso.html`) y publicar un aviso (`solicitar-asistente.html`). Ninguna de las dos lleva a **esa** persona: el aviso lo contesta quien quiera, y entrar como Familia no abre nada contra ese Asistente. El mecanismo es el chat, que hoy existe solamente en `mockup-app.html` (punto 6), y las tablas diseñadas para eso —`conversaciones` y `mensajes`, atadas a Familia, Asistente y aviso, `docs/TABLAS_QUE_FALTAN.md`— **no están en la base**: lo que hay es la tabla `messages`, colgada de una búsqueda de cuidado y no del vínculo entre dos personas | Desde el perfil de un Asistente publicado, una Familia con sesión pueda abrir una conversación con esa persona y el Asistente la reciba. Va detrás del punto 6: primero las tablas y el chat reescrito, después el botón. **Con qué condiciones una Familia puede escribirle a alguien que no contestó ningún aviso lo decide el Desarrollador**: es regla comercial, y `docs/ALCANCE.md` §4 la tiene congelada |
| 48 | **Diez textos no llegan al mínimo legible, y los tres motivos son decisiones de diseño, no del código.** Medido el 25 de agosto de 2026 sobre los 763 textos de las dieciséis pantallas, contra el fondo que de verdad tienen debajo, de día y de noche. **(a)** Letra blanca sobre `--azul-medio` llega a 3.21:1 y hace falta 4.5:1, en cinco lugares: el redondel del paso activo del formulario (`formulario-integral.html` y `registrar-asistente.html`), la banda con el nombre adentro del teléfono dibujado (`index.html` y `registrar-asistente.html`) y el botón de ingresar de `mockup-app.html` (`css/mockup-app.css:58`). Ese último de noche se lee, pero de casualidad: sus letras salen de `--superficie`, que de noche se vuelve gris oscuro, así que el botón azul termina con letra oscura sin que nadie lo haya decidido. El arreglo **cambia cómo se ve**: o el fondo pasa a `--azul-oscuro` —y entonces el paso activo se confunde con el paso ya hecho, que ya usa ese color— o la letra pasa a oscura. **(b)** Los colores que elige una Prestadora no tienen versión de noche: los de la Prestadora de ejemplo salen de `primary_color` y `accent_color` de su fila en `tenants`, `js/apiClient.js` los escribe encima de los tokens al abrir sesión, y de noche el enlace «Registrarse como Asistente» de `pwa-asistente` queda en 1.43:1 —azul marino sobre gris oscuro—. De día el mismo par de colores deja seis textos entre 4.13:1 y 4.2:1. El problema de fondo no es esta Prestadora: es que **nada valida el color que elige una Prestadora**, y cuando haya cientos va a haber cientos de colores ilegibles. **(c)** Las estrellas de calificación quedan en 4.4996:1, que es 4.5 raspando por abajo: `--naranja-alerta-texto` promete 4.5:1 sobre fondo claro y se queda a cuatro diezmilésimas. Ése es un token de Careonys, así que el arreglo va allá | Los tres estén decididos: (a) qué se cambia en el redondel y en la banda, (b) qué hace el sistema con un color de Prestadora que no se lee —rechazarlo al cargarlo, o calcularle una versión de noche—, y (c) `--naranja-alerta-texto` una pizca más oscuro en Careonys. Cerrado cuando los 763 textos midan 4.5:1 o más en los dos modos |
| 7 | **Los vocabularios ya viven en tablas; falta la oferta.** **Cerrado el 29 de agosto de 2026 la mitad grande**: los 24 vocabularios (142 opciones) pasaron a dos tablas genéricas —`vocabularios` y `vocabulario_items`, migración 0038—, con los dos escalones que ya usaba `cursos` (0008): `tenant_id` vacío es el catálogo general que trae el producto, `tenant_id` cargado es lo que agregó esa Prestadora. Las pantallas los piden por `vocabularios_de` (`js/apiClient.js:254`), que es una puerta como `zonas_de`, y `data/catalogo-vocabularios.json` dejó de ser la verdad: pasó a ser una copia generada desde la base —`scripts/generar_vocabularios.mjs`—, que sigue existiendo sólo porque los dos programas para el teléfono la guardan para andar sin conexión, y que `scripts/verificar_catalogo.mjs` compara en cada `commit`. **Cerrada también la mitad de los cursos, el 31 de agosto de 2026**: el Desarrollador decidió que la oferta de cursos —la oferta, no el contenido— se hace pública, la migración 0051 abrió la vista `oferta_de_cursos` con las filas del catálogo general (`tenant_id` vacío) que están publicadas y sólo con las columnas con las que se arma la tarjeta, y `cursos.html` las pide por `ofertaDeCursos()`. La tabla `cursos` le sigue sin conceder nada a `anon`, y lo que se estudia, sus evaluaciones y sus preguntas no salen por ahí. **Lo que queda abierto son los 9 servicios**, que salen de `data/catalogo-oferta.json` porque no tienen tabla; las evaluaciones de la oferta salen del mismo archivo y por el mismo motivo | Los servicios tengan tabla y salgan de la base, como ya salen los vocabularios y los cursos. Una pantalla portada contra una tabla vacía no da error: queda vacía y nadie se entera |
| 130 | **La tabla `cursos` guarda el nombre y la descripción dos veces.** La migración 0051 le agregó `nombre_i18n` y `descripcion_i18n` —`jsonb`, con los tres idiomas, que es lo que pide la regla de i18n desde el día uno— y dejó en pie las columnas viejas `nombre` y `descripcion`, de texto suelto y en un solo idioma. No se borraron ahí mismo porque las escriben y las leen otros lugares del producto, y sacarlas sin haberlos revisado uno por uno pierde contenido sin avisar. Mientras convivan, dos columnas pueden decir cosas distintas sobre la misma fila y nada las obliga a coincidir. La vista `oferta_de_cursos` publica sólo las nuevas | Se revise quién escribe y quién lee `cursos.nombre` y `cursos.descripcion`, se pasen todos a las de tres idiomas, y una migración posterior borre las viejas. Hasta entonces, hoy no hay pantalla que dé de alta un curso —los únicos que insertan son las migraciones 0008 y 0048—, así que la migración que inserte una fila nueva llena las dos |
| 50 | **Los dos botones de «Ingresar con Google» y «Ingresar con Facebook» no hacen absolutamente nada.** Están en `mockup-app.html:75` y `:77`, debajo del botón de ingresar, separados por una línea que dice «O». No tienen identificador, no tienen ningún guion que los escuche, y en `supabase/config.toml` el único proveedor de acceso externo que aparece es Apple, apagado (`supabase/config.toml:355`): quien los toca no recibe error ni aviso, la pantalla se queda igual y parece que la aplicación se colgó. Son «los cuatro estados» al revés —ni cargando, ni error, ni vacío, ni listo—. Son además los dos únicos colores exentos de `scripts/verificar_paleta.mjs`, porque son las marcas de otras dos empresas. **Ofrecer entrar con la cuenta de otro es una decisión, no una tarea**: obliga a configurar cada proveedor, a decidir qué pasa cuando el correo ya existe con contraseña, y a mirar qué datos entrega cada uno | El Desarrollador decida: se configuran los proveedores de verdad, **o** los dos botones se sacan. Mientras tanto, un botón que no hace nada es peor que no tenerlo |
| 51 | **El único importe del esquema se guarda sin moneda.** `caregivers.hourly_rate` (`supabase/migrations/0001_esquema_inicial.sql:102`) es un `numeric` a secas, y no hay ninguna columna de moneda en ninguna tabla. Se había comprobado el 25 de agosto de 2026 recorriendo las quince migraciones, que entonces eran veintidós tablas; el 26 se volvió a comprobar contra el esquema en vivo, que hoy tiene veinticuatro, y sigue sin haber ni una. **La segunda comprobación vale más que la primera**, porque las migraciones dicen qué se quiso y el esquema dice qué quedó. La regla «todo importe se guarda con su moneda» no lo admite —un número solo, leído un año después, no se sabe cuánto vale— y «i18n desde el día uno» pide `es-AR`, `en` y `pt-BR` desde el primer día, así que más de una moneda no es una hipótesis lejana. El chequeo `scripts/verificar_esquema.mjs` lo tiene anotado como exención con su motivo, así que está a la vista y no puede entrar un importe nuevo sin moneda. **Toca una columna que ya tiene datos escritos, así que lo decide el Desarrollador** (la regla de la empresa «se consulta cuando hay que borrar o pisar datos»): son filas inventadas, pero la migración tiene que decir qué moneda se le pone a lo que ya está guardado | `caregivers` tenga columna de moneda, obligatoria y poblada, y la exención se borre del chequeo |
| 52 | **El reporte de cuidado cuelga del aviso, y Careonys ya decidió hace rato que cuelga de la guardia.** Acá `reportes.aviso_id` ata cada anotación al aviso que dio origen al trabajo. En el repositorio de Careonys, `docs/DATA_MODEL.md`, la misma tabla se llama igual, `reportes`, y cuelga de `guardia_id`: la **guardia** es una jornada concreta de trabajo —un Asistente, un Paciente, una fecha, un horario, un check-in y un check-out—, y el reporte diario es de esa jornada. El aviso no aparece en ninguna parte de esa cadena, y es correcto que no aparezca: **el aviso es cómo empezó la relación, no lo que es el trabajo**. Por eso en prestación directa hay reporte igual, sin que nadie haya publicado nada. **Y no es sólo esta tabla.** `reportes` de este repositorio **es** `reportes` de Careonys, desde la 0019 también en el nombre, y `clock_ins` **es** el `checkin_at`/`checkout_at` de `guardias`: son las mismas cosas con otro nombre y otra forma, en dos bases distintas | Se traiga acá la `guardia` de Careonys, que ya existe diseñada y con RLS, y los reportes y los mensajes cuelguen de ella y no del aviso. No se decide nada nuevo: se deja de decidir dos veces lo mismo. Toca datos ya guardados, así que la ordena el Desarrollador. **Desde la migración 0020 esto dejó de ser prolijidad y pasó a costar una pantalla**: la de reportes de la Familia está vacía a propósito, porque un reporte no guarda a qué cuidado corresponde y no hay forma de darle los suyos sin darle los de todos. La cláusula que se la devuelve ya está escrita en la política y empieza a funcionar sola el día que el reporte cuelgue de la guardia |
| 55 | **El renombre de `verificaciones_asistente` en Careonys.** La decisión ya está tomada: el Desarrollador la aceptó el 25 de agosto de 2026 y está escrita entera en `docs/MODULOS.md`, sección «Verificar un papel y incorporar a una persona son dos cosas». En resumen: la de este repositorio se queda como está, porque es el módulo Verificación y porque tiene el vencimiento, que es lo que distingue un papel de una etapa; **la de Careonys pasa a llamarse `etapas_incorporacion`** y guarda **sólo la entrevista**, porque las otras cuatro etapas ya tienen quién las guarde. **Acá no queda nada por hacer.** El renombre corre sobre el repositorio de Careonys y sobre datos ya guardados, y este repositorio sólo lee los otros proyectos | Se ejecute con la fusión, del lado de Careonys. Queda pendiente sólo por eso, no porque falte decidir algo |
| 56 | **Qué suma en el legajo comprobado, y cuánto.** El 25 de agosto de 2026 quedó decidido que las estrellas son sólo de la calificación de las Familias y que el legajo lleva escudo (`docs/DISENO.md`, «Tres cosas que se miden, tres formas distintas de dibujarlas»). Falta el número. **Primero hubo que contar, porque no eran dos:** al lado de `domicilio` y `referencia` —las dos con `puerta: ninguna`— están **`matricula` y `titulo`, que son condicionales**: para un `cuidador_domiciliario` no se exigen (`tipo_asistente.requiere_matricula = false`), así que quien igual las tiene está acreditando algo que nadie le pidió. Y afuera del catálogo de verificaciones el legajo guarda cuatro cosas más: los cursos aprobados **en el sistema** (`intentos_evaluacion.aprobado`), las cinco certificaciones que la persona **declara** (lista `certificacion`), los estudios (`estudios_asistente`, con `nivel_educativo`) y la experiencia (`experiencia_laboral_asistente`, que tiene fechas y por lo tanto años). Son ocho fuentes, no dos. **Propuesta de la línea de comandos:** (1) **el puntaje cuenta sólo lo comprobado**; lo que la persona declara y nadie miró se muestra en el perfil pero no suma, porque si sumara, el escudo mediría cuánto escribió alguien y no cuánto se le comprobó; (2) **las obligatorias no entran en el puntaje**, porque son la puerta: quien está en la modalidad ya las pasó, y contarlas le daría puntos a todos por lo mismo; (3) queda una lista de cinco, **igual para todos**: domicilio comprobado, referencia comprobada, matrícula comprobada aunque no se la exijan, título comprobado aunque no se lo exijan, y al menos un curso aprobado en el sistema; (4) **todas arrancan valiendo lo mismo, y cada Prestadora lo cambia desde su panel**: el argumento de los pesos iguales —que nadie puede defender por qué el domicilio vale menos que la referencia— valía para *nuestro* criterio, y el Desarrollador corrigió que el criterio que hay que defender es el de cada Prestadora, que puede no ser el nuestro; **y puede incluso no querer calificar**, así que el puntaje entero se apaga con una llave. Aplicado el 25 de agosto de 2026 en `supabase/migrations/0018_cada_prestadora_pondera_su_puntaje.sql`: `puntaje_prestadora.califica` es la llave y `ponderacion_comprobacion.ponderacion` es cuánto suma cada una. **La columna se llamaba `peso` y el Desarrollador la leyó como dinero**, así que la migración `0022_la_ponderacion_no_es_plata_y_suma_cien.sql` la renombró y, con el mismo sí, agregó la regla que él puso: **las ponderaciones de una Prestadora suman 100 y la base no acepta otra cosa**. Arrancan en 20 las cinco, que es 100 repartido en partes iguales. Subirle a una obliga a bajarle a otra, y agregar una comprobación sexta deja de poder hacerse a escondidas: el total queda en 120 y no entra hasta que alguien reacomode; (5) **se muestra como fracción y con la lista al lado** —«4 de 5 comprobaciones»— porque un número suelto invita a comparar personas y la lista invita a decidir. **La fracción ya está muerta, no en riesgo:** sólo se lee bien si las cinco valen lo mismo, y desde la 0022 valen lo que cada Prestadora diga. El número es un porcentaje, que es además lo que la suma a 100 vuelve natural —domicilio y referencia comprobadas, 40 %—. La lista al lado se queda | Queda una decisión y una pantalla. **(a)** Si «al menos un curso aprobado» es una sola comprobación o una por curso —la línea de comandos recomienda una sola, para que el puntaje no se vuelva una carrera de cursos—. **(b) se disolvió el 25 de agosto de 2026:** preguntaba qué ve una Familia en un directorio donde conviven Prestadoras que ponderan distinto, y ese directorio dejó de existir —cada Prestadora tiene el suyo y no se mezcla nada, migración 0021—, así que los dos Asistentes que se comparaban lado a lado nunca están lado a lado. **(c)** Falta la pantalla para tocar las ponderaciones: la tabla existe y la regla del 100 también, el formulario no. El Desarrollador agregó que **forma parte del legajo de cada Asistente**, y eso hay que entenderlo antes de dibujarlo: la ponderación es una por Prestadora y no una por persona, así que lo que cuelga del legajo es el resultado —qué se le comprobó y cuánto suma— y no el formulario que reparte los 100. **Y esa pantalla es donde va el aviso legal**, agregado el 2026-08-26: encender `puntaje_prestadora.califica` es encender una función de las que traen riesgo —puntuar y ordenar personas—, y la regla de los productos Careonys pide avisarle a quien la enciende, que acá es la Prestadora. Sin pantalla no hay aviso, y sin aviso la llave no se enciende en ninguna Prestadora real |
| ~~58~~ | **Cerrado el 25 de agosto de 2026: la puerta existe.** El Desarrollador había escrito que cada Prestadora «sólo puede verse y habilitarse desde el panel de control de CeltaTech, que es al fin y al cabo el dueño del software y con quien cada una de las prestadoras contrata su uso», y de este lado no había por dónde. La línea de comandos abrió el pendiente diciendo además que ese panel no existía, y **eso era falso**: existe, está publicado y tiene su base. El pendiente pedía siete cosas y terminó siendo dos, porque el Desarrollador puso el límite entre las dos mitades —*«una cosa es el producto, y otra es su manejo comercial, no mezclemos o hacemos líos»*— y cinco de las siete eran comerciales: se le anotaron a CeltaTech en `../../docs/SUGERENCIAS_DESDE_EL_MARKETPLACE.md` y no se construyeron. **Lo construido son las migraciones 0023 y 0024 y la función de borde `alta-y-baja`**, que verifica la firma de cada pedido y sabe hacer dos cosas: dar de alta una Prestadora y dejarla activa, suspendida o cancelada. Probada contra el servidor desplegado con doce comprobaciones (`scripts/probar_alta_y_baja.mjs`), y la primera corrida falló siete de las doce por un error real de la 0023, que es lo que arregló la 0024. De regalo quedó cumplida la regla que el modelo comercial declara no negociable: un producto que no le pregunta nada a CeltaTech no se cae cuando CeltaTech se cae | **Hecho.** Lo único que falta es del otro lado: que el panel de CeltaTech firme con la misma clave, que está en la caja fuerte local de este repositorio |
| 59 | **La puerta reconoce a un cliente por su nombre, y por eso puede juntar dos en uno.** `alta_de_prestadora` (migración 0024) deduce el nombre corto de la razón social y, si ya hay una fila con ese nombre corto, da por hecho que es el mismo cliente y devuelve la que estaba. Eso rompe de las dos maneras: **dos empresas distintas que se llamen igual terminan siendo una sola Organización y compartiendo datos** —que es exactamente lo que prohíbe la regla de la empresa «aislamiento entre Organizaciones»— y **un cliente al que CeltaTech le corrija el nombre entra como uno nuevo**. El identificador no puede salir del nombre porque el nombre no es único y además cambia. **Y CeltaTech ya manda uno**: `suscripcion_id`, en el cuerpo del alta de `../../docs/MODELO_COMERCIAL_CELTATECH.md` §6.1, que la puerta hoy recibe y descarta. De paso cae el argumento con el que se justificó no llevar la lista de mensajes atendidos que pide §6.2: esa lista se evitó apoyándose en el nombre. **Encimado hay un segundo problema, de vocabulario:** las dos funciones y sus mensajes de error dicen «Prestadora», y el 25 de agosto de 2026 el Desarrollador ya había dicho que a estos clientes no se los puede llamar así porque pueden no prestar ningún cuidado (`../../docs/GLOSARIO_PRODUCTOS_CAREONYS.md:18` lo confirma: Prestadora es el cliente que contrata Careonys, no cualquier cliente). Se escribió igual, y quedó guardado en la base, que es lo que «lo que se guarda para siempre se nombra por lo que hace» manda no hacer | El Desarrollador resuelva las dos, que van juntas en una misma migración porque las dos tocan las mismas funciones. **(a) Por dónde se reconoce a un cliente:** la línea de comandos recomienda guardar el `suscripcion_id` que manda CeltaTech, como texto opaco que el producto nunca interpreta —espejo exacto de lo que CeltaTech hace con el `tenant_ref`—, y deduplicar por ahí en vez de por el nombre. **(b) Cómo se llaman las funciones**, que es el §5 de `../../docs/SUGERENCIAS_DESDE_EL_MARKETPLACE.md` todavía abierto: no se propone ninguna palabra nueva; la recomendación es usar la que ya está aprobada y ya nombra esto mismo, **Organización**, que la regla de la empresa «las cuatro entidades que no se mezclan nunca» define como el espacio de datos aislado adentro de un producto y que `../../docs/GLOSARIO_PRODUCTOS_CAREONYS.md:21` separa a propósito de «Prestadora». **Hasta que se arregle, ninguna alta real debe pasar por la puerta**: hoy no pasó ninguna, y la única que hubo la creó y la borró la prueba |
| 60 | **El Tipo de Asistente está escrito en masculino y ahora choca con el género al lado.** Desde el 26 de agosto de 2026 la tarjeta del directorio dice «Enfermero universitario · Femenino · Zona Norte», y las dos palabras se contradicen a la vista. El desajuste ya existía —las etiquetas de `tipo_asistente` en `data/catalogo-vocabularios.json` están todas en masculino—, pero recién se ve ahora que el género aparece pegado. Las salidas son tres y **ninguna la elige la línea de comandos**, porque son decisiones de redacción: dejar la etiqueta neutra («Enfermería universitaria»), guardar dos formas por clave y elegir según el género, o separar las dos cosas en la tarjeta para que no se lean como una frase | El Desarrollador elija una de las tres y el catálogo quede escrito según esa decisión |
| 63 | **El precio y la fecha se escriben siempre a la argentina, aunque la pantalla esté en inglés.** Las tres funciones que les dan forma en `js/texto.js` tienen el idioma escrito adentro: `fechaCorta()` (`js/texto.js:118`), `horaCorta()` (`js/texto.js:135`) e `importe()` (`js/texto.js:152`) pedían todas `Intl` con `'es-AR'`, e `importe()` además anteponía `$` a mano. Se vio el 26 de agosto de 2026 al comprobar `directorio.html` en los tres idiomas: la tarjeta en inglés dice **«$4.500 per hour»**, que en inglés se lee «cuatro dólares con medio», mil veces menos de lo que vale; y «12/08/2026» es el 12 de agosto acá, el 12 de agosto en el Reino Unido y el 8 de diciembre en los Estados Unidos, sin que nada avise cuál. No es cosmético: es un importe y una fecha dichos mal. **Se arregla con el idioma que ya resuelve `Catalogo.idioma`** —está a mano, es el mismo punto único de verdad— y la moneda tiene que salir del dato y no del signo escrito a mano, que es justo lo que pide el pendiente 51. Los 16 chequeos no lo ven, porque para ellos `Intl.NumberFormat('es-AR')` es código y no texto visible. **El 26 de agosto de 2026 se hizo la mitad del idioma.** Las tres funciones piden ahora el idioma a `Catalogo` (`js/texto.js:68`), `importe()` dejó de anteponer `$` a mano y recibe la moneda por parámetro, y el chequeo de frases tiene una regla 6 que se queja de cualquier `Intl` con el idioma escrito adentro (`scripts/verificar_frases.mjs:138`), probada rompiendo el archivo a propósito. Comprobado en el navegador, en las tres: 4500 sale «$ 4.500», «ARS 4,500» y «ARS 4.500»; el 26/08/2026 sale «26/08/2026», «08/26/2026» y «26/08/2026». **La mitad de la moneda sigue abierta y espera al pendiente 51**: mientras `caregivers.hourly_rate` sea un número a secas, la moneda sale de `MONEDA_POR_OMISION` (`js/texto.js:85`), que es un solo lugar pero es igual una moneda supuesta. **Y probándolo apareció algo más:** el arreglo vale al abrir la pantalla, no al cambiar de idioma con la pantalla abierta. `Catalogo.traducir()` sólo toca los elementos que llevan `data-frase` (`js/catalogo.js:791`), y un precio dibujado con `textContent` no lleva ninguno. Comprobado el 26 de agosto de 2026 en `directorio.html`: pasando de castellano a inglés en vivo, «$ 4.500 por hora» queda en «$ 4.500 per hour» —la unidad se tradujo, el número no—, que es justo el «$» que se lee como dólares. Pasa igual con el precio de `perfil.html:403` y con las horas y fechas de `mockup-app.html:534`, `pwa-familia/index.html:1019` y `pwa-asistente/index.html:1050`. Hoy no lo sufre nadie porque todavía no hay selector de idioma a la vista, y ponerlo es la decisión de diseño que sigue esperando; pero el día que lo haya, hace falta que las pantallas que dibujan datos se vuelvan a dibujar cuando el idioma cambia, y eso toca `js/catalogo.js` en sus tres copias más `directorio.html`, `perfil.html`, `mockup-app.html`, `pwa-familia/index.html` y `pwa-asistente/index.html`. Es un cambio de mecanismo repartido, así que va con plan antes de código | Tres cosas, y las tres son distintas. El idioma: **hecho**. El chequeo que lo cuida: **hecho**. Falta que la moneda salga del dato —depende del pendiente 51— y que cambiar de idioma con la pantalla abierta vuelva a escribir los números y las fechas ya dibujados, que necesita plan aprobado antes de tocar código |
| 9 | **El i18n ya funciona; falta mudar 36 de las 47 pantallas.** La regla «i18n desde el día uno» pide `es-AR`, `en` y `pt-BR` desde el primer día, y el 26 de agosto de 2026 se construyó el mecanismo entero: `data/catalogo-frases.json` con **155 frases en los tres idiomas**, los atributos `data-frase`, `data-frase-<atributo>` y `data-huecos` leídos por `js/catalogo.js`, `Catalogo.traducir()` para lo que arma el código, el idioma resuelto por `?idioma=`, por lo guardado o por el navegador, y `scripts/verificar_frases.mjs` con cinco reglas corriendo antes de cada `commit`. Convertidas: los mensajes de error (`js/texto.js`, que ahora clasifica con `claveDeError()` y deja la frase al catálogo), el campo de contraseña (`js/clave.js`, cuyo `revisar()` también devuelve la clave del aviso y no la frase) y el camino entero de la contraseña —`acceso.html`, `recuperar-clave.html` y `nueva-clave.html`—, para que quien no puede entrar pida el enlace y elija una contraseña nueva sin cambiar de idioma en el medio; y `examen.html` entera, que fue la primera con singular y plural y dejó la regla para las que faltan —**dos frases distintas y no una armada con pedazos**—; y `directorio.html` entera el 26 de agosto de 2026, que además convirtió **la barra de arriba y el pie**, los mismos que repiten otras siete pantallas: las claves `nav.*` y `pie.*` que salieron de ahí no se vuelven a escribir, y con ellas se lleva convertida la frase que más se repite en todo el proyecto. La cuenta de resultados (`js/main.js`) escribe ahora la clave y no el texto, así que cambia sola al cambiar de idioma. Aparecieron además dos cosas que no se veían: `scripts/texto_visible.mjs` leía `alt` adentro de `data-frase-alt` y daba por texto a mano la clave del catálogo —arreglado con el mismo `(?<![-\w])` que ya usaba `despejar()`—, y el botón del banner final se lee en mayúsculas, que es una decisión de diseño y pasó al CSS para que la frase viva una sola vez. Y `perfil.html` entera el mismo día, que es la pantalla que se abre al tocar «Ver perfil»: dejarla en castellano rompía el idioma en el clic siguiente, que es la pantalla convertida a medias de `docs/PLAN_I18N.md` §6 estirada entre dos archivos. Trece claves nuevas y nada más: la barra, el pie, la insignia, los reemplazos urgentes, el precio por hora y «Reintentar» son los mismos de la grilla y se piden con las claves que ya existían. El título de la pestaña deja de ser una frase apenas hay un nombre real —se le saca el `data-frase`—, porque si no el catálogo lo pisaba de vuelta al cambiar de idioma. Apareció además que `&gt;`, el signo que separa las migas, se informaba como texto escrito a mano: una entidad de HTML es un signo y no una palabra, y el chequeo de frases ahora las saca antes de mirar. **Y el inventario estaba midiendo mal desde que existe la primera pantalla convertida**: `scripts/inventario_textos.mjs` no sabía nada de `data-frase`, así que el castellano que queda adentro de un elemento ya convertido —el que se ve mientras el catálogo viaja— se seguía contando como trabajo por hacer, y las seis pantallas listas figuraban enteras. Se arregló despejándolas con la misma función que ya usaba el chequeo, que para eso pasó a `scripts/texto_visible.mjs`, el módulo que existe justo para lo que miran dos guiones. **Lo que falta no eran 733 frases distintas sino 665**, y las seis pantallas convertidas bajaron de 168 apariciones a 3, que son tres cadenas del guion que el inventario confunde con texto —dos claves del catálogo y el nombre de un vocabulario—. **El chequeo dice cuánto falta**: «6 de 45 archivos ya convertidos». Las fechas, en cambio, ya estaban en un solo lugar: `Texto.fechaCorta()` (`js/texto.js:118`) es el único lugar del proyecto donde se le da forma a una fecha. **Sale de este grupo el 26 de agosto de 2026**: la decisión 3 del plan se tomó como se recomendaba —esto se hace **antes** de React, porque cada pantalla nueva lo encarece y la migración va a agregar pantallas. **Se frenó en 6 de 45, y el 26 de agosto de 2026 volvió a arrancar en 10 de 46.** Las diez pantallas que quedaban con texto estaban todas trabadas por algo que decide el Desarrollador, y ninguna se puede convertir a medias: la regla 4a de `scripts/verificar_frases.mjs` pide que en una pantalla convertida no quede texto escrito a mano. **Cerrar la parte de los carteles falsos del pendiente 64 destrabó cinco de esas diez**, y cuatro se convirtieron el mismo día: `index.html`, `cursos.html`, `soporte-remoto.html` y `solicitar-asistente.html`, **205 frases nuevas** que dejan el catálogo en 460. La quinta, `formulario-integral.html`, se dejó como está a propósito: está sentenciada a borrarse en ese mismo pendiente, y traducir 119 frases de una pantalla que se va es trabajo tirado. **Lo que falta bajó de 665 frases distintas a 479.** **Y el 27 de agosto de 2026 se descubrió que ni siquiera eran 479, sino 414**: el inventario contaba como texto 92 cosas que no lo son —claves que se le piden al catálogo, valores que viajan al servidor, atributos adentro de plantillas de marcado y pedazos de plantilla cortada—. De los 122 textos que decía sacar de los guiones, 30 eran texto. Las seis familias de ruido y cómo se reconoce cada una están en `docs/PLAN_I18N.md` sección 2.2.1; el criterio es que **ninguna se reconoce mirando si la cadena parece una frase**, sino por lo que el proyecto ya escribió alrededor. Comprobado al revés, con un archivo de mentira donde cuatro frases tienen que aparecer y dos cosas que no son texto no; y las categorías que el arreglo no toca dieron 588 apariciones antes y 588 después. Los siete archivos que quedan son exactamente los seis trabados más `js/main.js`. **Y el 28 de agosto de 2026 apareció que `js/main.js` tampoco se puede convertir hoy**, contra lo que este renglón daba a entender al ponerlo aparte de los trabados: sus seis frases que quedan son exactamente las cinco del modal de videollamada más el nombre inventado, o sea el pendiente 87 entero, que espera decisión del Desarrollador —medido con `node scripts/inventario_textos.mjs --detalle`—. Traducir un texto que promete un cifrado que el producto no hace sería dejarlo prolijo en tres idiomas en vez de sacarlo. **Así que este pendiente está trabado del todo**: no queda ningún archivo que se pueda convertir sin contestar algo antes. **Y convertirlas mostró tres cosas que ninguna pantalla sola habría mostrado.** Una: `scripts/texto_visible.mjs` contaba como texto visible el `value` de un redondel, de un casillero, de un campo escondido y de una opción de lista, que no se leen en la pantalla —son el dato que viaja al servidor, y `js/formulario-consulta.js` compara `novedades === 'si'` letra por letra—. Traducirlos rompía el formulario en los tres idiomas a la vez. Se arregló en `sinValoresGuardados()` (`scripts/texto_visible.mjs:42`), que deja afuera el `value` de un botón porque ése sí se lee, y con una prueba de seis casos donde dos tienen que fallar. **La misma regla estaba escrita dos veces**: `scripts/inventario_textos.mjs` tenía su propia copia, más floja, que no miraba el `type`; ahora consume la compartida. Dos: **los moldes que llena la oferta no se traducían nunca.** `Catalogo._llenarOferta()` clonaba el `<template>` y llamaba a `Identidad` pero no a `Catalogo.traducir()`, así que lo que cuelga de un molde —21 `data-frase` en `cursos.html`, el cartel «Próximamente» de `index.html`— se veía siempre en castellano, dijera lo que dijera el resto de la pantalla. Se agregó la llamada (`js/catalogo.js:574`) y se comprobó en el navegador: en inglés la tarjeta dice «8 hours / Certificate / ENROL». Tres: `campo()` devolvía `String(objeto)` —o sea «[object Object]»— para cualquier campo que trajera sus tres idiomas colgando; hoy pasa por el mismo criterio de idioma que el nombre. **Lo que sigue trabado son cinco pantallas y una capa entera.** `pwa-familia/index.html` y `mockup-app.html` están en el pendiente 65 —afirman un estado que no consultaron—; `registrar-asistente.html:536` pide aceptar unos Términos y Condiciones que sólo existen en castellano y que el pendiente 49 dice que ni siquiera habría que estar mostrando; `panel-prestadora.html:176` trae un aviso de responsabilidad legal, que la regla del riesgo legal no deja escribir en otros dos idiomas sin el documento de cada país; y `formulario-integral.html` espera el borrado. **Y cuando se destraben, el sello del pie ya tiene clave**: las nueve «Powered by» que quedan —en `mockup-app.html`, `pwa-asistente/index.html` y `pwa-familia/index.html`— reusan `pie.sello_producto` y **no inventan una clave nueva**, partidas en dos elementos como quedó el de `js/apiClient.js:160`: el rótulo por un lado y el nombre de la Prestadora por el otro, porque el catálogo escribe el texto entero del elemento marcado y compartirlo borraría el nombre. **Y la capa: el contenido de los datos.** `data/catalogo-oferta.json` (9 servicios, 6 cursos y 1 evaluación) y los 142 ítems de los 24 vocabularios de `data/catalogo-vocabularios.json` están sólo en castellano. El mecanismo ya los soporta —`Catalogo.textoDe()`, `js/catalogo.js:382`—: los campos están vacíos, nada más. Comprobado en el navegador el 26 de agosto de 2026: `cursos.html?idioma=en` tiene el marco entero en inglés y las seis tarjetas de curso en castellano. Traducir cualquiera de esos textos lo multiplica por tres, así que se paró, se anotó, y se hizo el mismo día: **218 textos quedaron en los tres idiomas** —24 títulos de vocabulario, los 142 ítems, 10 bajadas, y los 43 textos de la oferta: 9 servicios, 6 cursos y la evaluación entera con sus preguntas y sus opciones—. Comprobado en el navegador, no supuesto: `cursos.html?idioma=en` dibuja «Introduction to caring for older adults / Basic level», y `directorio.html?idioma=pt-BR` abre sus cuatro desplegables en portugués —«Todas as zonas», «Assistente / Cuidador domiciliar», «Todas as patologias», «Endereço comprovado»—. El juntador comprueba antes de escribir que el `es-AR` coincida **byte a byte** con el original —se comparan `Buffer`, no cadenas, para que una tilde descompuesta no pase—, que no falte ni sobre ninguna clave mirado en las dos direcciones, y que `en` y `pt-BR` estén y no estén vacíos; y se probó a propósito contra cuatro copias rotas —falta una clave, sobra una clave, `es-AR` cambiado, `pt-BR` vacío— para que la comprobación pueda fallar de verdad. **Una sola quedó sin traducir, a propósito**: «Guardia de 12 horas» (`data/catalogo-vocabularios.json`, vocabulario `modalidad_contratacion`). Guardia es palabra del glosario de los dos productos y no está traducida en ningún archivo del proyecto; la palabra obvia en inglés, *shift*, ya está tomada por «turno», y el glosario dice que una Guardia **no** es un turno, así que reusarla pisaría una distinción que el glosario define a propósito. Se dejó sin `en` ni `pt-BR` —así `Catalogo.textoDe()` cae al castellano y no se ve ningún cartel raro— y la decisión queda abierta. **Y traducir los datos mostró que el inglés y el portugués no estaban parejos entre catálogos**, porque cada archivo se escribió aparte: `Asistente` era «Caregiver» en las 47 apariciones del catálogo de frases y «assistant» en tres sueltas de `catalogo-autorizaciones.json` y `catalogo-disponibilidad.json`, y «assistente» en minúscula en portugués; la ortografía inglesa mezclaba británico y americano —«authorised» y «recognised» conviviendo con «specialized» y «Personalized», «wellbeing» con «well-being»—; y la Matrícula era «Matrícula» en `catalogo-fichas.json`, que en portugués de Brasil es la inscripción a un curso y no la habilitación profesional, contra «registro profissional» en los otros dos. Se emparejaron los trece, eligiendo en cada caso la forma que ya dominaba. Sale de eso `servicios.asistente_virtual`, que dice «Virtual assistant» a propósito: ahí Asistente no nombra a la persona del glosario sino a un programa. **Y el 30 de agosto de 2026 se convirtió `pwa-asistente/index.html`, que era una de las trabadas.** Son los ciento ocho textos escritos a mano que el propio archivo tenía anotados como otra tarea: 109 cambios de marcado —121 atributos `data-frase*` que piden 106 claves distintas— y **93 claves nuevas**, que dejan el catálogo en 576 frases y el chequeo en «11 de 47 archivos ya convertidos». Lo que falta bajó de 414 frases distintas a 349. **La traba del pendiente 49 no se levantó, y hay que decirlo así**: la casilla del paso 5 sigue siendo obligatoria —`required` en `pwa-asistente/index.html:669`, y el guardado la vuelve a exigir en `pwa-asistente/index.html:2179`— sobre unos Términos que sus propios borradores declaran sin revisión profesional. Lo que cambia es el alcance de la traba **en esta pantalla**: acá el texto es sólo un rótulo y **no enlaza el documento**, a diferencia de `registrar-asistente.html:536`, así que traducirlo no promete en inglés ni en portugués un documento que no existe en esos idiomas. Traducir el rótulo y sacar la casilla son dos trabajos distintos, y el segundo sigue esperando al abogado. **Convertirla obligó además a tres decisiones que no son traducción.** Una: el ejemplo del campo de medicamentos decía «Ej: Donepecilo 10mg», o sea un principio activo y una dosis escritos por el producto; quedó «Ej: qué se administró y a qué hora» (`reporte.medicamentos_ejemplo`), que es la misma línea entre avisar y prescribir que sostienen las Guías de cuidado. Dos: las tres «Powered by» de esta pantalla pasaron a `pie.sello_producto` —«Con la tecnología de»—, partidas en dos elementos como manda este mismo renglón más arriba, y el nombre del producto quedó en `comun.producto`, que junto con `comun.organizacion` son los dos marcadores de marca declarados en `IGUALES_EN_TODOS` (`scripts/verificar_frases.mjs:80`) porque no son texto sino el hueco donde `js/identidad.js` escribe el nombre de verdad, igual en los tres idiomas. Tres: el nombre y el correo inventados que traía el cajón —«Asistente de Prueba», «asistente@ejemplo.com»— se sacaron en vez de traducirse: los escribe la sesión, y una frase del catálogo los habría vuelto a pintar encima en cada pasada. Y `legajo.espera_texto` se reescribió para que la dirección de correo no viva en el medio de la oración, porque `despejar()` (`scripts/texto_visible.mjs:85`) sólo despeja el texto que no tiene nada anidado adentro. **Apareció también un texto que la regla 4a no ve**: el renglón vacío de una lista desplegable vive en el atributo `data-catalogo-vacio`, así que el chequeo daba la pantalla por limpia con tres listas del legajo todavía en castellano. Se marcaron con `data-frase-data-catalogo-vacio`, que es como ya lo hacían `directorio.html:79` y `cursos.html:171`, y salieron de ahí `comun.seleccionar` y `legajo.elegir_tipo`. **Y comprobarlas en el navegador destapó otra cosa**: la pantalla seguía en castellano con las frases ya publicadas, porque el archivo lo contestaba la copia que el programa del teléfono guarda para andar sin conexión, y ese número se subía a mano. Lo cuida desde ese día `scripts/verificar_sinconexion.mjs`. **La pantalla arranca ahora con `Catalogo.aplicarEnDocumento()` y no con `Catalogo.cargarFrases()`**, y en ese orden a propósito: `ClienteDatos.initTenant()` llama a `Identidad.resolverOrganizacion()`, que barre el documento resolviendo `{{organizacion}}` sobre el texto que ya esté escrito, así que si el catálogo escribe después, los marcadores quedan sin resolver. Comprobado en el navegador y no supuesto: en `pt-BR` el título de la pestaña dice «Careonys Assistente de Cuidado — Portal Móvel», el cajón y el acceso están en portugués, los ejemplos de los campos también —«E-mail», «Buscar uma patologia», «Ex.: o que foi administrado e em que horário»— y los dos marcadores de marca siguen resueltos en «Careonys». | Las 47 pantallas lean del catálogo y el chequeo diga «47 de 47». **El contenido de los datos ya está**, que era la mitad que no se ve desde el chequeo de frases —una pantalla traducida que muestra tarjetas en castellano no está traducida—: los dos catálogos de datos quedaron en los tres idiomas el 26 de agosto de 2026, salvo «Guardia de 12 horas», que espera una palabra. **Falta además el selector visible de idioma**, que no se puso porque dónde va en el encabezado de catorce pantallas es una decisión de diseño y ésas se consultan; y **quién traduce las 349 frases que faltan**, que es la única decisión abierta del plan (`docs/PLAN_I18N.md`, sección 5, punto 2). Las cinco palabras del negocio que la línea de comandos tradujo sola —Prestadora, Asistente, legajo, Aviso y Acompañamiento— están en la sección 5.1 de ese archivo para que el Desarrollador las confirme. **Y aparece una sexta, que no se tradujo porque no es del negocio sino de la marca: «Careonys Academy»**, el nombre del espacio de cursos, escrito en inglés en `cursos.html:121` desde antes de esta conversión. Se dejó igual en los tres idiomas —está en `IGUALES_EN_TODOS` de `scripts/verificar_frases.mjs`, que es la puerta que la regla deja abierta para los nombres comerciales—, pero **la tabla de equivalencias del glosario de la empresa pide castellano existiendo la forma castellana**, y «Academy» no pasa las cinco preguntas de una palabra nueva. Que quede o que se traduzca lo decide el Desarrollador. **Y una séptima, del otro lado**: `tipo_asistente/cuidador_domiciliario` dice «Asistente / Cuidador domiciliario», o sea el término general del glosario y un tipo de Asistente en el mismo rótulo. En inglés eso choca de frente, porque Asistente **es** «Caregiver»: la traducción literal daría «Caregiver / Home caregiver». Se escribió «Caregiver / **Home carer**», que es la forma corriente del inglés británico que el proyecto ya usa —«authorised», «recognised», «licence»— y que evita «worker», prohibida por el glosario para Asistente. Conviene confirmarla |
| 64 | **Cinco formularios públicos contestaban que mandaron algo, y no mandaban nada. El 26 de agosto de 2026 los cinco pasaron a decir la verdad, y la mitad del pendiente sigue abierta.** **Lo que el Desarrollador contestó.** Primero, que `formulario-integral.html` se va: textual, «formulario integral me parece que es un elemento outsider que intentó hacer algo pero que sólo podés usar como referencia por si alguno de los ítems que figura allí falta en el formulario de reclutamiento de asistentes, fuera de eso no tiene utilidad». Y después, sobre a dónde mandar lo que juntan `cursos.html` e `index.html`: «usá el sentido común y enviálos a donde sea coherente». **Lo que se midió antes de decidir, y cambió la respuesta.** El plan era hacer que los cuatro guardaran como guardaba `solicitar-asistente.html`, que era el único que lo intentaba. Se probó ese mismo alta contra la base local, con datos inventados, y **no funciona para nadie que llegue al portal**: sin sesión contesta **401, «permission denied»** —`anon` no tiene permiso sobre la tabla— y con una cuenta recién creada contesta **403**, porque la política «Busquedas de la Prestadora» exige `tenant_id = prestadora_actual()` (`supabase/migrations/0002_aislamiento_por_prestadora.sql:87`) y una cuenta nueva no tiene Prestadora. La prueba trae su sostén —la misma fila con una sesión válida— así que podía salir en verde y no salió. O sea que **hoy ninguna consulta del portal puede llegar a la base**, y el que decía la verdad la decía siempre en su forma mala. Abrirle la tabla a quien no inició sesión es ampliar el acceso anónimo, que es decisión del Desarrollador —el punto dos de esta misma fila—, no de esta tarea. **Qué se hizo entonces.** Un solo guion para los cinco, `js/formulario-consulta.js`, que hace una cosa y la dice como es: si hay sesión **y** se resolvió una Prestadora guarda la consulta —el único caso en que el guardado puede funcionar, y el que va a andar solo el día que se abra la puerta—, y si no, muestra el correo del producto con la consulta ya redactada adentro y deja que la persona la mande. **Nada dice «enviado» hasta que algo se envió.** Los cinco estados que ve una persona salen del catálogo en los tres idiomas, el botón se apaga mientras corre, y el motivo va a `consultation_reason`, que la migración 0013 creó justamente para distinguir a quien busca cuidado de quien pregunta por un curso. Las cinco pantallas llevan `data-consulta` (`index.html:266`, `cursos.html:153`, `soporte-remoto.html:142`, `solicitar-asistente.html:339`, `formulario-integral.html:295`) y `js/main.js:45` ya no engancha ningún formulario de consulta: se le borró la rama que prendía el cartel verde sin llamar a nadie. **(f)** El panel de videollamada de `js/main.js:286` sigue como estaba: arma una sala que no conecta nada con nadie y no lo abre ningún botón. **Lo que queda abierto, y es todo del Desarrollador.** Uno: **el consentimiento de novedades lo siguen preguntando los cinco y no lo recibe ninguna tabla**; hoy viaja adentro del correo, que es mejor que perderse, pero un consentimiento guardado a medias es peor que no preguntarlo. Dos: **si `avisos` se abre o no** a quien no inició sesión, y por cuál de las dos puertas —una tabla nueva con política de alta, o una función de borde como la de `supabase/functions/alta-y-baja/index.ts`—. Tres: **el borrado de `formulario-integral.html`**, que no se hizo, porque adentro tiene lo único que publica un aviso de verdad —el paso a paso de `formulario-integral.html:95`, por `js/main.js:202`— y ese paso a paso choca con la misma pared: también exige Prestadora resuelta. Antes de borrar hay que mudarlo o comprobar que otra pantalla hace lo mismo. **La comparación de campos ya está hecha**, el 26 de agosto de 2026: de los seis que pregunta `#form-registro-asistente` (`formulario-integral.html:341`), cinco tienen equivalente en `registrar-asistente.html` —y `zona` es más estricto allá, un desplegable obligatorio del vocabulario en vez de texto libre—; **el único que no está en ninguna parte es el consentimiento de novedades** (`formulario-integral.html:363`), que tampoco tiene columna en ninguna tabla del esquema y es el punto uno de esta misma lista. O sea que, salvo ese, borrar el formulario integral no pierde ningún campo. Cuatro: **`#form-registro-asistente` (`formulario-integral.html:341`) sigue sin que lo escuche nadie**; el navegador lo manda por omisión y la página se recarga. No viaja ningún dato personal en la dirección —los campos no tienen `name`—, pero lo escrito se pierde. Se deja así a propósito: la pantalla está sentenciada y arreglarla es trabajo tirado | El Desarrollador conteste las tres que quedan —el consentimiento de novedades, si se abre la puerta sin sesión, y qué pasa con el paso a paso antes de borrar `formulario-integral.html`—, y ninguna pantalla conteste que mandó algo que no mandó. **La parte de los carteles falsos ya está cerrada**, así que las cinco pantallas quedan libres para convertirse al i18n: lo que hoy escriben sale del catálogo en los tres idiomas |
| 65 | **Dos pantallas muestran el estado de una persona sin haberlo consultado, y una tercera cosa se pregunta y se tira.** Salió al elegir la próxima pantalla del i18n. **(a)** `pwa-familia/index.html:633` y `mockup-app.html:141` traen escrito «Complete las Verificaciones — Email: ✓ Validado | Celular: ⌛ Pendiente». Ahí no escribe ningún guion: comprobado el 26 de agosto de 2026, `verification-widget` sólo aparece en el CSS de esas dos pantallas y en su propio HTML. Quien entra lee que su correo está validado y su celular no, diga lo que diga la base. **(b)** `pwa-familia/index.html:724` y `mockup-app.html:350` traen una ficha de paciente entera escrita a mano —«Abuela Rosa», «Diagnóstico: Alzheimer Moderado»— que tampoco escribe nadie. En `mockup-app.html` el tercer renglón dice «Asistente: María Gómez» y en `pwa-familia` dice «Familiar a cargo: Abuela Rosa»: al copiar se cambió el rótulo y no el valor, así que la paciente figura como su propio familiar a cargo. Y justo debajo, la lista de reportes sí sale de la base y explica bien por qué está vacía (`pwa-familia/index.html:1671`): la pantalla muestra un diagnóstico inventado arriba y la verdad abajo. **Esto desmiente lo que daba por comprobado el pendiente 14**, que ya quedó corregido. **(c)** Y `pwa-familia/index.html:773` pregunta la edad del paciente en `pb-edad`, que no lee nadie: el guion que publica el aviso (`pwa-familia/index.html:1840`) manda nombre, zona, modalidad, franjas, patologías y descripción, y la edad se pierde al enviar. `pb-edad` aparece una sola vez en todo el proyecto | El Desarrollador decida qué muestra cada uno de esos dos recuadros —el estado real de las verificaciones, el Paciente real de esa cuenta, o ninguno de los dos— y qué se hace con la edad: que viaje al aviso o que no se pregunte. Ninguna pantalla afirme un estado que no consultó. **Mientras tanto no se convierten al i18n `pwa-familia/index.html` ni `mockup-app.html`**: la regla 4a de `scripts/verificar_frases.mjs` no deja convertir media pantalla, así que traducir el resto obliga a traducir también lo inventado |
| 96 | **No existe la pantalla donde una Prestadora agrega una opción propia.** Desde la migración 0038 el catálogo tiene dos escalones —el general que trae el producto y el que agrega cada Prestadora—, y el segundo funciona: `presdemo` tiene «Esclerosis múltiple» e «Insuficiencia renal en diálisis», `cuidarnorte` tiene «Fibromialgia», ninguna ve la de la otra y las tres ven el catálogo general (comprobado el 29 de agosto de 2026 contra la base local). **Pero esas filas entraron por la migración 0040**, porque no hay ninguna pantalla que las cargue. Las Prestadoras ficticias se tratan como clientes reales, así que un dato que hoy sólo se puede cargar escribiendo un archivo `.sql` es una pantalla que falta, no una comodidad de desarrollo. Del lado de la base ya está todo: las políticas dejan a cada Prestadora dar de alta, cambiar y borrar **sólo lo suyo**, y un disparador rechaza tocar el catálogo general o colgarse del vocabulario de otra | Una Prestadora pueda agregar, cambiar y desactivar sus propias opciones desde la aplicación, sin pasar por una migración. Y la lista `cerrada` siga sin admitirlas, que es lo que hoy hace el disparador `el_vocabulario_no_cruza_prestadoras` |
| 98 | **El producto promete avisar antes de que venza una matrícula, y no hay nada detrás.** `data/catalogo-fichas.json:44-46` dice, en los tres idiomas, *«Se avisa antes de que venza. Una matrícula vencida inhabilita para atender.»*, y lo lee toda persona que carga su legajo. Hoy no existe ni el mensaje ni el control de vencimiento: la fecha se guarda y nadie la mira. Son dos promesas y las dos están sin cumplir, porque tampoco hay nada que inhabilite a quien tiene la matrícula vencida. **El 31 de agosto de 2026 se escribió el inventario y el plan**, que son los dos primeros pasos de «antes de un cambio grande»: `docs/PLAN_VENCIMIENTOS.md`. **Está sin aprobar y no se escribió nada de código.** De ahí salieron cuatro cosas que no se veían. Una: **el esquema fue escrito esperando esto** — `verificaciones_asistente.estado` ya acepta `vencido` y nadie lo escribe nunca (`supabase/migrations/0004_legajo_matricula_verificaciones_banderas.sql:143`), y hay un índice construido para una consulta que nadie escribió (`:237`). Dos: **el catálogo ya marca el campo** con `"vigencia": true` (`data/catalogo-fichas.json:43`) y ningún guion lee esa marca. Tres: **la mitad grande no necesita nada de lo que está trabado** — que un papel vencido saque del directorio se calcula al leer, sin tarea programada, sin correo y sin palabra nueva; lo que espera es sólo el mensaje que sale hacia afuera. Cuatro: **«Aviso» está tomado** (`docs/GLOSARIO.md:15`), así que cómo se llama lo que el sistema manda es una palabra a decidir y no se inventa | El vencimiento lo controle el sistema y dispare la notificación, como pidió el Desarrollador: *«de esto se debe ocupar el sistema y disparar alarmas, pedidos a los asistentes… la Prestadora debe poder configurar muy bien todo para no tener que andar de la rutina después»*. Con cuánta anticipación avisa y a quién lo configura la Prestadora, no lo decide el código. Va junto con el punto 68 |
| 99 | **Una opción del catálogo está en un solo idioma, a propósito y a la vista.** `modalidad_contratacion` / `guardia_12` no tiene `en` ni `pt-BR`, y no es un descuido: «Guardia» es una palabra del glosario de los dos productos y no está traducida en ninguna parte; usar «shift» o «turno» pisaría una distinción que el glosario hace a propósito. Desde la migración 0038 el agujero **está declarado, no escondido**: la columna `i18n_pendiente` lo marca y la nota obligatoria dice por qué, así que se lo puede listar con `select clave, nota from vocabulario_items where i18n_pendiente;`. Es la única fila así de 142 | El Desarrollador decida cómo se dice «guardia» en inglés y en portugués, o decida que se deja en castellano en los tres idiomas. Cargados los dos textos, `i18n_pendiente` vuelve a falso y el `check` de la migración 0038 pasa a exigirle los tres idiomas como a las demás |
| 102 | **La Guía que escribe una Prestadora no llega al teléfono de su gente sin señal, y la general tampoco.** Desde la migración 0041 las guías salen de `guias_de`, que es una llamada a la base: sin conexión no hay guía. El catálogo resolvió esto con una copia en `data/catalogo-vocabularios.json` que viaja a los dos programas para teléfono, pero **acá esa salida no sirve tal cual**: el archivo se le entrega a cualquiera, y lo que escribió una Prestadora sobre cómo cuidar a sus Pacientes no puede repartirse a los teléfonos de otra. Son dos cosas ciertas que se empujan: la guía general puede viajar como viaja el catálogo, y la propia no. Y el momento en que más se necesita —una casa, una emergencia, un sótano sin señal— es justo cuando la llamada falla | Se decida y se construya cómo llega la guía al teléfono. La general puede sumarse al archivo que ya se genera. Para la propia hay que elegir: guardarla en el teléfono al iniciar sesión, con lo que queda escrita en un aparato que puede perderse; o aceptar que sin señal se vea sólo la general, y decirlo en la pantalla en vez de mostrar el hueco. **La decisión es del Desarrollador**, porque cambia qué dato sale de la base hacia un aparato |
| 104 | **Las diecinueve Guías de cuidado generales están escritas, presentadas para revisar, y ninguna publicada todavía.** La migración `supabase/migrations/0042_las_diecinueve_guias_generales.sql` cargó las 19 con sus cuatro partes en `es-AR`, `en` y `pt-BR`, como borrador —`publicada = false`—, porque la base exige quién la revisó y cuándo para poder publicarla (`supabase/migrations/0041_las_guias_de_cuidado.sql:145`) y **esa firma no la puede poner una migración**. El 31 de agosto de 2026 el Desarrollador decidió quién firma: *«yo haré una primera revisión de las mismas y también quedarán, al igual que los documentos legales, para cuando todo esté concluido, mientras tanto las que revise yo serán las válidas. Con el tiempo se podrán agregar o quitar o modificar las guías, al igual que los documentos legales, son documentos vivos»*. Con eso se escribió `docs/GUIAS_PARA_REVISAR.md`, que presenta las 19 en castellano para leerlas de corrido, en el orden del catálogo general, con la clave y el renglón de la migración al pie de cada una, y con una lista final de los nueve puntos que conviene mirar dos veces —seis rozan el borde de «ninguna maniobra clínica» y tres son de redacción—. **Falta que las lea y diga qué corrige**; después va una migración que aplica sus correcciones y las publica con `revisada_por` y `revisada_el`. Mientras tanto la puerta `guias_de` sólo devuelve publicadas, así que la aplicación muestra «todavía no hay guías» y no un texto sin firmar | El Desarrollador haya leído las diecinueve y dicho qué corrige, y una migración las publique con `revisada_por` y `revisada_el` escritos. **Ninguna pasa a `publicada` sin ese paso**, ni siquiera para probar la pantalla. Al concluir el proyecto van a revisión profesional, como los documentos legales |
| 105 | **La Guía de cuidado está en una biblioteca y no al lado del Paciente al que se va a cuidar.** La pantalla nueva de la aplicación del Asistente —`pwa-asistente/index.html:777`— muestra **todas** las guías publicadas, con un buscador. Está bien para consultar, y no es donde hace falta: quien entra a una casa no tiene que acordarse de buscar la patología de esa persona. La biblioteca se construyó así porque **hoy no existe ninguna pantalla donde el Asistente vea al Paciente que va a atender** —la aplicación tiene cuatro pantallas y ninguna es ésa—, así que no hay dónde colgar la guía. Es una carencia de ubicación, no de contenido: el texto ya llega bien y el aislamiento ya está probado | Exista la pantalla del Paciente asignado y la guía de su patología aparezca ahí, abierta y sin buscarla, además de quedar la biblioteca para consultar el resto. Se hace junto con esa pantalla, no antes |
| 143 | **La puerta del alta está declarada y no la cierra nadie, y la regla como está escrita se muerde la cola.** La puerta de la publicación ya se cerró —migración `supabase/migrations/0061_la_puerta_de_publicacion_se_cierra.sql`, el 2 de septiembre de 2026—: un legajo sin los antecedentes penales y el certificado de salud comprobados no aparece en el directorio, y si su tipo de Asistente lo exige, tampoco sin la matrícula y el título. Lo que queda abierta es **la otra puerta**. `data/catalogo-verificaciones.json` dice que el documento de identidad frena el **alta** —«sin saber quién es la persona, nada de lo demás significa nada»—, y ahí hay un problema que no es de programación: el mismo catálogo define el alta como **terminar de cargar el legajo**, que lo hace el Aspirante, mientras que la verificación `dni` la marca **la Prestadora**, que recién mira el legajo después. Cerrada tal como está escrita, ningún alta se podría terminar hasta que alguien de la Prestadora abra el documento, y hasta entonces la persona no tendría dónde esperar. **Y las dos salidas razonables son decisiones de negocio, no de código:** que el alta se cierre con el documento *cargado* y la verificación quede para la validación, o que el legajo tenga un estado intermedio de «cargado y sin mirar» que hoy no existe. **No se elige ninguna sin el Desarrollador.** El estado `en_revision` de `caregivers` existe desde la migración 0001 y podría ser eso, pero hoy significa otra cosa —«la Prestadora todavía no lo validó»—, y usarlo para las dos cosas deja sin distinguir «no cargó los papeles» de «los cargó y nadie los miró». **No incluye los plazos**, que son el pendiente 98 y tienen su propio plan en `docs/PLAN_VENCIMIENTOS.md` | El Desarrollador diga qué frena el documento de identidad: el fin del alta, la validación, o un estado nuevo entre las dos. Con esa palabra, la puerta `alta` se cierra igual que la otra —la regla ya vive en `extra.puerta` de `vocabulario_items`, no hay que escribir ninguna condición nueva en el código— y se prueba con las dos Organizaciones ficticias y con la prueba pudiendo fallar |
| 73 | **Dos vocabularios del catálogo no los usa ninguna pantalla.** `verificacion` y `discapacidad` están definidos en `data/catalogo-vocabularios.json`, con sus opciones en los tres idiomas, y no los pide nadie: ni por atributo del marcado, ni desde una ficha del legajo, ni desde el guion. Eso no es un defecto —por eso `scripts/verificar_usos.mjs` los informa y no rompe la construcción—, es una pregunta sin contestar: o falta la pantalla que los iba a usar, o sobran ellos. **La otra mitad de este pendiente ya está hecha:** la lista `usado_en` se calcula desde el código y hay un chequeo que rompe la construcción cuando el catálogo dice una cosa y el marcado hace otra. **Y de paso se corrigió una cuenta de este mismo renglón:** decía que eran cinco los vocabularios sin usar, y son dos. Los otros tres —`tarea_hogar`, `tarea_acompanamiento` y `puesto_experiencia`— sí se usan, desde `data/catalogo-fichas.json`, que es por donde las fichas del legajo declaran sus campos; la primera medición no miraba ahí | El Desarrollador decida, para cada uno de los dos: se borra del catálogo, o se anota qué pantalla lo va a pedir y queda como pendiente aparte |
| 79 | **Un buscador lee `{{organizacion}}` donde debería leer un nombre.** Los marcadores se resuelven en el navegador, así que lo que el servidor entrega lleva el marcador crudo: el `<title>` y la `<meta name="description">` de cada pantalla se sirven con `{{organizacion}}` adentro. Quien abre la pantalla ve bien —comprobado el 26 de agosto de 2026 en `careonys-marketplace.vercel.app`: título, descripción y pie dicen «Careonys» y no queda ningún marcador—, pero un buscador que no ejecuta guiones indexa el marcador. Antes indexaba «PresDemo», que estaba igual de mal y además nombraba a una Prestadora; esto no empeora nada, pero tampoco lo arregla. **El que sirve la pantalla sí sabe cuál es la Prestadora**, porque está en el subdominio o en `?t=`: puede resolver el marcador antes de entregarla. | El `<title>` y la `<meta description>` que entrega el servidor lleguen ya con el nombre de la Prestadora resuelto, y se compruebe pidiendo la pantalla sin ejecutar guiones |
| 84 | **El vocabulario de condición fiscal nombra un organismo que cambió de nombre, y es de un solo país.** `condicion_fiscal` (`data/catalogo-vocabularios.json`) nombra a la AFIP, que en Argentina pasó a llamarse ARCA. **No está verificado contra la fuente oficial en esta sesión** —se señala, no se afirma—, y cambiarlo sin verificar sería peor que dejarlo. Aparte de eso, las tres opciones —monotributo, monotributo social, responsable inscripto— son categorías legales argentinas que no tienen equivalente en ningún otro país: traducirlas al inglés o al portugués sería inventar figuras impositivas que no existen, así que quedaron idénticas en los tres idiomas a propósito. Es exactamente el caso que pide la regla de los cálculos legales parametrizados por jurisdicción: el día que se venda en un segundo país, esta lista no se traduce, se reemplaza por la de ese país. **Verificado y corregido el 28 de agosto de 2026 la mitad que no esperaba a nadie.** El organismo cambió de nombre: el Decreto 953/2024, publicado en el Boletín Oficial el 25 de octubre de 2024, disuelve la AFIP y crea en su lugar la **Agencia de Recaudación y Control Aduanero (ARCA)**. Se leyó el texto de los artículos 1 y 2 en el propio Boletín Oficial, no un resumen. **Y el alcance era más ancho de lo que decía este renglón**, que sólo nombraba al vocabulario: la sigla estaba además en la frase `solicitar.cobertura_punto_facturacion` de `data/catalogo-frases.json` —en los tres idiomas— y en el rótulo escrito a mano de `registrar-asistente.html`. Se corrigieron los tres, en los tres idiomas, y se refrescaron las cuatro copias que `scripts/verificar_copias.mjs` declara. De paso el identificador del campo pasó de `afip-tipo` a `condicion-fiscal`: la regla dice que lo guardado se nombra por su función y no por la marca de turno, y un organismo que ya cambió de nombre una vez puede cambiar otra. Comprobado en el navegador en inglés y en portugués. **Lo que sigue abierto es la otra mitad**, que sí necesita decisión: atar la lista a la jurisdicción | Se verifique el nombre del organismo contra la fuente oficial y, si cambió, se corrija; y la lista quede atada a la jurisdicción en vez de escrita como si valiera en todas — el nombre quedó verificado y corregido el 28 de agosto de 2026; falta la jurisdicción, que es la mitad que necesita al Desarrollador. **El 28 de agosto de 2026 el Desarrollador cerró la parte del texto visible**: «ARCA es en Argentina, como pensamos que se pueda comercializar en otros corresponde poner en lugar de *Condición de Registro ARCA* *Condición Fiscal*, que es más universal». Así que el rótulo de `registrar-asistente.html` dice ahora **Condición Fiscal**, y por el mismo criterio dejaron de nombrar al organismo el título del vocabulario —`Condición fiscal` / `Tax status` / `Situação fiscal`— y la frase `solicitar.cobertura_punto_facturacion`, que ahora dice `Facturación electrónica acreditada`. **Las opciones de adentro siguen siendo argentinas**, y eso es lo que queda abierto acá y en el pendiente 92 |
| 85 | **El campo `etiqueta` de los cursos repite lo que ya dice `nivel`, y los dos se van a separar solos.** De los seis cursos de `data/catalogo-oferta.json`, cuatro ponen en `etiqueta` lo mismo que ya está en `nivel` —«Nivel básico», «Intermedio», «Avanzado»— y dos ponen un tema —«Bienestar», «Salud», «Comunicación»—, o sea que el campo mezcla dos ejes distintos. Y ni siquiera se escriben igual entre ellos: el primero dice «Nivel básico» y los otros dos dicen «Intermedio» y «Avanzado», sin la palabra. El dato traducido en dos lugares diverge; ya divergía en castellano. Al traducir se alineó «Nivel básico» con el vocabulario `nivel_curso` para que no se despeguen más, pero eso es un parche: el arreglo es que `etiqueta` diga una sola cosa, o que salga de `nivel` y desaparezca. La `nota` de `nivel_curso` en el catálogo ya avisaba que decidir qué hace ese campo era otra tarea | `etiqueta` tenga un solo significado, o se derive de `nivel` y se borre del archivo |
| 86 | **Tres textos de los vocabularios están escritos distinto de sus vecinos, sin motivo.** `patologia/oncologico` dice «Paciente oncológico» en singular mientras los de al lado dicen «Pacientes anticoagulados» y «Pacientes postrados» en plural; `nivel_reputacion/plata` dice «Asistente senior», que mete una palabra inglesa sin aprobar adentro de una frase en castellano —y la tabla de equivalencias del glosario de la empresa pide castellano existiendo la forma castellana—; y la clave `certificacion/curso_gerontologico` nombra gerontología pero su texto dice «Curso general de asistente de cuidado domiciliario», que es otra cosa: la clave es dato guardado y no se renombra, así que lo que hay que decidir es si el texto está mal o si la clave nombra mal desde el principio. Aparecieron al traducir los 142 ítems uno por uno, que es la primera vez que alguien los leyó todos seguidos | Los tres digan lo que corresponde, y la traducción a los tres idiomas se rehaga sobre el texto corregido |
| 87 | **Hay un modal de videollamada que no lo abre nada, y si algo lo abriera prometería una sala cifrada que no existe.** `js/main.js:286` arma una entrevista por videollamada simulada. La engancha a `document.querySelectorAll('a[href*="video"], .btn-video-call')` (`js/main.js:287`), y **en todo el proyecto no hay un solo elemento que case**: ni una clase `btn-video-call`, ni un `href` que diga «video» en ningún `.html` —medido el 26 de agosto de 2026 con `grep` sobre el marcado entero, cero coincidencias—. O sea que es código que hoy nadie puede alcanzar. Si alguna vez se alcanzara, trae tres cosas mal: dice **«Conectando sala segura 8x8 Encryption...»** (`js/main.js:304`), que promete un cifrado que el producto no hace; nombra a una persona inventada, **«Asistente: Marisa Miranda»** (`js/main.js:305`), escrita adentro del código; y sus cinco textos visibles están a mano, así que `index.html` y sus hermanas no pueden terminar el i18n mientras estén. Los estilos que lo dibujan son otros 75 renglones, de `css/styles.css:1754` a `:1802`. **No se borró por cuenta propia** porque puede ser una función planeada, y borrar una función planeada cuesta más que dejarla anotada. | El Desarrollador diga si la videollamada se construye. Si no: se borran las tres partes —el guion, los estilos y lo que quede— y el chequeo de frases confirma que `index.html` quedó sin texto a mano. Si sí: se construye de verdad, sus textos salen del catálogo y el cartel no promete ningún cifrado que no haya. |
| 88 | **Una cita puede apuntar al renglón equivocado y el chequeo la da por buena.** El 26 de agosto de 2026, con `scripts/verificar_referencias.mjs` en verde —185 citas, todas «apuntando a algo»—, **24 citas apuntaban a un renglón que no era el suyo**: 10 de `docs/ALCANCE.md` a `js/apiClient.js`, 7 más del mismo documento a otros archivos, 6 de esta lista y 1 de `docs/PLAN_I18N.md`. Ya están corregidas, una por una, buscando cada número por el nombre que la propia frase nombra. Lo que queda abierto es **por qué el chequeo no las vio**: sólo rechaza el renglón vacío y la llave suelta (`scripts/verificar_referencias.mjs:87`), y su propio encabezado ya cuenta que se probó exigir que el identificador nombrado en la frase estuviera cerca del renglón citado, y daba tres avisos falsos de cada cinco (`scripts/verificar_referencias.mjs:42`). Se volvió a medir ese mismo día con esa heurística y el resultado la confirma: 107 citas nombran un identificador que existe en el archivo citado, 65 quedan «lejos» y sólo 24 resultaron ciertas. **Lo que traba:** mientras el número lo lleve nada más que la prosa, cualquier renglón agregado arriba corre la cita y nadie se entera hasta que cae justo en una llave. **Y volvió a pasar al día siguiente, con el pendiente ya escrito.** El 27 de agosto de 2026 un arreglo de `js/main.js` corrió diez renglones el bloque de la videollamada, y de las cinco citas que lo nombran el chequeo vio **una sola**: la que fue a caer sobre una llave suelta. Las otras cuatro aterrizaron sobre renglones con algo escrito —una etiqueta de apertura, un `style`— y pasaron como si nada. Se corrigieron a mano, buscando cada una por lo que la frase nombra. **Y el atajo tentador ya se midió y no sirve.** El 31 de agosto de 2026 se probó una regla más fina que la de antes: si a menos de sesenta caracteres de la cita hay un fragmento entre acentos graves, exigir que ese fragmento aparezca tal cual en el renglón citado. Sobre las 305 citas del repositorio, 127 tienen un fragmento así y **sólo 41 coinciden: 86 darían aviso falso**. No se vuelve a intentar por ese lado: mientras el ancla no se escriba a propósito, adivinarla desde la prosa de al lado hace más ruido que trabajo. **Y ese mismo 31 de agosto aparecieron dos casos más, que valen porque son de las dos formas distintas en que una cita se rompe.** El primero es deriva, otra vez, y se la hizo este renglón de al lado: el arreglo que cerró el pendiente 112 metió 27 renglones arriba en `js/apiClient.js`, y las dos citas del pendiente 109 —escritas horas antes, y correctas cuando se escribieron— se corrieron las dos 27 renglones. Las dos aterrizaron sobre renglones con algo escrito, así que el chequeo siguió en verde. Corregidas. El segundo no es deriva y es peor: `docs/PLAN_ZONAS_DE_COBERTURA.md` señalaba el renglón 758 de `js/apiClient.js` como el que traduce la zona —sin acentos graves alrededor del número, a propósito: escrita como cita, esta frase le pediría al chequeo que la resolviera—, y **el día que se escribió el renglón que la traducía era el 727**; el 758 era `llevar('tareas', 'tasks_required')`. O sea que **la cita nació mal**, no se despegó: estuvo tres días en verde señalando otra cosa, y lo que la descubrió fue la casualidad de que un cambio de arriba la hiciera caer sobre un renglón en blanco. Ninguna heurística sobre la prosa habría agarrado ese caso, porque la prosa no nombraba nada que estuviera en el renglón bueno. **Lo que muestran los dos juntos es que el verde de este chequeo no mide lo que parece medir**: dice «apunta a algo», y se lee «apunta a lo que dice». **Y el 31 de agosto de 2026 la mitad de la deriva se cerró midiéndola.** `scripts/verificar_deriva.mjs` no adivina: le pregunta al historial qué decía el renglón citado el día en que se escribió la cita —`git blame` da el commit que escribió ese renglón del documento, `git cat-file` da el archivo citado como era entonces—, y busca ese texto en el archivo de hoy. Encontró **67 citas corridas**, con el otro chequeo en verde sobre las 67, y las 67 están arregladas; estaban en los dos únicos documentos vivos que citan código, `docs/ALCANCE.md` y éste. Al construirlo aparecieron dos cosas más. Una: **la forma corta ``:78`` no la miraba ningún chequeo**, porque no empieza por un nombre de archivo y ninguna de las dos expresiones la reconocía. Eran 22, y dos ya no apuntaban a nada —una a un renglón que sólo cierra una llave, otra a un renglón en blanco—. Ahora heredan el archivo de la cita entera que tienen a la izquierda, y el corpus visible pasó de 332 a 357. Dos: **apareció una segunda cita nacida mal**, el pendiente 35 —cerrado el 31 de agosto de 2026— mandaba a `pwa-asistente/index.html:647` por «Mis Capacitaciones» y esa pantalla empieza en el 634; el 572 era el párrafo de la pantalla de espera. Y una tercera, que es el fenómeno demostrándose solo: **la refactorización que hizo falta para escribir el chequeo rompió tres citas del propio chequeo**, en el mismo rato en que se lo estaba escribiendo. Quedan tres casos que la herramienta no puede resolver —renglones editados en el lugar, donde el número sigue estando bien y una cita realmente vieja se ve exactamente igual—, y por eso ésos se cuentan sin poner nada en rojo. | El Desarrollador decida si una cita lleva además una palabra ancla —algo como `` `js/apiClient.js:431 cambiarEstadoAspirante` ``— que el chequeo pueda exigir adentro del renglón citado. **La deriva ya no la necesita**: `scripts/verificar_deriva.mjs` la agarra sola y sin adivinar nada. La necesitan los dos casos que quedan, la cita nacida mal y el renglón editado en el lugar, que ninguna herramienta puede distinguir de una cita sana mientras la frase no diga qué fue a buscar. Mientras no se decida, el formato no se toca. |
| 91 | **Perder la mitad del corpus sigue sin avisar.** La guarda del 28 de agosto de 2026 (pendiente 69) frena al chequeo que se queda con **cero** archivos, y eso ya está hecho y probado. Lo que no frena es quedarse con **algunos**: casi todos piden las pantallas y los `.js` juntos, así que perder unos les deja los otros y el chequeo sigue en verde con un número más chico, que es exactamente el problema que el 69 vino a atacar. **La mitad de las pantallas quedó tapada el 31 de agosto de 2026**, cuando la extensión pasó a salir de `EXTENSIONES_DE_PANTALLA`; la de los `.js` no. **Y el experimento ya no se hace a mano**: lo corre `scripts/probar_perdida_de_corpus.mjs`, que copia el proyecto, le renombra los `.js` a `.ts`, corre la red de los dos lados y separa a los que se plantaron de los que dijeron ✔ con otro número. El 31 de agosto de 2026 daba **diez ciegos**, y el reparto de las otras dos columnas lo imprime el guión. Ese mismo día se le puso un control: la red corre en la copia **antes** de renombrar nada, y al que ya sale rojo ahí se lo informa como no medible en vez de acreditarlo entre los que se plantaron —eran tres, y ninguno de esos rojos hablaba del corpus: `deriva` y `sinconexion` le preguntan al historial de `git`, que la copia no tiene, y `referencias` sigue citas que salen del proyecto—. **Los nombres tampoco se escriben acá, y por lo mismo:** el 31 de agosto de 2026 pasaron de nueve a diez el mismo día, cuando nació `sensibles`, y la lista escrita quedó vieja en el acto —cada chequeo nuevo que mire los `.js` entra ací solo—. Los imprime el guion, con su par de números al lado. Todos revisaron menos archivos y lo contaron como éxito; `escapado` es el más claro, porque pierde dos tercios del corpus y lo suyo —que los datos y los errores no salgan crudos a la pantalla— vive justamente en el JavaScript. **Los pares de números no se escriben acá**: los imprime el guion, y esta lista ya se pudrió una vez. **Eran nueve y no ocho** cuando se contó la primera vez: la medición hecha a mano ese día se había salteado `estilos`, cuyo mensaje lleva cuatro números de los que sólo dos se mueven, así que a ojo se lee igual. Es exactamente el motivo por el que esto pasó a ser un guion. **La salida obvia está trabada:** exigir que cada extensión nombrada encuentre al menos un archivo rompería hoy mismo, porque `scripts/verificar_identidad.mjs:39` nombra `.webmanifest` y `.txt` y el proyecto no tiene ni un archivo de ninguna de las dos. **Las tres salidas posibles:** que cada chequeo declare qué extensiones le son obligatorias y cuáles opcionales; que se saquen del recorrido las dos que nunca aparecen; o que la guarda cuente por extensión con una lista de exentos y su motivo al lado, como ya hace `verificar_red.mjs` con los chequeos. **No se elige ninguna sin el Desarrollador**, porque una política de exenciones es una decisión, y la de al lado —cuál se exime y por qué— es la que decide si la red sirve o se apaga sola | Se elija una de las tres, quede escrita, y `node scripts/probar_perdida_de_corpus.mjs` dé verde —o sea que ningún chequeo diga ✔ con menos archivos—. La prueba tiene que poder fallar, y contra la red de hoy falla: diez siguen dando ✔ con menos archivos. Se comprobó además que sabe dar verde, dejándole ver los `.ts` a uno de los nueve: pasó de la columna de los ciegos a la de los que no perdieron nada. **La misma prueba con las pantallas renombradas ya no sirve** —desde el 31 de agosto los chequeos siguen la extensión y la encuentran igual—, así que la de los `.js` es la que queda |
| 92 | **Media docena de campos del formulario están atados a los usos de Argentina, y hoy se muestran como si valieran en todas partes.** Lo planteó el Desarrollador el 28 de agosto de 2026, mirando el formulario de reclutamiento: «ya son varias cosas que estarían atadas a los usos y costumbres de cada país, convendría, en una segunda etapa de desarrollo, investigar otros países y adaptar las interfases a cada una». **Y fijó el alcance en la misma frase: «esto está completamente fuera de la versión 1».** Los que se conocen hasta ahora son cuatro. **(a) El identificador fiscal de la persona**: `CUIT / CUIL` es argentino, y cada país tiene el suyo con otro nombre, otra longitud y otra forma de validarse. **(b) La condición fiscal**: monotributo, monotributo social y responsable inscripto son categorías legales argentinas que no tienen equivalente afuera —es lo que ya dice el pendiente 84, y su mitad abierta es este mismo problema—. **(c) El identificador bancario**: `CBU / Alias` es argentino; afuera son IBAN, CLABE, PIX y varios más. **(d) Las zonas de cobertura**, que hoy nombran al Área Metropolitana de Buenos Aires y a nada más. **Lo que no hay que perder de vista:** no es traducir, es reemplazar. Un rótulo traducido sobre un dato que en ese país no existe queda peor que en el idioma original, porque **parece** correcto. Por eso el criterio que ya se aplicó al texto visible —sacar el nombre del organismo del rótulo, dejar la función— es la mitad barata; la cara es que la lista de opciones y la validación salgan de la jurisdicción. | Se investigue, para cada país donde se vaya a vender, cómo se llama y cómo se valida cada uno de esos cuatro datos, y el formulario los tome de la jurisdicción en vez de tenerlos escritos. **No se empieza antes de que la versión 1 esté cerrada**, por decisión del Desarrollador |
| 93 | **Una Prestadora no tiene dónde escribir su propia bajada, así que el producto se la escribió.** Lo señaló el Desarrollador el 28 de agosto de 2026: «no se suma a Careonys, se suma a la prestadora… ojo con mezclar el software con quien lo utiliza». Se corrigió ese mismo día lo que era un error de marcador —dos textos de `registrar-asistente.html` y cuatro frases del catálogo decían `{{producto}}` donde correspondía `{{organizacion}}`— y también lo que era texto escrito a mano **sobre** la Prestadora: el pie de `registrar-asistente.html` y el encabezado de `mockup-app.html` y de `pwa-familia/index.html` agregaban «— Servicios de Cuidado» al lado del nombre, que en PresDemo se leía «PresDemo — Servicios de Cuidado — Servicios de Cuidado». **Lo que queda abierto es el hueco que eso destapó**: la tabla `tenants` tiene `name` y `description`, y `description` hoy guarda una nota interna —«Prestadora de ejemplo, con datos inventados»—, no una bajada para mostrar. Con lo cual todavía quedan dos frases del pie hablando en nombre de la Prestadora sin que ella las haya escrito: «Conectamos familias con Asistentes calificados» en `registrar-asistente.html`, y la misma clase de texto en `formulario-integral.html`, que es una pantalla de referencia. **Inventar una columna nueva es inventar una palabra de negocio**, y eso se consulta antes | El Desarrollador diga si la Prestadora tiene una bajada propia —y cómo se llama—, o si el pie no lleva ninguna. Con eso decidido, sale de la base o se borra de las pantallas |
| 95 | **La Prestadora no tiene dónde configurar nada: todo lo suyo se carga por migración, o sea que lo carga el que programa.** Apareció el 29 de agosto de 2026, cuando el Desarrollador fijó que **las Prestadoras ficticias se tratan como clientes reales** —«creo que debemos tratar a los ficticios que hemos creado como reales, para eso está, para ver cómo funciona el sistema, o cómo falla»—. Con ese criterio se miró qué puede hacer PresDemo por sí misma, y la respuesta es nada de su configuración. `panel-prestadora.html` tiene dos cosas —los aspirantes y la auditoría de un legajo—, las dos sobre personas; **ninguna sobre la Prestadora**. Y las cinco tablas que definen cómo trabaja cada una entran por migración: `zonas_cobertura` (0035), `ponderacion_comprobacion` y `puntaje_prestadora` (0018, la primera renombrada por la 0022) y `cursos` y `evaluaciones` (0008). Ninguna se escribe desde ninguna pantalla —comprobado buscando escrituras en los tres `apiClient.js`: sólo se leen—. **Por qué importa más de lo que parece:** una función que sólo se ejerce cargando la fila a mano no está construida, está simulada, y su falla no aparece nunca porque nadie la usa. El caso de las zonas lo muestra entero: la pantalla del Asistente, el guardado, el directorio y el perfil se probaron los cuatro, y el único tramo que nadie ejerció es justamente el que arma la lista. **Además hay una decisión de reparto adentro**, que no se toca sin consultar: la de la empresa dice que el producto no restringe por razones comerciales, así que esto es configuración del producto y no de CeltaTech, pero **cuánto puede configurar cada Prestadora sí puede ser comercial** —y eso se le anota a CeltaTech y no se construye acá | PresDemo pueda armar sus zonas, ponderar su puntaje y cargar sus cursos entrando al panel, sin que nadie escriba una migración; y las tres Prestadoras ficticias queden cargadas **por esa pantalla** y no por el seed. Se puede cerrar por partes, y la primera es la de zonas, que es la que ya tiene la tabla, la RLS y el módulo hechos |
| 108 | **La opción A del sello no tiene todavía dónde avisarse, porque no hay pantalla donde cambiar los papeles.** Desde la migración 0047, cambiar `documents` en un legajo sellado devuelve el sello a `en_revision` y saca a la persona del directorio hasta que la Prestadora vuelva a mirarlo. Eso es lo que se eligió y está bien; lo que falta es que la persona lo sepa **antes** de tocar el papel, o se siente un castigo sorpresa. Lo pide el plan que lo decidió: `docs/PLAN_SEGURIDAD_DE_COLUMNAS.md:114`. **Hoy la frase no se puede escribir:** ninguna pantalla cambia `documents` —el mapeo existe en `js/apiClient.js:1234` y no lo usa nadie—, y `scripts/verificar_frases.mjs` se pone en rojo con toda frase de catálogo que ninguna pantalla nombre, por el motivo escrito ahí mismo en su punto 3. Así que la frase entra el día que entre la pantalla, no antes. **Se cruza con los pendientes 45 y 47**, que son los que faltan para que un Asistente tenga con qué entrar | Exista la pantalla donde la persona cambia sus papeles, y antes de guardar el cambio le diga, con texto del catálogo en los tres idiomas, que hacerlo devuelve su legajo a revisión y lo saca del directorio mientras tanto. Probado con una cuenta ficticia ya sellada: que el aviso salga, y que después de aceptar, el legajo quede en `en_revision` |
| 110 | **Siete columnas de la siembra no se llenan ni una sola vez, así que nada las prueba.** Lo mide `scripts/probar_coherencia_de_la_siembra.mjs` sobre las 225 columnas de las 25 tablas con datos; el 31 de agosto de 2026 encontró catorce, la migración 0048 cerró cinco y dos quedaron exentas con su motivo, todo el mismo día. No se ve en ninguna pantalla, porque la pantalla se dibuja igual: **la consulta que la olvida y la consulta que la trae contestan lo mismo.** Es el argumento con el que la migración 0027 cargó comprobaciones en los legajos, aplicado a la siembra entera. Son cuatro grupos distintos y no se arreglan igual. **(a) De los catálogos de dos escalones queda uno, y no es un hueco de siembra sino una pregunta.** `cursos`, `evaluaciones`, `preguntas_evaluacion` y `opciones_pregunta` guardaban sólo la oferta general —`tenant_id` nulo— y ninguna fila propia de una Prestadora, así que la mitad `tenant_id = prestadora_actual()` de esas políticas de RLS no tenía nada que la ejercitara. **Lo cerró la 0048**, que hace que PresDemo arme su curso de RCP avanzada con su evaluación y sus dos preguntas, y de paso llenó `evaluaciones.curso_id`; `scripts/probar_aislamiento.mjs` lo mira ahora en tres comprobaciones nuevas. Sigue vacía `vocabularios.tenant_id`, y ahí **no falta sembrar, falta decidir**: los ítems propios de una Prestadora ya existen —cinco en `vocabulario_items`, entre ellos la certificación `rcp_avanzada` de PresDemo—, pero una **lista** entera propia de una Prestadora es otra función, y hay que resolver antes qué pasa cuando una Prestadora quiere su propia lista de patologías junto a la general: ¿la reemplaza, la extiende, conviven las dos? Sembrarla sin contestar eso es elegir la respuesta a escondidas. **(b) Dos columnas que apuntan a una cuenta, y ahí la siembra no llega: exentas.** `caregivers.user_id` y `avisos.familia_id` apuntan a `auth.users`, y la 0030 ya había escrito por qué no las llena: dar de alta una cuenta desde una migración significa **escribir una clave adentro del repositorio**. Eximirlas no tapa nada, y ése fue el requisito para hacerlo: `probar_aislamiento.mjs` recorre los dos caminos con cuentas de verdad —carga legajos a nombre de quien inició sesión, publica avisos a nombre de cada Familia, y comprueba de quién quedó cada uno—. La siembra era el lugar equivocado para probarlas, no un lugar donde faltaran. **(c) Dos columnas de la comprobación de papeles, y a una no la escribe absolutamente nadie.** `verificaciones_asistente.verificado_por` es quién comprobó el papel. Ninguna migración puede escribirla —apunta a `profiles`, que los crea el disparador de alta (`supabase/migrations/0005_acceso_por_sesion.sql:80`) y existen sólo para quien se registró—, **y aun así no se exime**, que es la diferencia con el grupo (b) y conviene no perderla de vista: aquéllas las escribe algo, ésta no la escribe nadie. Ninguna pantalla, ningún guion y ninguna prueba tocan `verificaciones_asistente` fuera de las migraciones —comprobado el 31 de agosto de 2026 recorriendo el proyecto entero—, así que **el producto no anota quién comprobó un papel**, y eximirla sería esconderlo. La 0027 escribió `verificado_el` a propósito y dejó ésta afuera sin decirlo. `plazo_vence_el` es el plazo del que se ocupa el pendiente 98: hoy su camino no tiene ni una fila con fecha, y llenarlo antes de que ese pendiente decida cómo se calcula sería elegir la respuesta a escondidas. **(d) Cuatro columnas de `caregivers` que parecen la forma vieja, todavía viva.** `bank_info`, `education_info` y `reference_info` conviven con las tablas dedicadas que las reemplazaron —`referencias_asistente`, `estudios_asistente`— y `js/apiClient.js` las sigue leyendo y escribiendo (renglones 859-860 al leer y 932-934 al escribir). **Y las tres no están igual de vivas**, que es lo que hace falta saber para contestar: `cbu` y `educacion` se los manda `registrar-asistente.html:1047-1054`, pero `referencia` no se lo manda **nadie**, así que `js/apiClient.js:1238` es la misma forma de traducción muerta que sacó la migración 0050 con `zona`, esperando un dato que ya no manda ninguna pantalla; `pwa-asistente/index.html:648` ya llama «el formato viejo» a una de ellas. `documents` la miran la 0007, la 0026 y la 0047. **No es un arreglo, es una pregunta**: cuál de las dos formas queda | Cada una de las siete esté sembrada, o esté en `LA_SIEMBRA_NO_PUEDE` con el motivo escrito de por qué una migración no puede llenarla **y con algo que sí la recorra** —que es lo que ya pasa con ocho: los dos `archivo_url`, que viven en el depósito; `tenants.estado_fijado_en`, que se escribe cuando el estado cambia; `experiencia_laboral_asistente.puesto_otro`; `tenants.referencia_celtatech`, que la 0025 dice que las ficticias nunca van a tener; `avisos.grid_schedule_7x3`, que la 0016 reemplazó por filas; y `caregivers.user_id` con `avisos.familia_id`, que apuntan a una cuenta y las recorre `probar_aislamiento.mjs`—. **La segunda mitad de esa condición no es adorno**: sin ella, eximir es dejar de mirar. Lo que queda: el (a) y el (d) los decide el Desarrollador antes de que se pueda sembrar nada; `plazo_vence_el` sale del pendiente 98; y `verificado_por` no se cierra sembrando, porque lo que falta es que alguien la escriba |
| 111 | **Cuatro tablas enteras sin una sola fila, y no son todas el mismo hueco. Hoy quedan tres.** Lo mide la cuarta comprobación de `scripts/probar_coherencia_de_la_siembra.mjs` sobre las 29 tablas del esquema, el 31 de agosto de 2026. Hizo falta pedir el volcado del esquema aparte, porque **desde el volcado de datos una tabla vacía es invisible**: ese volcado sólo nombra las tablas que tienen filas, así que la tabla entera sin sembrar no aparece en ningún renglón y nadie la extraña. Es el mismo argumento del pendiente 110 un escalón más arriba. Son tres casos distintos. **(a) `documentos_asistente` no la escribe absolutamente nadie, y el producto ya guarda lo mismo en otro lado.** Recorriendo el proyecto entero ese día no aparece ni una pantalla, ni un guion, ni una prueba que la toque fuera de las migraciones. Lo que sí pasa es otra cosa: `registrar-asistente.html:996-1000` sube el documento de identidad, los antecedentes penales y el título al depósito `documentos-cuidadores`, y los caminos terminan en la columna `documents` de `caregivers` —`registrar-asistente.html:1057`, y de ahí a la base por `js/apiClient.js:1234`—. O sea que **hay dos formas de guardar el mismo hecho y una está muerta**, como ya pasó con `messages` y las `conversaciones` heredadas (pendientes 6 y 46). Y no es prolijidad: la tabla dedicada tiene `tipo`, `presentado_el`, `vencimiento` y `verificado`, y el objeto de `documents` no tiene ninguno de los cuatro, así que hoy **el producto sube el papel y no anota qué papel es, cuándo se presentó ni cuándo vence**. `docs/PLAN_VENCIMIENTOS.md:40` apoya el pendiente 98 justamente en `documentos_asistente.vencimiento`, que es una columna de una tabla que no escribe nadie. Ese plan decía al empezar que «el dato está»; medido contra la base el mismo día, de las tres fechas en las que se apoya hay una sola cargada —la de la Matrícula—, y su sección 2.1 quedó corregida diciendo cuál es cuál. Importa porque un control que mire las tres por igual encuentra cero vencimientos en dos de ellas para siempre, y contesta que no hay nada que avisar sin poder equivocarse. **(b) `clock_ins` no la escribía nadie porque la única pantalla que lo intentaba lo hacía mal** —fue el pendiente 112, cerrado el 31 de agosto de 2026—, y eso salió de tirar de este mismo hilo. Su política de RLS —`supabase/migrations/0002_aislamiento_por_prestadora.sql:105`— no la había recorrido nunca nadie; el mismo día la prueba de aislamiento creció tres comprobaciones que sí la recorren, con cuentas de verdad. **Cerrado ese mismo día por la migración 0049**, que siembra nueve fichadas en las dos Prestadoras que tienen gente cargada: cuatro turnos cerrados y uno abierto, cada uno en una zona que esa Asistente cubre de verdad según `zonas_asistente`, porque una entrada marcada donde esa persona no trabaja es un dato falso aunque la persona sea inventada. El turno abierto está a propósito: una entrada sin su salida es el estado normal de quien todavía está trabajando, y es el caso que hace fallar a la pantalla que sume horas restando una de otra — con sólo pares perfectos, esa pantalla se puede escribir mal y salir bien. **Y sembrarla hizo falta aunque el botón ya escribiera**, que es lo que costó ver: apretarlo dejó dos filas de verdad, y con ellas la comprobación pasó a contestar que la tabla tenía filas. Pero **ninguna migración las repone**, así que la primera base armada desde cero volvía a tenerla vacía: verde hoy y rojo mañana sin que nadie tocara nada, y un verde intermitente se lee como «anda» las veces que anda. Por eso `scripts/soltar_asistente_local.mjs` borra lo que se escribió apretando el botón y deja lo sembrado, y los distingue sin listas escritas a mano: una fila cuyo identificador nombra alguna migración es de la siembra, una que no lo nombra ninguna la escribió una persona. **(c) `messages` y `reportes` sí se recorren, pero no queda nada.** Las escribe `probar_aislamiento.mjs` con cuentas de verdad y las borra al limpiar, que es una prueba más fuerte que una fila sembrada —pasa por las políticas—. Lo que queda sin una sola fila es lo que ven las pantallas que las leen, y ahí el pendiente 52 ya dice que las dos cuelgan de donde no corresponde | Cada una de las que queden esté sembrada, o esté en `LA_SIEMBRA_NO_PUEDE_TABLA` con el motivo de por qué una migración no puede llenarla **y con algo que sí la recorra**, que son las dos mitades del mismo criterio del pendiente 110. Hoy no hay ninguna adentro. **La (a) no se cierra sembrando**: primero el Desarrollador elige cuál de las dos formas de guardar los papeles queda —es la misma pregunta que el grupo (d) del 110—, y hasta que eso se conteste sembrar la tabla muerta sería darle la razón a escondidas. **La (b) está cerrada** por la migración 0049. La (c) depende del pendiente 52 |
| 120 | **`docs/DISENO.md` se cuenta mal a sí mismo, dos veces.** Salió al vaciar `docs/Nueva carpeta/` el 31 de agosto de 2026, y las dos siguen vigentes ese mismo día, comprobadas contra el archivo de hoy. **Una:** la conclusión dice que **6** apuntan a huecos (`docs/DISENO.md:5`) y la sección que los enumera dice **Nueve** (`docs/DISENO.md:82`), listando cuatro medidas de redondeo, una duración de animación, un tercer nivel de gris, el anillo del foco de teclado y las dos piezas de `--texto-sobre-color`. La cuenta que cierra el documento es 20+6+6=32; con nueve da 35. **Dos:** los grises se colapsan y después se reclaman. La tabla de equivalencias manda `--text-light` a `--texto-secundario` con la nota de que Careonys tiene dos niveles de gris y no cuatro (`docs/DISENO.md:32`), y la lista de huecos vuelve a pedir un tercer nivel de gris para texto (`docs/DISENO.md:83`). O se colapsó o falta, y las dos cosas a la vez no. **Importa por quién lo va a leer**: la regla de este producto dice que la tabla de equivalencias con el sistema de diseño de Careonys se hace una vez y **antes** de portar la primera pantalla, y un documento que no se pone de acuerdo consigo mismo no sirve de tabla de equivalencias | Las dos cuentas den lo mismo en las dos puntas del documento, y el tercer nivel de gris esté colapsado o esté pedido, no las dos cosas |
| 121 | **Los documentos heredados de negocio arrastran tres errores de cuenta y de rótulo.** Salieron al vaciar `docs/Nueva carpeta/` el 31 de agosto de 2026 y se comprobaron ese mismo día contra el archivo de hoy. **(a) El margen está redondeado hacia abajo y escrito como si fuera exacto**: $1.000 sobre $3.500 es 28,57% y está escrito 28,5% en dos lugares (`docs/modelo_de_negocios_prestadora.md:41` y `:71`). El EBITDA de 12,5% sí cierra. **(b) El cuadro de resultados promete cuotas y no tiene ninguna**: se titula «Ingresos Totales por Servicios y Cuotas» (`docs/modelo_de_negocios_prestadora.md:68`) y sus $28.000.000 son exactamente 8.000 horas por $3.500; las otras cuatro fuentes de ingreso que el mismo documento declara aportan cero. Así que **subestima su propio negocio con un rótulo que promete lo contrario**. **(c) Las advertencias de los dos modelos de negocio son idénticas renglón por renglón** (`docs/modelo_de_negocios_careonys.md:3` a `:11`, comprobado con `diff` el 31 de agosto de 2026): la misma advertencia describe dos documentos distintos. **Los dos puntos que tenía esta fila sobre los documentos de términos se cerraron el 31 de agosto de 2026**, con la reescritura completa de los dos: las fechas que se contradecían y el «persona asistida» sin corregir ya no existen. **Ninguno de los tres que quedan se construye**: son documentos, y arreglarlos es corregir prosa | Cada uno de los tres esté corregido en el archivo que lo tiene, o el Desarrollador diga que ese documento se descarta entero. **Ninguna cifra se toca sin su palabra**: cambiar 28,5% por 28,57% es aritmética, pero cambiar el rótulo del cuadro de resultados es decidir qué vende la Prestadora |
| 122 | **Dos cobros distintos se llaman igual, y un producto lo venden dos.** Salió al vaciar `docs/Nueva carpeta/` el 31 de agosto de 2026. **La homonimia:** hay dos porcentajes del 10% al 15% que no son la misma cosa —uno lo cobra CeltaTech sobre las horas transaccionadas y el otro lo cobra la Prestadora a la familia por tramitarle los reintegros—, y ningún glosario los distingue, así que «la comisión del quince» ya significa dos cosas. El análisis que salió de ahí proponía llamarlas «comisión de plataforma» y «honorario de gestoría»; son dos palabras de negocio nuevas, así que no se usan hasta que se aprueben. **El producto con dos dueños:** el «Gestor del Cuidado» lo vende CeltaTech como membresía de teleasistencia (`docs/modelo_de_negocios_careonys.md:62`) y lo vende la Prestadora como abono mensual con informes diarios (`docs/modelo_de_negocios_prestadora.md:46`), sin ningún reparto escrito en ninguna parte; y **ya es texto de producto**, en `data/catalogo-frases.json:2477`. **Y un hueco legal concreto:** los términos de los Asistentes no advierten que entrar a la red pueda costar dinero —el único arancel que nombran son los cursos opcionales—, así que si la Prestadora cobrara ese arancel lo estaría cobrando fuera de lo que la persona aceptó. **Esto es comercial y no se construye acá**, por `docs/ALCANCE.md` §4: queda anotado y espera | El Desarrollador decida los dos repartos y apruebe o rechace los dos nombres. El hueco de los términos va con el pendiente 49, que es el que espera al abogado |
| 123 | **La base de esta máquina no se comporta como dice su propia configuración, y ninguna prueba lo notaba.** `supabase/config.toml:253` pide `enable_confirmations = true` —que es lo que hace el servidor publicado— y sin embargo el servidor de cuentas de esta máquina contesta `mailer_autoconfirm: true` en `/auth/v1/settings`: las cuentas nacen confirmadas. La causa quedó medida el 31 de agosto de 2026: el contenedor de cuentas **se creó el 2026-08-24** y el archivo de configuración cambió el 2026-08-25, y las variables de un contenedor quedan fijadas cuando se lo crea, no cuando se lo enciende, así que bajar y volver a levantar la base no lo mueve —se probó—. Lo mismo pasa en los otros dos proyectos que esta máquina tiene levantados. **Por qué importa:** toda prueba que crea una cuenta y entra con ella pasa hoy porque el servidor local la confirma solo. Contra el servidor publicado, que no confirma, esa misma cuenta no podría entrar sin abrir el correo. O sea que **verde acá no dice que el mismo camino ande allá**, y esa diferencia no está escrita en ninguna parte. Arreglarlo es recrear el contenedor de cuentas —borrarlo y volver a levantar la base, que no toca ningún dato porque las cuentas viven en la base y no en el contenedor—, y **se hace mirando qué se pone en rojo**: es de esperar que varias pruebas tengan que confirmar la cuenta a propósito, con la llave de servicio, en vez de que el servidor lo haga por ellas. | El servidor de esta máquina contesta `mailer_autoconfirm: false`, y `node scripts/probar_todo.mjs` vuelve a dar lo mismo que da hoy. |
| 124 | **Los términos no le llegan a quien tiene que firmarlos.** Cinco hallazgos de la misma familia, medidos el 31 de agosto de 2026 recorriendo las 17 pantallas. **(a)** `registrar-asistente.html:624` enlaza en el pie los términos de **Familias**, en una pantalla de alta de Asistentes, y se contradice con `:539`, que enlaza bien los de Asistentes. **(b)** `formulario-integral.html` tiene **una sola** casilla de términos, y cuelga del formulario equivocado: está en `formulario-integral.html:415-421`, adentro de `form-filtro-directorio` (`:388`), que es el buscador —y se llama `acepto-terminos-wizard`, o sea que se escribió para el asistente por pasos y terminó cosida al filtro—. Los tres formularios de alta de esa pantalla —el asistente por pasos de `:96`, la solicitud de Familia de `:293` y el alta de Asistente de `:339`— no tienen ninguna, y el pie (`:465`) ofrece los de Familias a las tres. **(c)** `pwa-asistente/index.html:668-671` obliga a aceptar «los Términos y Condiciones de Legajo para Asistentes» **sin ningún enlace que los muestre**, mientras su hermana web sí los enlaza; y `pwa-familia/index.html` no nombra los términos en ninguna pantalla, ni en la de publicar. **(d)** El «Términos» del pie tiene cinco destinos distintos entre ocho pantallas: a los de Familias en `index.html:517`, `registrar-asistente.html:624` y `formulario-integral.html:465`; a `#` —o sea a ningún lado— en `directorio.html:201`, `perfil.html:237` y `solicitar-asistente.html:407`; y no existe en `cursos.html` ni en `soporte-remoto.html`. Un rótulo que en tres pantallas no lleva a ninguna parte se ve igual que uno que sí. **(e) Cumplido el 31 de agosto de 2026.** «Privacidad» apuntaba a `#` en las tres pantallas y no había ningún documento de privacidad en `docs/`: prometía uno que nadie había escrito. Ahora existe `docs/politica_de_datos.md`, las tres lo enlazan, y `.vercelignore` lo deja salir al sitio como a los dos de términos. **Los cuatro puntos anteriores siguen abiertos.** Lo de `formulario-integral.html` hay que leerlo junto con el 64, porque esa pantalla está sentenciada y puede que el arreglo se lo lleve el borrado. | Cada formulario de alta tiene su casilla con el documento de su público; los dos programas de teléfono muestran el enlace que hacen aceptar; el «Términos» del pie lleva al mismo lugar en todas las pantallas que lo tengan; y «Privacidad» o enlaza un documento que existe o se saca. |
| 125 | **Cinco destinos de navegación que no llegan a donde dicen.** Medidos el 31 de agosto de 2026 sobre los 359 destinos de las 17 pantallas. **(a)** Ocho enlaces «Registrarme» apuntan al ancla `#registro` y **esa ancla no existe**: `registrar-asistente.html` no tiene ningún `id="registro"`. Son `index.html:511`, `directorio.html:195`, `perfil.html:231`, `solicitar-asistente.html:401`, `formulario-integral.html:461` y `registrar-asistente.html:619`, más los dos botones principales de la propia pantalla de alta —`:58` y `:186`—, que son justamente los que tendrían que bajar al formulario y no bajan. **(b)** `perfil.html:189` manda «Publicar un Aviso» a `solicitar-asistente.html`, mientras la misma frase de catálogo —`nav.publicar_aviso`— va a `formulario-integral.html` en las otras catorce apariciones, **incluida la barra de la propia `perfil.html`, renglón 37**. **(c)** `mockup-app.html` no la enlaza ninguna de las 17 pantallas, y `docs/INVENTARIO.md:73` afirma que se llega desde el botón «Contactar» del directorio, cuando `directorio.html:351` manda ese botón a `perfil.html?id=…#contactar`. Es una tercera aplicación de Familia, huérfana, conviviendo con `pwa-familia/`. **(d)** El sobre de contacto del **pie** falta en `cursos.html` y `soporte-remoto.html`, que sí lo tienen en la barra de arriba; las otras seis lo tienen en los dos lugares. **(e)** El «Contacto» del pie sólo tiene destino real en `index.html:516` —`#contact`, que existe en `index.html:254`—; en las otras siete va a `#`. | Ningún enlace apunta a un ancla que no existe; «Publicar un Aviso» lleva al mismo lugar desde todas partes; y de `mockup-app.html` se decidió si se enlaza o se saca, con `docs/INVENTARIO.md` diciendo lo que de verdad pasa. |
| ~~126~~ | **Cerrado el 1 de septiembre de 2026: la herramienta de seguridad que no corría nadie.** `scripts/barrer_aislamiento.mjs` saca la lista de tablas de las migraciones y le pregunta al servidor publicado, sin sesión y con la clave publicable que cualquiera lee del navegador, si alguna devuelve filas. Existía desde antes y no la llamaba ningún camino: `scripts/verificar_todo.mjs:36` enumera la carpeta y se queda con lo que empieza por `verificar_`, y `scripts/probar_todo.mjs` tenía la lista escrita a mano. **Y la primera corrida encontró algo**: la vista `oferta_de_cursos` devuelve seis filas a quien no inició sesión. No era un escape —la abrió el Desarrollador el 31 de agosto de 2026, `supabase/migrations/0051_la_oferta_de_cursos_se_ve_sin_sesion.sql:195`, y la vista filtra adentro `tenant_id is null and publicado`, así que no muestra fila de ninguna Prestadora—, pero nadie lo había escrito en la lista de excepciones que el propio barrido exige. | **Hecho.** El barrido entró en `scripts/probar_todo.mjs`, que ahora **enumera la carpeta y falla si alguna prueba no está ni en `PRUEBAS` ni en `AFUERA_A_PROPOSITO` con su motivo escrito** —probado poniendo un guion de mentira, que la puso en rojo—. La regla mira `probar_` y `barrer_`, porque el guion que faltaba empezaba por el prefijo que nadie miraba. Las dos que quedan afuera dejaron de ser dos `console.log` al final y pasaron a ser datos, que es lo que se puede comparar contra la carpeta. Y `oferta_de_cursos` quedó declarada en `ABIERTAS_A_PROPOSITO` con su motivo, que **el propio barrido revisa en cada corrida**: si esa vista dejara de devolver filas sin sesión, el renglón estaría perdonando algo que ya no existe y el barrido se pone en rojo —probado con un nombre de mentira—. Lo hace él porque `scripts/probar_exenciones.mjs` sólo mira los `verificar_`. |
| 142 | **Dos imágenes que no las nombra nadie.** La parte peor de este pendiente —que era el pendiente 126, hoy cerrado— ya no está: era `scripts/barrer_aislamiento.mjs`, la herramienta que pregunta por **todas** las tablas sacadas de las migraciones —incluidas las que se creen mañana— y a la que no llegaba ningún camino. **Se cerró el 1 de septiembre de 2026** y se cuenta acá abajo. Queda lo de las imágenes. **(a)** `assets/images/Nueva carpeta/` tiene 11 archivos que no nombra ni una pantalla, ni el guardado sin conexión de las dos aplicaciones de teléfono, ni ningún documento. Dos comparten nombre con una imagen viva pero **no son la misma**: pesan más y son los originales. **Y esa carpeta no la guarda el repositorio** —`.gitignore:32`—, así que borrarla no es archivarla: es destruirla, y por eso no se toca sin su palabra. **(b)** `assets/images/perfil_claudia.png` no la nombra nadie, y ésta sí la guarda el repositorio, así que borrarla se deshace. | De las imágenes huérfanas se dijo si se guardan o se van. |
| 127 | **Tres documentos del esquema describen una base que ya no es ésta.** **(a)** `docs/ESQUEMA.md:4` abre diciendo *«hoy hay veinte aplicadas y veinticuatro tablas»*: hoy hay **50 migraciones** y unas **28 tablas vivas** —el barrido da 32 nombres, y cuatro son el nombre anterior de otra: `care_searches`, `logbook_entries`, `banderas_asistente` y `ponderacion_comprobacion`—. El documento no lleva la salvedad de ser una foto fechada, que es lo que salva a `docs/INVENTARIO.md`. **(b)** La regla 2 de ese mismo documento, `docs/ESQUEMA.md:92-93`, manda poner la columna de la Organización *«Incluye las que hoy no la tienen, como los reportes»*, y **los reportes ya la tienen** desde `supabase/migrations/0002_aislamiento_por_prestadora.sql:53`, con su política en `:110`. Está escrita en presente y hoy es falsa. **(c)** `docs/TABLAS_QUE_FALTAN.md:241` abre la ficha *«10. `reportes` — alguien denuncia algo»* entre las tablas que no existen, y **`reportes` existe**: es el cuaderno de reportes de cuidado, que se llama así desde `supabase/migrations/0019_el_cuaderno_se_llama_reporte.sql:34`. Mismo nombre para dos tablas distintas, y quien escriba la ficha 10 tal como está choca contra una tabla ocupada; `docs/ESQUEMA.md:141` ya la llama `moderacion`. De paso, `docs/ESQUEMA.md:141` y `README.md:81` dicen los dos *«las diez tablas que aún no existen»* y **no son las mismas diez**: uno funde el chat en una y suma la configuración, que el otro deja afuera a propósito. | Los tres documentos dicen los números de hoy —o llevan escrito de qué día son—, la regla 2 nombra una tabla que de verdad no tenga la columna, y la ficha 10 no se llama como una tabla que ya existe. |
| 128 | **Cinco cosas escritas que no las llama nadie, y una rama que por eso quedó inalcanzable.** Medido el 31 de agosto de 2026 con `node scripts/listar.mjs --buscar`, que recorre las pantallas, los guiones y los documentos. **(a)** `js/apiClient.js:476` `getAvisosFamilia()` y `js/apiClient.js:530` `getFranjasDeAviso()`: ninguna pantalla las llama, o sea que **ninguna pantalla lista avisos**. Y como la primera es la única que le pasa `avisos` a `_supabaseGet` (`js/apiClient.js:481`), esa rama entera no la alcanza nadie. **(b)** Con esa rama muerta, el único llamador vivo de `_supabaseGet` es `js/apiClient.js:201`, y sus tres llamadores —`panel-prestadora.html:228`, `panel-prestadora.html:320` y `pwa-asistente/index.html:1551`— no le pasan ningún filtro, así que las dos ramas de `js/apiClient.js:1109-1110` no las ejerce nadie. **Y una de las dos está mal escrita para el día que alguien la use**: traduce el estado a `verification_status`, que es columna de `caregivers`, adentro de una función que recibe la tabla por parámetro —en `avisos` la columna se llama `status`—. **(c)** Tres métodos públicos sin un solo llamador: `js/catalogo.js:398` `Catalogo.etiqueta()` —que convive con `etiquetaSiExiste()` en `js/catalogo.js:408`, con decenas de llamadas—, `js/identidad.js:164` `Identidad.organizacion()` y `js/auth.js:223` `Sesion.onAuthStateChange()`, que además lo nombra `docs/INVENTARIO.md:321` en prosa. **Ojo con el precio:** los tres archivos están triplicados byte a byte, así que cada borrado son tres, y `scripts/verificar_copias.mjs` lo exige antes de cada `commit`. | De cada una se decidió si se borra o si le falta la pantalla que la iba a llamar, y lo que se borre sale de las tres copias a la vez. |
| 129 | **Veinte citas se habían corrido de renglón y los dos chequeos que las miran pasaron en verde.** Salió el 31 de agosto de 2026, al medir a mano las 252 citas con forma `archivo:renglón` de este mismo archivo: veinte apuntaban a un comentario, a un renglón en blanco o al cierre de un bloque vecino, casi todas por corrimientos de `js/apiClient.js`. **Ninguno de los dos chequeos las vio, y cada uno por un motivo distinto.** `scripts/verificar_referencias.mjs` mira 509 citas pero sólo pregunta si el renglón existe y no está en blanco: un renglón corrido sigue existiendo, así que pasa. `scripts/verificar_deriva.mjs` sí compara el texto —es el que sirve—, pero llega a 338 de esas 509, y además **saltea todo renglón que todavía no esté en el historial**: `git blame` le pone a lo no confirmado un identificador de puros ceros y el guion lo descarta (`scripts/verificar_deriva.mjs:215`). Como el gancho de `commit` corre justamente sobre lo que aún no está confirmado, **una cita que nace mal nunca se revisa**: se comprobó poniendo a propósito `js/main.js:280` —que hoy dice otra cosa— en un archivo sin confirmar, y el chequeo dio verde igual. **Lo que no se midió** es si la agarraría una vez confirmada; para saberlo hay que confirmar la cita mala a propósito, y eso ensucia el historial, así que se hace con el gancho de por medio y no salteándolo. Y falta explicar el otro hueco: **por qué 171 de las 509 citas no llegan a la comparación de texto**. | Un renglón citado que dejó de decir lo que la cita fue a buscar pone algo en rojo, también la primera vez que se escribe; y la diferencia entre las 509 y las que de verdad se comparan está explicada o cerrada. |
| 116 | **La marca del producto encabeza las dos aplicaciones de la Familia y del Asistente, que es justo donde no va.** La regla compartida es de un renglón: «En toda pantalla, correo o aviso dirigido a una Familia o a un Asistente, la marca principal es la de la Prestadora de esa sesión. El producto aparece sólo en una línea al pie» (`..\..\docs\REGLAS_PRODUCTOS_CAREONYS.md`, punto 4). El pendiente 93 arregló el 28 de agosto de 2026 los casos que se vieron a ojo —dos textos de `registrar-asistente.html` y cuatro frases del catálogo que decían `{{producto}}` donde iba `{{organizacion}}`—, con la frase del Desarrollador al lado: «no se suma a Careonys, se suma a la prestadora… ojo con mezclar el software con quien lo utiliza». **Nadie barrió el resto.** Medido el 31 de agosto de 2026 con `node scripts/listar.mjs .html --buscar "\{\{producto"`: **40 apariciones en 17 pantallas, y 18 de ellas caen fuera del pie, en siete pantallas dirigidas a una Familia o a un Asistente.** El pie está bien —los «Sobre {{producto}}» y las «Con la tecnología de» son exactamente la línea al pie que la regla permite—, y las pantallas del personal de la Prestadora —`panel-prestadora.html`, `guias-prestadora.html`— quedan afuera de la regla, porque a ésas sí las encabeza el producto. Lo que cae fuera del pie no es todo la misma decisión, y por eso va separado. **Uno: el título de la pestaña, que es el caso más claro.** De las 17 pantallas, **sólo dos llevan la marca del producto en el `<title>`, y son exactamente las dos aplicaciones**: `pwa-asistente/index.html:6` («{{producto}} Asistente — Portal Móvil») y `pwa-familia/index.html:6` («{{producto}} Familia — PWA Portal»). Las otras seis que llevan marca llevan `{{organizacion}}` —`index.html:7`, `panel-prestadora.html:9`, `guias-prestadora.html:9`, `registrar-asistente.html:9`, `formulario-integral.html:7` y `mockup-app.html:7`— y nueve no llevan ninguna. **Dos: el botón de entrar**, que es la acción más grande de esas dos pantallas: «Ingresar a {{producto}} Asistente» (`pwa-asistente/index.html:337`) e «Ingresar a {{producto}} Familia» (`pwa-familia/index.html:596`). Justo arriba del botón el encabezado ya está bien —logotipo de la Prestadora y `<h2 class="tenant-name">{{organizacion}}</h2>`, en `pwa-asistente/index.html:324` y `pwa-familia/index.html:586`—, así que la pantalla dice el nombre de la Prestadora y a renglón seguido ofrece entrar a otra marca. **Tres: la misma oración escrita con las dos marcas en la misma pantalla.** `index.html:9` describe el sitio como «{{organizacion}} facilita el acceso a cientos de Asistentes calificados» y `index.html:375` lo titula «{{producto}} facilita el acceso a cientos de Asistentes calificados». Es la misma frase con dos dueños distintos, a 366 renglones de distancia. **Cuatro: las nueve que quedan** —`index.html:187`, `:362` y `:477` (una cita de prensa), `solicitar-asistente.html:57`, `:250` y `:288`, `soporte-remoto.html:9` (la descripción de la pestaña), `registrar-asistente.html:542` y `pwa-asistente/index.html:8`—, donde hay que mirar de a una, porque algunas hablan del producto de verdad: «Descargue {{producto}} en su celular» nombra la aplicación que se baja, y ésa puede que esté bien. **Y cuatro apariciones que no son este pendiente, para no mezclarlas.** «{{producto}} Academy» —`cursos.html:121`, `registrar-asistente.html:349`, `pwa-asistente/index.html:574`— es un nombre comercial y ya espera decisión adentro del pendiente 9. Y «Términos y Condiciones de Legajo para Asistentes de {{producto}}» (`pwa-asistente/index.html:668`) dice de quién es un documento legal, no quién encabeza la pantalla: eso lo decide el pendiente 49. **Por qué se consulta en vez de arreglarse:** cada uno de esos textos es texto visible en tres idiomas, y en varios no hay reemplazo obvio —«Ingresar a Careonys Asistente» no se convierte solo en nada—. La recomendación, para que sea un sí o un no: **el título y el botón de las dos aplicaciones pasan a `{{organizacion}}`**, porque ahí la regla no admite lectura y las otras quince pantallas ya lo hacen así; **el grupo tres se resuelve dejando la oración una sola vez**, con la marca que el Desarrollador elija; y **el grupo cuatro se mira de a uno** | El Desarrollador diga, grupo por grupo, si el texto lleva la marca de la Prestadora o la del producto; los textos queden cambiados en los tres idiomas; y **exista un chequeo que lo sostenga**, porque hoy la regla vive sólo en la prosa de `REGLAS_PRODUCTOS_CAREONYS.md` y nada impide que la próxima pantalla vuelva a escribir `{{producto}}` en su encabezado. Ese chequeo no se puede escribir antes: nacería en rojo con estas dieciocho, y una lista de perdonados de dieciocho renglones es exactamente la enfermedad que la red de chequeos viene persiguiendo |
| 117 | **La aplicación del Asistente pide cuatro documentos y no sube ninguno.** El paso «Documentación requerida» de `pwa-asistente/index.html` tiene cuatro selectores de archivo de verdad —Foto de Perfil, Foto DNI, Antecedentes Penales y Título, en los renglones 508 a 511—, y **ninguno lo lee nadie**. Tres no tienen siquiera identificador, y el cuarto, `w-foto`, aparece **una sola vez en todo el proyecto: en su propio renglón**. `guardarLegajo` (`pwa-asistente/index.html:2105`) junta las cuatro fichas repetibles —matrícula, estudio, experiencia laboral y referencia, en los renglones 1923 a 1926— y sube sus archivos con `FichasLegajo.subirArchivos()` (`pwa-asistente/index.html:2120`), pero esos cuatro no están entre ellas: `data/catalogo-fichas.json` declara exactamente esas cuatro fichas y **ninguna es el DNI, los antecedentes penales ni la foto**. Así que la persona toca cuatro veces, elige cuatro archivos, guarda, ve que se guardó, y su legajo sube sin ninguno de los cuatro. **Es la misma pantalla contada dos veces con distinta suerte**: `registrar-asistente.html`, la versión de escritorio, sí los sube —los lee por identificador en el renglón 1008 (`foto-perfil`, `file-dni`, `file-penales`, `file-titulo`) y llama a `Sesion.uploadFile()` en el 1012—. O sea que existe el código que falta, escrito al lado, en otro archivo. **Lo peor no es que falte, es que no avisa**: un selector de archivo que se llena y no sube se ve exactamente igual que uno que sube. Y lo que se pierde son los dos papeles con los que se decide si esta persona puede entrar a una casa a cuidar a alguien. **No se arregla acá por dos motivos, y el segundo es el que manda.** Uno: copiar el bloque de `registrar-asistente.html` a la aplicación sería la cuarta copia del mismo patrón —ya hay tres de `js/auth.js`, pendiente 13—, así que lo que corresponde es que las dos pantallas llamen al mismo lugar, y eso es un cambio de forma, no un arreglo. Dos: **dónde queda anotado cada papel es justo lo que está sin decidir** —el pendiente 111 dice que `documentos_asistente` no tiene una sola fila y que los papeles terminan adentro de un `jsonb` sin tipo, sin fecha y sin vencimiento, y el 98 espera esa misma decisión para poder avisar antes de que algo venza—. Subir los cuatro al mismo `jsonb` de hoy sería construir sobre lo que ya se sabe que hay que rehacer | Los cuatro documentos lleguen al depósito y queden anotados en el legajo cuando se guarda desde el teléfono, **y las dos pantallas suban por el mismo camino y no por dos**. Probado desde el teléfono con una cuenta ficticia, como se hizo en el pendiente 112, ya cerrado: elegir los cuatro archivos, guardar, y encontrar los cuatro en `documentos-cuidadores` bajo la carpeta de esa cuenta y sus cuatro referencias en el legajo. Antes hace falta que el pendiente 111 decida dónde se anota un papel del legajo |
| 118 | **Qué archivos se aceptan está escrito a mano en cuatro formas distintas, y ninguna coincide con la que declara la migración.** El único lugar donde eso está declarado es `supabase/migrations/0006_archivos_del_legajo.sql:30`, que le da a `documentos-cuidadores` `image/jpeg`, `image/png`, `image/webp`, `image/heic` y `application/pdf`, y a `avatares` los mismos menos el PDF (`:40`). En las pantallas hay otra cosa, y **se despegó en las dos direcciones**: `js/fichas-legajo.js:138` escribe `.jpg,.jpeg,.png,.pdf`, o sea **de menos** —deja afuera `webp` y `heic`, y `heic` es lo que sale de un iPhone sin tocar nada, así que la foto de la credencial no se puede ni elegir—; y `image/*,.pdf` —`registrar-asistente.html:478`, `:488` y `:498`, y `pwa-asistente/index.html:581`, `:582` y `:583`— abre **de más**: deja elegir un `gif`, un `bmp` o un `tiff` que el depósito después rechaza, con el archivo ya subido entero. Los dos de la foto —`registrar-asistente.html:360` y `pwa-asistente/index.html:580`— dicen `image/*` y les pasa lo mismo. Es un catálogo escrito adentro de una pantalla, que es lo que la regla de los catálogos viene a evitar, y ya se despegó de su declaración sin que nada avisara. **Se cruza con algo que tampoco está**: nada mira el tamaño del archivo antes de subirlo —`.size` no aparece en ninguno de los 49 archivos revisados—, así que el límite de 10 MB vive sólo en el depósito y actúa cuando el archivo ya viajó, que es exactamente lo que la regla de la empresa nombra como insuficiente. Lo que sí está bien es el aviso: `Texto.claveDeError()` (`js/texto.js:196`) reconoce las dos negativas del depósito y contesta `error.archivo_pesado` y `error.archivo_tipo`, en los tres idiomas. **El 31 de agosto de 2026 se cerró la mitad grande, que es la del tamaño.** De las dos salidas que este pendiente planteaba se tomó la segunda —declararlo de este lado y que un chequeo comprueba que coincida— y no la primera —leerlo del depósito al arrancar—, por dos motivos: la regla de la empresa ya elige esa forma cuando la plataforma obliga a duplicar («original, copias generadas y comprobación automática»), y leerlo al arrancar agrega un pedido de red a cada carga para averiguar algo que no cambia. Así que `js/auth.js` declara `DEPOSITOS` con el tope y los tipos de cada uno, `_porQueNoSeSube()` decide antes de subir, y la cuarta regla de `scripts/verificar_deposito.mjs` compara esa copia contra `limitesDeclarados()` —la lectura de la migración— **y además ejerce la decisión con nueve casos**, así que no alcanza con que las dos listas digan lo mismo: el rechazo tiene que funcionar. **Lo que queda abierto es el `accept=`**, que son los seis renglones escritos a mano de arriba. No es un agujero —el control real ya está antes de subir— sino una molestia y una mentira: al iPhone se le sigue diciendo que no puede elegir su propia foto, y a quien elige un `gif` se le deja elegirlo para rechazarlo después | El `accept=` de cada selector de archivo salga de `DEPOSITOS` y no de seis renglones escritos a mano —lo natural es un `data-deposito` en el `<input>` y una función que se lo escriba al cargar la pantalla—, y que un chequeo lo sostenga, porque hoy nada impide que la próxima pantalla vuelva a escribir la lista a mano. Toca `registrar-asistente.html:360`, `:478`, `:488` y `:498`, `js/fichas-legajo.js:138` y `pwa-asistente/index.html:580` a `:583` |

### Reparto de las 22 tablas, aplicado el 25 de agosto de 2026

`docs/MODULOS.md` reparte **módulos**, no tablas, y por eso la línea no se podía recorrer con una
búsqueda. Esto la baja a tabla por tabla. **Era una propuesta y el Desarrollador la aprobó
entera**: las tres de esta modalidad ya llevan el prefijo en el repositorio, a la espera de que
las dos migraciones se peguen en el servidor (pendiente 52).

| Lado | Tablas |
|---|---|
| **De esta modalidad** (llevan el prefijo de la modalidad) | `avisos`, `franjas_aviso`, y la vista `directorio` |
| **Compartidas** (se quedan como están) | `tenants`, `profiles`, `caregivers`, `clock_ins`, `reportes`, `matriculas_asistente`, `estudios_asistente`, `experiencia_laboral_asistente`, `referencias_asistente`, `documentos_asistente`, `verificaciones_asistente`, `autorizaciones_asistente`, `cursos`, `evaluaciones`, `preguntas_evaluacion`, `opciones_pregunta`, `intentos_evaluacion`, `disponibilidad_asistente`, `franjas_asistente` |
| **La única dudosa** | `messages` |

**Por qué `messages` queda aparte.** `docs/MODULOS.md:73` pone «Contacto y su costo» de este lado,
y el chat es cómo una Familia llega a un Asistente. Pero en prestación directa la Prestadora
también le escribe a su personal, y esa tabla ya existe desde la 0001 con ese uso. Las dos
lecturas son defendibles y la pregunta que decide —*¿esto seguiría teniendo sentido en prestación
directa?*— acá contesta que sí. Se deja marcada en vez de resolverse sola.

**La palabra ya está: son dos palabras distintas y nombran dos cosas distintas.** El Desarrollador
lo aclaró el 25 de agosto de 2026. El **Aviso** es lo que la Familia publica y queda guardado. La
**Búsqueda** es la acción de buscar, y la hacen los dos lados de tres maneras: la Familia busca
entre los Asistentes disponibles, la Familia busca también publicando un aviso, y el Asistente
busca cuando revisa los avisos publicados hasta encontrar uno que le cuadre. **De ahí sale el
nombre de la tabla sin discusión**: lo que `avisos` guarda son avisos, no búsquedas, así
que le corresponde `avisos`; y `franjas_aviso`, que guarda los días y turnos de ese
aviso, es `franjas_aviso` —igual que su hermana `franjas_asistente`, que guarda los del
Asistente—. Las dos palabras quedaron propuestas en `docs/GLOSARIO.md` §3 y **esperan aprobación**
antes de tocar una línea, que es lo que manda §3. Nada se renombró todavía.

**Dos tablas compartidas apuntan al aviso, y eso pesa más que las palabras.** `reportes` y
`messages` tienen una columna `aviso_id` que referencia `avisos`
(`supabase/migrations/0001_esquema_inicial.sql:242` y `:247`). Los reportes y el chat existen igual
en prestación directa, pero ahí no hay ningún aviso al que apuntar: esa columna quedaría siempre
vacía. Es exactamente la filtración que `docs/MODULOS.md` quiere evitar, y ninguna búsqueda de
texto la ve, porque `aviso_id` no contiene ninguna de las siete palabras. **También decide la
duda sobre `messages`**: si el chat cuelga de un aviso, el chat es de este lado. Queda para la
misma decisión, y son dos preguntas más: si un reporte tiene que poder colgar de un aviso o si le
alcanza con colgar del Vínculo, y lo mismo para el chat.

**El costo de renombrar `avisos` es menor de lo que suena.** En Postgres es un `alter table
… rename to …`: las políticas, los índices, las claves foráneas y la función de la 0002 siguen
apuntando solas al mismo objeto, y no se mueve un solo dato. Lo que sí hay que cambiar a la par
son los 11 archivos que la nombran, porque el nombre de la tabla es además la dirección web por la
que la aplicación la consulta. No es una migración de datos; es un renglón y un reemplazo
parejo.

**Lo que este reparto arregla y la búsqueda de texto no.** Puesto el prefijo, «buscar la palabra
en lo compartido» pasa a ser una orden que se puede correr. Sin él, la prueba al revés de
`docs/MODULOS.md` —«Cómo se comprueba que la línea está bien puesta»— no distingue una filtración de la frase que enuncia la regla: se midió el 25
de agosto de 2026 y de once apariciones de esas palabras en las migraciones compartidas, once eran
comentarios y varias eran el propio enunciado. Está contado en `docs/ALCANCE.md`.

---

## Los eslabones del mercado

La aplicación es una cadena de cinco eslabones —entrar, pedir, encontrarse, trabajar y
controlar— y hasta el 31 de agosto de 2026 el tercero **no existía**: ninguna tabla ataba un
aviso con un Asistente. Lo crea la migración
`supabase/migrations/0054_el_contacto_es_lo_unico_que_queda_guardado.sql`, y lo que sigue es lo
que esa migración deja abierto a propósito.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| ~~136~~ | **Cerrado el 1 de septiembre de 2026: las tres pantallas existen y el aislamiento se probó.** La migración 0054 había creado `postulaciones`, `conversaciones` y `mensajes` sin que ningún archivo del navegador las nombrara. Hoy los dos caminos del mercado (`CLAUDE.md` §1) terminan en una pantalla: el Asistente ve los avisos abiertos de su Prestadora y se postula desde ahí (`pwa-asistente/index.html`, pantalla `avisos`); la Familia lee las postulaciones que le llegaron y contacta desde el perfil del directorio (`pwa-familia/index.html`, pantalla `postulaciones`, y `perfil.html`); y las dos partes escriben en la misma conversación, que es un solo archivo compartido por los dos programas (`js/conversacion.js`, copiado a las dos carpetas y comparado byte a byte por `scripts/verificar_copias.mjs`). Las tres tienen sus cuatro estados y su texto en los tres idiomas. **Lo que hizo falta agregar del lado de la base fue la migración 0055**, con cuatro funciones `security definer` que le muestran al Asistente el aviso sin mostrarle quién lo puso: la RLS sola no servía, porque el aviso es de la Familia y darle la fila entera le habría dado también el contacto, que es justo lo que la Prestadora cobra. **Y el aislamiento se comprobó, no se supuso**: `scripts/probar_aislamiento.mjs` pasó de 82 a 100 comprobaciones, y las 18 nuevas cargan un contacto inventado de verdad en el aviso de prueba antes de preguntar —sin eso la comprobación habría mirado una columna vacía y dado verde con la función rota—, verifican que ninguna de las cuatro funciones devuelve `contact_info`, `familia_id` ni el nombre del paciente, que un Asistente ve el aviso de su Prestadora y ninguno de la otra, que una Familia recibe cero avisos, y que sin sesión las cuatro callan | **Hecho.** Queda abierto el **137**, que es lo otro que hace falta para que el canal no se use para saltear a la Prestadora, y el **141**, que es la mitad operativa |
| 137 | **Adentro de la conversación no hay nada que impida pasar un teléfono o un correo.** El Desarrollador lo recordó el 24 de agosto de 2026: si los datos de contacto se filtran por el canal, las dos partes siguen afuera y la Prestadora cobra una sola vez y nunca más. `mensajes` guarda el contenido tal como llega, y **el control tiene que estar en el servidor**: uno escrito en el navegador lo saltea cualquiera que mande el pedido a mano | La escritura de un mensaje pase por una comprobación del lado de la base o del servidor, el texto que avisa salga del catálogo en los tres idiomas, y una prueba mande un mensaje con un teléfono adentro **por fuera de la pantalla** y compruebe que no queda guardado |
| 138 | **La Prestadora no tiene cómo ver cuántos contactos hubo, que es justo lo que cobra.** La 0054 no le concede nada sobre las tres tablas, y es a propósito: el contenido es de las dos partes. Pero el hecho del contacto es el único dato comercial del producto, y hoy no hay forma de contarlo. **Depende de que se descongele la lógica comercial** (`docs/ALCANCE.md` §4), así que no se construye antes | Esté decidido qué se cobra, a quién y cuándo; y recién entonces una migración le dé a la Prestadora una vista **agregada y sin contenido** —cuántos contactos, por cuál camino y cuándo—, con una prueba que compruebe que desde esa vista no se llega a ningún mensaje |
| 139 | **La tabla `messages` quedó superada y sigue en pie.** Cuelga de un aviso (`search_id`, `supabase/migrations/0001_esquema_inicial.sql:137-142`), y por eso **no sabe representar el camino del directorio**, donde no hay ningún aviso. La 0054 la reemplaza con `conversaciones` y `mensajes`, que cuelgan del vínculo entre las dos personas. Dejarla es dejar dos lugares para lo mismo, que es exactamente lo que la regla del punto único de verdad evita. **Borrar datos necesita la palabra del Desarrollador**, aunque la tabla esté vacía | El Desarrollador confirme que se va, una migración la saque, y ningún archivo del navegador la nombre |
| ~~140~~ | **Cerrado el 1 de septiembre de 2026: la alarma que ve la Familia.** El Desarrollador fijó el 31 de agosto de 2026 que el producto **avisa a la Familia cuando detecta que algo no está bien** (`CLAUDE.md` §1), y que ése es el límite: informa y no decide. Lo que faltaba decidir —qué cuenta como «algo que no está bien»— no hacía falta inventarlo: **lo contesta la regla de que el software no sabe de ningún trato**. Sin horario acordado no se puede decir «faltó», porque no se sabe a qué hora tenía que estar, así que todo lo que el producto detecte tiene que salir **de las fichadas mismas**. Quedan dos hechos, y los dos son de la propia marca y no de la persona: `jornada_abierta` —una entrada que nunca cerró, más vieja que el tope de horas— y `salida_sin_entrada` —una salida sin entrada delante—. **«Hace días que no ficha» quedó afuera y no por olvido**: para llamarlo raro hay que saber que se esperaba una jornada, y eso es el trato. El tope no está escrito en el código: es una fila por Prestadora en `alarmas_prestadora`, de fábrica dieciséis horas, porque una Prestadora de internación nocturna y una de cuatro horas por la mañana no tienen el mismo número. **Y la Prestadora configura el tope pero no lee ninguna alarma**, por lo mismo que no lee la fichada: mirar si una jornada quedó abierta es mirar los horarios que cumple una persona. La función devuelve `clase` como **clave y no como frase**, así que el texto sale del catálogo en los tres idiomas y una clase que la pantalla no sabe nombrar no se dibuja muda. | **Hecho.** Migración 0057 —la tabla del tope con su RLS, el disparador que se la deja a toda Prestadora nueva, el arreglo para las que ya estaban, y `mis_alarmas()` con `PUBLIC` y `anon` revocados—; `misAlarmas()` en `js/apiClient.js`; la franja de alarmas arriba de la pantalla `asistencia` de `pwa-familia/index.html`, con sus cuatro estados y su reintento; nueve frases en los tres idiomas; y las comprobaciones 69 a 75 de `scripts/probar_aislamiento.mjs`, que pasaron entre las 127 el 1 de septiembre de 2026. **La prueba carga cinco marcas a propósito** —una jornada que cerró, una salida huérfana, una entrada vieja que nunca cerró y una recién marcada— y exige **exactamente dos** alarmas: sin las tres que no alarman, ni el tope ni el cierre de jornada estarían probados |
| ~~141~~ | **Cerrado el 1 de septiembre de 2026: la fichada se ata al vínculo y la Familia la ve.** `clock_ins` no tenía ninguna columna que la uniera a una Familia, así que la migración 0053, al sacársela de la vista al personal de la Prestadora, dejaba el dato sin nadie que lo mirara salvo el propio Asistente. La migración 0056 le agrega `conversacion_id`, con una clave foránea compuesta que exige que la conversación sea de la misma Prestadora, y dos políticas: el Asistente sigue viendo las suyas y **no puede colgar una de una conversación ajena** (`with check`), y la Familia lee de sólo lectura las de sus vínculos. Del lado de la pantalla, el Asistente elige para qué vínculo es la jornada antes de fichar (`pwa-asistente/index.html`, desplegable `gps-vinculo`) y la Familia las mira en la pantalla `asistencia` de `pwa-familia/index.html`, con sus cuatro estados y su reintento. **Es informativo y no decide nada**: no hay «aprobar», ni «validar», ni total de horas, ni liquidación. La fichada marcada sin vínculo —la única que se podía marcar antes— no se le abre a nadie por haber quedado suelta. | **Hecho.** Migración 0056; `js/apiClient.js` con `fichadasDelVinculo()` y `conversacionId` en `registrarFichadoGPS()`; las dos pantallas; y las comprobaciones 64 a 68 de `scripts/probar_aislamiento.mjs`, que pasaron entre las 127 el 1 de septiembre de 2026 |

---

## Aparecidos al comparar con un competidor real

Salieron del relevamiento del 24 de agosto de 2026. El detalle está en `docs/CATALOGO.md`,
sección «Lo que no es una lista».

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 20 | **Las claves viejas de Tipo de Asistente no estaban donde decía este renglón.** Decía que `caregivers.profession` guardaba `domiciliaria`, `enfermera`, `auxiliar` y `at`. Revisado el código el 24 de agosto de 2026, esas palabras estaban escritas en pantallas y no en la base: `directorio.html` las tenía en los cuatro filtros y en las ocho tarjetas de muestra, y `formulario-integral.html` en las tarjetas del paso 3: **las dos están corregidas y hoy salen del catálogo** (`formulario-integral.html:164`, comprobado el 25 de agosto de 2026). La siembra sí escribió claves malas, y eso lo repara la migración 0010 (pendiente 27, cerrado). **De la base no se puede afirmar nada desde acá**: `caregivers` le niega la lectura a quien no inició sesión, así que no se sabe si quedó alguna fila con una clave vieja escrita antes de que las pantallas tomaran el catálogo | Se borren los datos de personas antes de producción, que es cuando desaparecen esas filas. Son todas inventadas, así que **no se migran: se descartan** |
| 31 | **Dos columnas guardan catálogo sin tener catálogo.** `avisos.schedule_type` tiene hoy cuatro formas para lo mismo: la siembra escribe `turno_manana` y `guardia_12`, `js/main.js:258` escribe `flexible` y `js/formulario-consulta.js:214` escribe `A coordinar`. Ninguna es una clave de vocabulario: `guardia_12` es de `modalidad_contratacion` y `turno_manana` no es de ninguno. `caregivers.nationality` tampoco tiene vocabulario. Mientras no lo tengan, `scripts/verificar_claves.mjs` no las puede mirar. **Y ya cuesta:** la migración 0030 sembró diez avisos nuevos el 26 de agosto de 2026 y los diez quedaron **sin** `schedule_type`, a propósito, porque escribir una quinta forma para lo mismo empeora el problema en vez de arreglarlo. El horario de esos avisos vive en `franjas_aviso`, que sí tiene vocabulario. O sea que hoy la columna está vacía en la mayoría de las filas sembradas, y quien la lea desde una pantalla no va a encontrar nada. **La tercera, `cursos.nivel`, se cerró el 26 de agosto de 2026** y no esperaba una decisión: sus tres claves ya estaban guardadas desde la migración 0008, así que sólo faltaba el texto para mostrarlas (`data/catalogo-vocabularios.json`, vocabulario `nivel_curso`) | Se decida qué vocabulario gobierna cada una de las dos que quedan —si `schedule_type` es un turno, una modalidad de contratación o las dos cosas, que entonces son dos columnas—, se agregue a `data/catalogo-vocabularios.json` lo que falte, y las dos columnas entren en `COLUMNAS` de `scripts/verificar_claves.mjs` |
| 132 | **Dos servicios del catálogo siguen diciendo que los cursos son de las Familias, y uno de ellos está en la base publicada.** La fila 35 sacó el supuesto de los seis cursos, del título y del encabezado de `cursos.html`, pero no de la sección `servicios` de `data/catalogo-oferta.json`, donde el servicio `cursos` se llama «Cursos para familias» y se describe como «Formación para familiares de personas mayores…», y el servicio `asistente_virtual` ofrece «Respuestas a las dudas de quien acompaña a un familiar». **«Cursos para familias» está además en** `data/catalogo-vocabularios.json`, que `scripts/verificar_catalogo.mjs` compara contra la base **publicada**: cambiarlo ahí no es editar un archivo, es una migración. Se dejó afuera a propósito el 31 de agosto de 2026 para no meter una migración sobre la base publicada adentro de un trabajo de redacción | Los dos servicios estén escritos sin suponer quién cursa, en los tres idiomas, y el vocabulario esté cambiado por migración y aplicado a la base publicada |
| 40 | **Queda una columna vieja en `avisos` que ninguna pantalla vuelve a escribir.** El título anterior decía que la tabla de franjas no estaba en el servidor, y su propio cuerpo ya decía lo contrario; se corrigió el 26 de agosto de 2026 después de ver el esquema en vivo, donde `franjas_aviso` está y `grid_schedule_7x3` también. El 25 de agosto de 2026 las dos pantallas —`pwa-familia/index.html` y `formulario-integral.html`— pasaron a preguntar la misma grilla de veintiún casilleros que usa el Asistente, y `js/apiClient.js` escribe una fila por casillero en `franjas_aviso`. **La migración 0016 ya corrió**: la línea de comandos quedó enlazada con el proyecto de Supabase y aplica las migraciones sola, y el 25 de agosto de 2026 el servidor contestó que están las veinte. Así que la tabla existe y las franjas se guardan. Lo único que queda es **`avisos.grid_schedule_7x3`**, que sigue ahí con las dos formas viejas adentro y que ninguna pantalla vuelve a escribir. Lo hecho está en `docs/ALCANCE.md` | `grid_schedule_7x3` esté vaciada y sacada con `alter table public.avisos drop column grid_schedule_7x3;`. Toca datos guardados, así que la ordena el Desarrollador |
| 41 | **El nombre del paciente guarda cuatro datos pegados en un solo texto.** `js/main.js` arma `patient_name` como `Paciente de 82 años (femenino) - Acompañamiento de tarde`: edad, género, título del aviso y la palabra «Paciente», todo en la columna que debería tener un nombre. Ninguno de los cuatro se puede filtrar ni contar, y la edad y el género ya tienen vocabulario propio. La pantalla del teléfono, en cambio, guarda ahí sólo el nombre, así que la misma columna tiene hoy dos formatos | `avisos` tenga columna para la edad y para el género del paciente, y otra para el título del aviso, y `patient_name` guarde el nombre y nada más. **Toca dato ya guardado, así que lo decide el Desarrollador** (la regla de la empresa «se consulta cuando hay que borrar o pisar datos»): son cuatro filas ficticias, pero la migración tiene que decir qué se hace con lo que ya está escrito |

---

## Los dos mundos: CeltaTech y sus Clientes

El Desarrollador fijó el 31 de agosto de 2026 que **CeltaTech no presta ningún servicio: lo
prestan sus Clientes**, y que con la gente que trata con un Cliente —Familias, Asistentes,
proveedores, personal— CeltaTech no tiene vínculo de ninguna índole. La regla quedó escrita en
`..\..\CLAUDE.md`, sección «CeltaTech no presta ningún servicio, y la línea termina en el
Cliente». Los tres documentos legales ya se corrigieron ese mismo día; acá está lo que la
decisión abre y todavía no existe.

Ese mismo día precisó dónde vive cada documento: **los de CeltaTech, adentro de sus términos y de sus contratos con el Cliente**; **los de cada Prestadora, de ella**, y CeltaTech a lo sumo le entrega un modelo **a título de sugerencia y sin ninguna responsabilidad sobre él**.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 133 | **El software no le da a la Prestadora ningún lugar para tomar sus propios recaudos legales.** El Desarrollador lo pidió explícitamente ese día: *«el software proveerá al cliente (prestadora) un entorno para que ellos tomen sus recaudos»*. Hoy no hay tal entorno: los tres documentos son tres archivos fijos en `docs/`, iguales para toda Prestadora, escritos por CeltaTech y enlazados a mano desde las pantallas —`index.html:517` y `:518`, `directorio.html:201` y `:202`, `solicitar-asistente.html:407` y `:408`—. Eso contradice la decisión por partida triple. **Una**: se dicen de la Prestadora según su propio encabezado, pero ella no los puede ni ver ni cambiar. **Dos**: una segunda Prestadora con otro criterio no tiene dónde poner el suyo. **Y tres, que es la grave**: lo que CeltaTech puede entregar es **un modelo a título de sugerencia**, y hoy esos textos no se entregan, **se aceptan** —con casilla obligatoria al darse de alta—, así que la persona está aceptando un documento que escribió CeltaTech y que ninguna Prestadora adoptó. Un modelo que alguien firma dejó de ser un modelo. Es además la única forma de que el producto deje de hablar por sus Clientes **El 31 de agosto de 2026 el Desarrollador decidió dónde va ese entorno: en la configuración del producto**, donde la Prestadora tiene el modelo delante y puede **editarlo, reemplazarlo por el suyo o no usar ninguno**. De ahí se sigue lo que hoy está mal y ya no es opinable: **la casilla obligatoria tiene que seguir a lo que la Prestadora configuró**, y si eligió no poner ningún documento, no hay nada que aceptar | Cada Prestadora pueda cargar, editar, reemplazar o dejar vacíos sus textos legales desde la configuración, las pantallas muestren los de la Prestadora por la que se entró —y ninguno si ella no puso ninguno—, y los tres archivos de `docs/` pasen a ser lo que la decisión dice que son: **un modelo sugerido**, que lo diga en su cara —quién lo entrega, que no es asesoramiento legal, que adoptarlo es decisión de la Prestadora—, y que ninguna pantalla haga aceptar un texto que ninguna Prestadora adoptó |
| 134 | **Los documentos legales de CeltaTech no existen, y son el único lugar donde pueden vivir los de este producto.** El Desarrollador lo precisó el 31 de agosto de 2026: los documentos legales de un producto viven **en los términos y condiciones y en los contratos entre CeltaTech y sus Clientes**. Ninguno de los dos está escrito, así que hoy este producto **no tiene dónde poner lo suyo**. Es el otro mundo, y falta entero. Los tres documentos legales corregidos ese día dicen ahora que lo que CeltaTech deba por el software se lo debe a la Prestadora «en el acuerdo entre las dos, que es otro documento y no éste» —`docs/terminos_y_condiciones_familias.md` §10.1 y `docs/terminos_y_condiciones_asistentes.md` §10.1—, **y ese otro documento no está escrito**. Mientras no lo esté, tampoco se puede contestar el hueco que quedó en `docs/politica_de_datos.md` §2.4: con qué nombre figura cada una ante el organismo de control. Y no es sólo que convenga tenerlo: la **Ley 25.326, art. 25** exige un contrato cuando alguien trata datos personales por cuenta de un tercero, y el **Decreto 1558/2001, Anexo I, art. 25** fija qué tiene que decir —niveles de seguridad, confidencialidad y reserva, que quien trata actúa sólo siguiendo instrucciones de quien es responsable, y que las medidas de seguridad del art. 9 también le incumben—. **No es de este producto**: es de CeltaTech y vale para todos, así que esta fila existe para que no se pierda, no para construirla acá | Existan los términos y condiciones de CeltaTech y el contrato con la Prestadora, este producto tenga adentro de ellos lo que le toca, y esta fila se mude a donde CeltaTech lleve los suyos |
| 135 | **El documento legal de la Argentina no nombra al organismo de control, y por eso dos documentos de este producto tienen un hueco.** La regla de la empresa dice que el aviso legal sale del documento legal de ese país y no se improvisa ni se deduce por parecido con otro, así que `docs/terminos_y_condiciones_asistentes.md` §4.13 y `docs/politica_de_datos.md` §14 dejan el hueco marcado a propósito. Una búsqueda documental del 31 de agosto de 2026 sobre fuentes oficiales encontró el dato: el organismo es la **Agencia de Acceso a la Información Pública**, creada por la Ley 27.275, y la cadena que la pone al frente de la Ley 25.326 pasa por el Decreto 746/2017 y el Decreto 899/2017, que sustituyó el art. 29 del Decreto 1558/2001. **Pero ese dato no se escribe acá**: va primero al documento legal del país, `..\careonys\docs\legal\argentina.md`, que vive en Careonys y **no se toca sin orden explícita**. La misma búsqueda dejó otro asunto que ese documento tampoco trae y que ninguno de los tres de acá menciona: el **art. 12 de la Ley 25.326**, que prohíbe la transferencia internacional de datos a países sin nivel de protección adecuado y **no trae excepción** para quien trata por cuenta de un tercero, lo que pega de lleno si el software aloja datos fuera de la Argentina. Ninguna de las dos cosas es asesoramiento legal: es investigación documental, y va a la revisión del abogado al cierre | El documento legal de la Argentina nombre al organismo de control y trate la transferencia internacional, y los dos huecos de acá se completen desde ahí |

---

## Se resuelve dentro de la migración a React

Nada de esto conviene arreglar antes: la migración reescribe cada pantalla y el arreglo se tiraría.

**Dos condiciones necesarias, no comodidades** —remarcado por el Desarrollador el 2026-08-26—:
i18n y que el modo claro/oscuro siga el del dispositivo. Las dos están **construidas**: la
del modo en `css/tokens.css` (ver `docs/DISENO.md`), y la del idioma en `data/catalogo-frases.json`
con `js/catalogo.js`. **Por eso el pendiente 9 ya no está en este grupo**: se mudó a «antes de
portar la primera pantalla» el 26 de agosto de 2026, porque cada pantalla nueva escrita a mano lo
encarece y la migración va a agregar pantallas. Ninguna pantalla nueva se da por terminada
mientras lea texto escrito a mano en vez del catálogo de frases, y
`scripts/verificar_frases.mjs` lo comprueba antes de cada `commit`.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 69 | **La red de chequeos no sobrevive a la migración, y no lo diría.** Es el paso 1 de «antes de un cambio grande» —el inventario— hecho sobre la red que sostiene al producto en vez de sobre el producto. **Medido el 26 de agosto de 2026 leyendo los dieciséis:** ninguno nombra `.jsx` ni `.tsx`, así que el día que la primera pantalla deja de ser un `.html`, **nueve dejan de mirarla y siguen diciendo ✔**, con un número más chico que nadie mira; `guiones` se queda revisando cero bloques, porque revisa los `<script>` de adentro del HTML y en Vite no hay ninguno. No es que la migración rompa la red: **la desarma en silencio**, que es peor, porque el `commit` sigue saliendo verde. Y no alcanza con agregarles la extensión: cuatro buscan una forma que en React no existe —el color adentro de `style="..."`, las plantillas que arman HTML, los manejadores escritos en el marcado, y el atributo `data-frase` que marca una pantalla como convertida—. **Lo que sí avisa fuerte es uno solo**, `referencias`, porque nombra caminos exactos y al renombrarse las pantallas no los encuentra; conviene no apagarlo. **De `copias` este renglón decía lo mismo y estaba mal:** el 28 de agosto de 2026 se corrió la mudanza de verdad, en una copia del proyecto con los dieciséis `.html` renombrados a `.jsx`, y `copias` dio los mismos 29 grupos que antes sin una sola diferencia, porque su lista nombra `.js`, `.json` y `.css` y ningún `.html`. **Y apareció algo que se midió al revés de lo esperado:** `contacto` e `identidad` corren el mismo código que corre la pantalla, cargándolo con `require()`, y eso funciona porque hoy no hay `package.json`. Levantar Vite crea uno con `"type": "module"`, y ahí `require()` **no falla: devuelve un objeto vacío**, probado, no supuesto. Que se note depende de una casualidad: los dos usan lo que cargaron enseguida y sin preguntar, así que revientan en el acto; si alguno hubiera preguntado antes «¿tiene nombre?», habría pasado en verde sin comprobar nada — la trampa del `undefined` que nombra la regla de la empresa. El detalle entero, chequeo por chequeo, está en `docs/ALCANCE.md`, al final de la hoja de ruta. **Hecha el 28 de agosto de 2026 la mitad que no esperaba a la migración**, porque protege hoy: `scripts/recorrido.mjs` tiene ahora `seRevisaron()` y `hayArchivos()`, y los diecinueve chequeos que recorren algo se plantan si ese algo da cero —dieciséis puntos donde se pedían archivos y ocho donde lo que podía quedar vacío era una lista o un catálogo—. El vigilante es `scripts/verificar_red.mjs`, chequeo número 21, que exige la guarda en cada `verificar_*.mjs` leyendo el archivo sin sus comentarios, para que nombrarla en un encabezado no cuente, y con un solo exento y el motivo escrito al lado. Se comprobó que puede fallar quitándole la guarda a `verificar_trato.mjs` en la copia de prueba: lo nombró y salió en rojo. **Y midiendo en vez de leyendo, la cuenta del 26 salió corta:** no eran nueve los que se callaban sino once, y **diecisiete de los veinte chequeos seguían diciendo ✔** después del renombre; `estilos` era uno de ellos y no estaba en la lista, porque informaba «ninguno de los 0 atributos `style=` del marcado» y lo contaba como éxito. Lo que la guarda **no** tapa —perder parte del corpus en vez de todo— quedó abierto en el pendiente 91. **Y el 31 de agosto de 2026 esa guarda pasó a comprobarse también corriendo y no sólo leyendo:** `verificar_red.mjs` mira que cada chequeo *nombre* a `hayArchivos` o a `seRevisaron`, y nombrarlas no es plantarse —un chequeo puede llamarlas para un corpus y trabajar con otro, y pasa con las dos palabras escritas y cero archivos revisados—. La segunda mitad de `scripts/probar_perdida_de_corpus.mjs` corre la red en una copia que sólo lleva `scripts/`: hoy se plantan 26 de 27, y el único verde es el exento. Se comprobó escribiendo un chequeo hecho para colarse: la mitad que lee lo dejó pasar y la que corre lo agarró. La lista de exentos dejó de estar adentro de `verificar_red.mjs` y vive en `ARMAN_SU_PROPIO_CORPUS`, en `scripts/recorrido.mjs`, pegada a la guarda de la que exime, porque dos listas que dicen lo mismo se arreglan una vez y queda mal la otra. **Y el 31 de agosto de 2026 se hizo la parte de la extensión, que también resultó no esperar a la migración:** estaba escrita a mano —`'.html'`— en cuarenta llamadas repartidas por diecinueve guiones, once de ellas en `verificar_paleta.mjs`, y ahora sale de `EXTENSIONES_DE_PANTALLA`, en `scripts/recorrido.mjs`, y de ningún otro lado. En la misma copia renombrada, ese solo renglón puesto en `['.jsx']` devolvió a sus números de siempre a los once que se callaban, y de veintiocho chequeos quedaron **tres** en rojo, que son justamente los tres que tienen que gritar: `estado` y `usos` comparan nombres de archivo escritos en la documentación, y `estados` busca las frases de su catálogo por el nombre de la pantalla. Los cuatro que cambian de forma no aparecen ahí, y no porque estén resueltos: un renombre no les cambia el contenido, así que recién se van a caer cuando el marcado sea React de verdad. Lo cuida `verificar_red.mjs`, que ahora también se pone en rojo si un chequeo vuelve a escribir la extensión a mano —probado con las tres formas, escrita, nombrada sólo en un comentario, y pedida como corresponde—, y él es el único exento de esa regla, porque sus pruebas de adentro la tienen que traer escrita para que haya algo que reconocer **Y el mismo día apareció una segunda forma de la misma atadura, que la regla nueva no veía:** la extensión metida adentro de la **clave de una exención**. `verificar_estados.mjs` eximía a `'mockup-app.html'`, y como la regla mira el literal suelto, la clave pasaba entera. El día que las pantallas dejen de ser `.html` esa exención no encuentra la suya y el chequeo se pone rojo por un motivo que no es el suyo. La clave se escribe ahora sin extensión y se la saca al comparar, y `verificar_red.mjs` tiene una sexta regla que se pone roja si alguna clave de exención vuelve a traerla —falsificada volviéndola a poner: la nombró y salió en rojo—. **No se ensanchó la regla vieja a propósito:** medido sobre los chequeos sin sus comentarios, la forma ancha agarra cinco archivos y uno solo es una atadura real; los otros cuatro son ejemplos de prueba, así que ensanchar hubiera pedido cuatro exenciones nuevas, que es justo lo que acababa de podrirse. | La red se adapte **antes** de la primera pantalla portada y no después: los cuatro que cambian de forma la tengan, `estado`, `usos` y `estados` dejen de atarse al nombre del archivo, `guiones` se jubile y `copias` se jubile recién cuando las tres copias dejen de existir. Y que ninguno pueda quedar mirando cero archivos y decir ✔: cada uno que se toque se arma un ejemplo malo y comprueba que lo agarra, como ya hacen `cajas` y `referencias` — **la guarda quedó hecha el 28 de agosto de 2026, y la extensión el 31, en sus dos formas: escrita a mano y metida adentro de la clave de una exención. De los tres atados al nombre del archivo, `estado` y `usos` no esperan a nadie: fallan fuerte y se arreglan corriendo su guión con `--escribir`, así que lo suyo es un minuto y no trabajo de migración. El que sí espera es `estados`, que busca las frases de su catálogo por el nombre de la pantalla, y con él lo que cambia de forma** |
| 12 | **Las dos PWAs enlazan al directorio padre** para imágenes y para pantallas del sitio: 3 enlaces en `pwa-asistente/index.html` y 13 en `pwa-familia/index.html`, más los que arma el guion (`../perfil.html` y `../examen.html`). Su service worker no alcanza esas rutas, así que fuera de línea no cargan | Haya un solo service worker en la raíz del sitio, que es lo que emite `vite-plugin-pwa`. El problema se disuelve, no se parcha |
| 13 | **`apiClient.js` y `auth.js` están triplicados byte a byte** —921 renglones en tres lugares— y `styles-pwa.css` duplicado. `css/tokens.css` está en tres lugares por la misma razón: el service worker de cada PWA solo alcanza su propia carpeta, así que fuera de línea no puede leer el del padre. **Y hay una tercera copia, más chica y más peligrosa**: el interruptor que enciende uno de «los cuatro estados» está escrito ocho veces, una por pantalla (`acceso.html:92`, `directorio.html:247`, `examen.html:297`, `mockup-app.html:445`, `nueva-clave.html:134`, `perfil.html:291`, `pwa-familia/index.html:1156` y `recuperar-clave.html:120`). Ya se habían separado: cuatro imponían `display: 'block'` y dos devolvían `display: ''`, que no es lo mismo cuando el panel es grid o flex. El 25 de agosto de 2026 se igualaron a la forma segura y se les escribió el motivo al lado. El 26, al pasar esconder a la clase `oculto` —contado en `docs/ALCANCE.md`—, las seis que escribían en el atributo `style` pasaron a `classList.toggle('oculto', …)` —vaciar el atributo ya no muestra nada, porque sin nada escrito ahí vuelve a mandar la clase— y las otras dos ya usaban `hidden`. Siguen siendo ocho | Existan imports reales. Desaparece solo al migrar. Mientras tanto `scripts/verificar_copias.mjs` compara las cinco copias byte a byte y falla si alguna se separó: no las une, pero avisa, y corre solo antes de cada `git commit` |
| 14 | **Dos fuentes de datos conviviendo**: Supabase y datos escritos en el HTML. Eran cuatro. El 24 de agosto de 2026 se fueron dos: el almacenamiento del navegador —el interruptor `useSupabase` valía siempre `true`, así que cada método de `js/apiClient.js` arrastraba una copia muerta que escribía en el navegador, y `js/main.js` sí escribía ahí de verdad y anunciaba éxito— y `data/cuidadores.json`, cuatro perfiles de muestra que no leía nadie. El 25 de agosto de 2026 se fueron las dos últimas: las tarjetas de Asistente escritas a mano en `mockup-app.html` y las ocho fichas de `perfil.html`, que hoy salen de `directorio` —contado en `docs/ALCANCE.md:881`—. **Ninguna pantalla tiene ya datos escritos adentro**, se dio por comprobado el 25 de agosto de 2026, **y el 26 de agosto de 2026 apareció que no era cierto**: `pwa-familia/index.html` y `mockup-app.html` traen escritas a mano una ficha de paciente y un recuadro de verificaciones, que son el pendiente 65. Lo que sigue habiendo es una segunda fuente que no es el HTML: los seis `data/catalogo-*.json`, que son el pendiente 7 | Quede una sola. El punto 2 ya se decidió el 24 de agosto de 2026, así que estas pantallas se pueden tocar: el directorio se ve sin sesión y tiene que leer de la base |
| 16 | **Las imágenes traen la paleta ajena adentro.** Las nueve ilustraciones de `assets/images/` están dibujadas con los colores del sitio de origen. Cambiar las variables CSS no cambia un `.png`: las pantallas van a quedar con la paleta de Careonys alrededor y la vieja en el medio | Cada ilustración se rehaga con la paleta propia o se reemplace. Va con el mapeo del sistema de diseño, no antes |
| 100 | **El panel de la Prestadora muestra dos números inventados y cuatro enlaces que no llevan a ningún lado.** De los cuatro recuadros de arriba, dos los llena la base —`kpi-revision` y `kpi-validados`, en `panel-prestadora.html:282-283`— y **dos están escritos a mano en el HTML**: «Presentismo GPS Hoy 100%» (`panel-prestadora.html:56`) y «Alertas Urgentes 0» (`panel-prestadora.html:60`). No los toca ningún guion: dicen siempre lo mismo, pase lo que pase. Y de las seis entradas del menú lateral (`panel-prestadora.html:36-41`), **cuatro siguen siendo `href="#"`** —Entrevistas de Selección, Presentismo GPS Vivo, Avisos de Familias y Facturación & Liquidación—. Eran cinco de cinco, incluida la que aparecía marcada como activa: el 30 de agosto de 2026 la activa pasó a apuntarse a sí misma y se agregó «Guías de cuidado», que lleva a `guias-prestadora.html`, la pantalla con la que se cerró el pendiente 103 el 30 de agosto de 2026. Una pantalla que muestra un 100% fijo es peor que una vacía: la vacía se nota | Los dos recuadros salgan de la base como los otros dos, o se saquen hasta que haya de dónde sacarlos; y cada entrada del menú lleve a su pantalla o no esté. Se resuelve dentro de la migración a React, que reescribe esta pantalla entera |

### Reparto de los estilos pegados al HTML

Sirve para saber qué pantalla va a costar más y en qué orden conviene portar.

**Esta tabla no se escribe a mano.** Sale de `node scripts/medir_estado.mjs --escribir` y
`scripts/verificar_estado.mjs` falla si alguien la edita sin volver a medir. Se hizo así el 31 de
agosto de 2026, porque la que estaba escrita a mano decía 687 atributos `style=` cuando ya eran
247, y nombraba tres pantallas que hoy no tienen ninguno.

<!-- reparto: lo escribe scripts/medir_estado.mjs, no se edita a mano -->

| Archivo | Atributos `style=` | Declaraciones |
|---|---:|---:|
| `mockup-app.html` | 51 | 218 |
| `registrar-asistente.html` | 40 | 181 |
| `pwa-familia/index.html` | 28 | 91 |
| `solicitar-asistente.html` | 28 | 125 |
| `index.html` | 27 | 122 |
| `pwa-asistente/index.html` | 24 | 84 |
| `formulario-integral.html` | 20 | 67 |
| `panel-prestadora.html` | 16 | 51 |
| `cursos.html` | 9 | 30 |
| `directorio.html` | 2 | 3 |
| `perfil.html` | 2 | 7 |
| `examen.html` | 1 | 2 |
| `soporte-remoto.html` | 1 | 3 |

Hay además 977 renglones de CSS en bloques `<style>` adentro del HTML:
533 en `pwa-familia/index.html`, 270 en `pwa-asistente/index.html`, 174 en `examen.html`.

<!-- fin del reparto -->
