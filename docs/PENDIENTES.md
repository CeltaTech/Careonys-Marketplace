# Pendientes

> Lo que está abierto, con **condición de cierre** para cada punto. Sin condición de cierre un
> pendiente no se puede dar por resuelto, así que no entra sin ella.
>
> Cuando algo se cierra **se borra de acá**, no se tacha. Si era una afirmación de estado, pasa a
> `docs/ALCANCE.md` con archivo y renglón.

---

## Seguridad

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 24 | **La evaluación de muestra se puede aprobar adivinando.** El mecanismo ya está bien: la corrige la base, la respuesta correcta no sale de ahí y el intento no se puede escribir a mano (migración 0008, comprobado en `scripts/probar_aislamiento.mjs`, puntos 21 a 27). Lo que falla es el contenido. Son **2 preguntas de 2 opciones cada una**, hay que acertar las dos y se dan **3 intentos**: quien no sepa nada acierta una vuelta con probabilidad 1 de 4, y en tres vueltas aprueba con probabilidad 37 de 64, **más de la mitad de las veces**. Un certificado que sale así sigue sin decir nada sobre quién va a entrar a una casa a cuidar a una persona | Cada evaluación publicada tenga preguntas suficientes como para que adivinar deje de ser un camino: con 10 preguntas de 4 opciones y 70% para aprobar, la probabilidad de acertar de casualidad baja a menos de 1 en 300. **El contenido no lo escribe la línea de comandos**: las preguntas las tiene que redactar alguien que sepa del tema, y la línea de comandos las carga. Mientras tanto la única evaluación cargada es la de muestra que sembró la migración 0008, y así está descrita ahí |
| 45 | **El proyecto alojado manda los correos con el servicio de fábrica, y ese servicio tiene un tope bajo.** Cada alta de Asistente o de Familia dispara un correo de confirmación, y a los pocos seguidos el servidor contesta *«email rate limit exceeded»* a todos por igual, incluso a `gmail.com`. No es un problema de pruebas solamente: un día con varias altas de verdad deja gente afuera, y lo que esa gente ve es que el alta falló. Comprobado el 24 y el 25 de agosto de 2026 intentando registrar cuentas ficticias contra el servidor alojado. **Y ya no es sólo un problema de altas: desde el 26 de agosto de 2026 la prueba de aislamiento no puede ni empezar.** `scripts/probar_aislamiento.mjs` arranca creando dos cuentas ficticias, y el tope la corta en la primera, así que la comprobación de seguridad más importante del producto quedó sin poder correr. El tope además se disfraza: la primera vez contesta *«Email address … is invalid»*, que manda a buscar el problema en el dominio del correo, y recién al reintentar dice *«email rate limit exceeded»*. **Esto lo abre el Desarrollador**: es configuración del proyecto alojado, `supabase/config.toml` no tiene llave para eso y `supabase config push` no la alcanza — se carga un servidor de correo propio en Authentication → Emails → SMTP Settings | El proyecto alojado mande los correos por un servidor propio, y varias altas seguidas dejen de fallar por el tope |
| 47 | **No hay ninguna cuenta de Asistente con la que se pueda entrar.** Este pendiente eran dos problemas y queda uno. **El cerrado**: las pantallas de acceso de las dos aplicaciones del teléfono traían un correo y una contraseña ya escritos en el HTML, cosa que no sobrevive a la primera persona real; los cuatro campos se vaciaron el 25 de agosto de 2026 y hoy arrancan con su indicación adentro (`pwa-asistente/index.html:310` y `:313`, `pwa-familia/index.html:571` y `:574`). **El abierto**: esa cuenta tampoco existía —contra el servidor alojado contesta «Invalid login credentials», comprobado el 25 de agosto de 2026—, así que nada de lo que vive detrás de una sesión se puede probar de punta a punta: «Mis Capacitaciones» se cerró sin haber podido ver la lista con los cursos de verdad. Crear una cuenta nueva choca con el tope de correos del pendiente 45 | Exista una cuenta de Asistente ficticia con la que se pueda entrar, lo que depende del pendiente 45 |
| 49 | **Los dos borradores legales dicen que no se muestran a nadie, y las pantallas los muestran con una casilla obligatoria.** `docs/terminos_y_condiciones_asistentes.md` y `docs/terminos_y_condiciones_familias.md` empiezan los dos con el mismo aviso: son un borrador sin revisión profesional, necesitan un abogado —sobre todo el deslinde de relación laboral, que es el punto más sensible del rubro en Argentina— y **no se muestran a ninguna persona usuaria hasta esa revisión**. Pero `registrar-asistente.html:522` y `formulario-integral.html:417` los enlazan cada uno junto a una casilla `required`: nadie se da de alta sin marcar que los leyó y los acepta. Están además en el pie de `index.html:508`, `formulario-integral.html:464` y `registrar-asistente.html:598`. Hoy no hay ninguna persona real y no le pasa nada a nadie; el día que la haya, la plataforma va a estar haciendo aceptar un texto que ella misma declara no revisado. **Esto no lo puede cerrar la línea de comandos**: hace falta que un abogado lea los dos documentos | Un abogado haya revisado los dos textos y el aviso de arriba se haya sacado, **o** las pantallas dejen de enlazarlos y la casilla no se pueda marcar. Lo que no puede seguir es que el documento diga una cosa y la pantalla haga la contraria |
| 61 | **Lo que se publica no es lo que se ve.** El sitio público `careonys-marketplace.vercel.app` sigue mostrando el directorio anterior al 24 de agosto de 2026: las ocho tarjetas inventadas escritas a mano —con nombre, edad y zona— que se sacaron en `fbb84d0`. **No es memoria del navegador ni el trabajador de servicio**: se pidió con `curl` y sin caché el 26 de agosto de 2026 y contesta lo viejo —la portada todavía se titula «PrestDemo» y tutea, `js/apiClient.js` contesta 200 y `data/catalogo-frases.json`, que se subió esa misma noche, contesta 404—. **La causa es más precisa de lo que decía este pendiente**, y se midió el 26 de agosto de 2026: las tres direcciones —la pública, `careonys-marketplace-celta-tech.vercel.app` y `careonys-marketplace-git-main-celta-tech.vercel.app`— están **las tres en la cuenta `celta-tech`, que es la misma a la que está enlazada la línea de comandos**, y `vercel alias ls` muestra a las tres clavadas al mismo despliegue de hace veintiún días. O sea que no es que el dominio sea de otra cuenta: **los despliegues nuevos llegan —uno por cada push, el último a los quince segundos del de esta noche— y ninguno se queda con la dirección**. Ahí hay que mirar. Y hay dos cosas que impiden comprobarlo desde acá: las dos direcciones largas contestan **302 hacia `vercel.com/sso-api`**, o sea que están detrás del inicio de sesión de Vercel y no las ve nadie de afuera —ni la línea de comandos, así que de lo recién publicado no se puede leer un solo byte—; y `vercel ls --prod` informa **todos** los despliegues con estado `UNKNOWN`, así que tampoco se puede confirmar que hayan construido bien. **Esto lo abre el Desarrollador**: son varias cuentas de Vercel, como con GitHub y Supabase, y la decisión de mover una dirección o aflojar la protección sale hacia afuera **Y la causa apareció el 26 de agosto de 2026, y no era ninguna de las que decía este renglón.** No es el dominio: `careonys-marketplace.vercel.app` está bien colgado del proyecto. No es que los despliegues no lleguen: llegan todos, uno por push, y traen la dirección con `-git-main-` adentro, que es la que Vercel le pone sólo a lo que entra por Git. **Los despliegues llegan y quedan bloqueados antes de empezar a construir**, con estado `UNKNOWN` en la línea de comandos y «Obstruido» en el tablero: veinte seguidos, el más nuevo a los quince segundos del último push. El motivo lo dice Vercel adentro del despliegue bloqueado: **«el autor de la confirmación no tenía acceso de contribución al proyecto»**, y abajo, «el plan Hobby no admite la colaboración en repositorios privados». El repositorio `CeltaTech/Careonys-Marketplace` es privado, la cuenta de Vercel es la de CeltaTech, y los commits venían firmados con el correo personal del Desarrollador, que GitHub atribuye a otra cuenta. Para Vercel eso es un colaborador, y en Hobby un colaborador no despliega un repositorio privado. **Se arregló haciendo que este repositorio firme como CeltaTech** —`git config user.email 310001372+CeltaTech@users.noreply.github.com`, sólo en esta carpeta—, que es la única de las cuatro salidas que no cuesta plata ni obliga a hacer público el código. Las otras tres, para que quede dicho por qué se descartaron: pagar Pro; hacer público el repositorio; o mudar el correo personal a la cuenta de GitHub de CeltaTech, que obliga a sacarlo antes de la otra cuenta porque GitHub no admite el mismo correo en dos | El dominio público sirva lo que hay en `main`, comprobado pidiendo una pantalla recién cambiada y viendo el cambio, y la protección de Vercel quede como el Desarrollador decida |
| 62 | **La tercera puerta está cerrada del lado del navegador y abierta del lado del servidor.** Desde el 26 de agosto de 2026 el chat reconoce un teléfono, un correo o un domicilio y no manda el mensaje (`js/contacto.js`, con las reglas en `data/patrones-contacto.json` y la prueba en `scripts/verificar_contacto.mjs`). **Eso no es un control**: corre en la máquina de quien escribe, así que quien quiera saltearlo abre la consola del navegador y lo saltea — y el que quiere saltearlo es exactamente la persona contra la que existe, porque llevarse la conversación afuera es lo que le conviene. La regla de la empresa «se valida en el servidor lo que entra» pide la otra mitad, y hoy **no se puede escribir**: no hay dónde ponerla. La tabla `messages` que hay en la base es del modelo equivocado y todavía no se decidió cuál queda (`docs/TABLAS_QUE_FALTAN.md`, puntos 2 y 3, y pendiente 6). Mientras tanto no hay daño posible, porque el chat de hoy no guarda nada: `messages` contesta 401 sin sesión. **Y hay algo que decidir de paso**: la regla nombra teléfono, correo y domicilio, pero un nombre de usuario de otra aplicación —«buscame en Instagram como…»— abre la misma puerta y bloquearlo puede cortar conversaciones legítimas. Es decisión del Desarrollador | La tabla del chat exista y su migración traiga, en el mismo archivo, el control que revisa el contenido antes de guardarlo — con las mismas reglas que usa la pantalla y no una segunda copia de ellas |
| 66 | **Un Asistente puede validarse solo el legajo, y con eso publicarse en el directorio como comprobado.** La política que le deja ser dueño de su propio legajo es `for all` y su `with check` nombra dos columnas —la cuenta y la Prestadora— y ninguna más (`supabase/migrations/0005_acceso_por_sesion.sql:177` a `:180`). `verification_status`, que es el dictamen de la Prestadora sobre los papeles de esa persona, no lo nombra nadie: **ni esa política, ni un disparador, ni un permiso de columna.** Comprobado el 26 de agosto de 2026 contra la base en vivo, no contra los archivos: se trajo el esquema real con `supabase db dump --linked` y ahí están las dos únicas políticas de `caregivers` —la del personal y la propia, con el mismo texto que dicen las migraciones—, `GRANT ALL ON TABLE public.caregivers TO authenticated`, **cero disparadores sobre `caregivers`**, y un solo permiso de columna en toda la base, que es `GRANT UPDATE(full_name) ON public.profiles`. **Por qué importa:** `directorio` publica exactamente a quien tenga `verification_status = 'validado_prestadora'` y `perfil_publicado` (`supabase/migrations/0026_el_directorio_dice_que_se_comprobo.sql:96`), y ese directorio se lee sin iniciar sesión por decisión del Desarrollador del 24 de agosto de 2026. La segunda condición es el consentimiento de la persona y le corresponde manejarlo; la primera es el sello que dice que la Prestadora le revisó el documento, los antecedentes y el título. Hoy lo único que separa a un desconocido de ese sello es una columna que él mismo puede escribir. **Y el camino está en producción, no es plomería teórica:** `registrar-asistente.html:972` da de alta a cualquiera con el rol `caregiver` (`:974`) y `:851` le crea el legajo con su `user_id`, así que la propiedad de la fila existe desde el primer minuto; de ahí en más alcanza un `PATCH` a `/rest/v1/caregivers` sobre la fila propia, sin pasar por ninguna pantalla del producto. Vale en los dos sentidos: el `with check` tampoco mira la columna al insertar, así que el alta puede nacer ya validada. **Es un olvido y no una decisión**, y se nota en que al lado está hecho bien: `verificaciones_asistente` —la prueba de cada control— lleva escrito «las escribe la Prestadora, nunca la persona verificada» y le da a la persona una política de sola lectura (`supabase/migrations/0005_acceso_por_sesion.sql:224` y `:231`), y `intentos_evaluacion`, de donde sale `curso_aprobado`, también es de sola lectura. Quedó protegida la evidencia y quedó suelto el veredicto. **Y el 26 de agosto de 2026 se hizo, así que ya no es una lectura de políticas: está ejecutado.** Este renglón decía antes que faltaba probarlo porque no había cuenta de Asistente; era cierto para las pantallas —siguen sin cuenta, pendientes 45 y 47— y no para la base local, donde el alta no pide confirmar el correo y una cuenta ficticia se crea sola. Contra la base local, con una cuenta recién nacida y datos inventados, con las 27 migraciones aplicadas, el resultado fue el peor de los posibles: **el legajo se creó directamente sellado** —un `POST` con `verification_status: 'validado_prestadora'` adentro contestó `201` y la columna quedó con ese valor, o sea que ni siquiera hace falta el `PATCH`—; **el sello se baja y se vuelve a subir cuando se quiera**, `PATCH` a `pendiente` y `PATCH` de vuelta a `validado_prestadora`, `200` las dos veces; y **la persona apareció en el directorio público**, pedido con la clave publicable y sin ninguna sesión, entre los legajos que la Prestadora dice haber validado. Todo lo ficticio se borró al terminar y se comprobó que ya no está en el directorio. **La prueba puede fallar, que es lo que la hace valer:** la misma cuenta, en la misma corrida, intentó mudar su legajo a la Prestadora ajena y recibió `403`. Si la barrera no existiera en ningún lado, ese intento también habría salido en verde y la prueba no distinguiría nada. **Y apareció algo que cruza con el pendiente 70:** la tarjeta del intruso salió con la lista `comprobaciones` vacía — que es exactamente lo que sale hoy en la tarjeta de cualquiera, porque ninguna pantalla carga esas comprobaciones. O sea que **la única señal que le habría permitido a una Familia notar la diferencia entre un legajo revisado y uno auto-sellado hoy no distingue nada**: los dos muestran la lista vacía. **Y el arreglo tiene una trampa que conviene dejar escrita:** un permiso de columna no sirve, porque el personal de la Prestadora y el Asistente son el mismo rol de base de datos —`authenticated`—, así que revocarle la escritura de esa columna se la saca también a quien tiene que validarla; lo que los distingue es la política, no el permiso. Quedan dos formas: partir la política propia para que su rama de actualización clave la columna, o un disparador `before insert or update` que rechace el cambio cuando `public.es_personal_de_prestadora()` sea falso. **Toca el modelo de seguridad y se aplica sobre la base de producción, así que va con plan y aprobación antes de código** | La columna `verification_status` sólo la pueda escribir el personal de la Prestadora. **La prueba ya está escrita y hoy da rojo a propósito**: `scripts/probar_sello_de_la_prestadora.mjs`, contra la base local, con una cuenta de Asistente inventada y las dos Prestadoras ficticias. Mira tres cosas —que el legajo no nazca sellado, que no se pueda sellar después, y que sin sello no salga en el directorio público— y las tres están en rojo. Se cierra cuando las tres den verde sin que se toque la prueba. **Y trae sus propias dos comprobaciones de sostén**, que se cuentan aparte porque no miden el producto sino si la prueba está en condiciones de medir: que la persona sí pueda corregir su teléfono, y que no pueda mudar su legajo a la Prestadora ajena. Si alguna de esas dos sale al revés, la prueba avisa que no sirve en vez de dar por bueno un verde que vendría de una sesión rota o de una tabla cerrada para todos |
| 67 | **Diecisiete tablas conceden más de lo que usan, y una de las que conceden es `TRUNCATE`.** La base en vivo tiene `GRANT ALL ON TABLE ... TO authenticated` sobre diecisiete tablas, aunque las migraciones que las pusieron a punto conceden sólo alta, baja, modificación y consulta (por ejemplo `supabase/migrations/0005_acceso_por_sesion.sql:242` para `caregivers`). **El motivo no es una migración vieja, y esto es lo importante del pendiente:** la base tiene `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public GRANT ALL ON TABLES TO anon` y lo mismo para `authenticated`, así que **toda tabla y toda vista nueva nace con todos los permisos dados a los dos roles**, sin que ninguna migración lo pida. No es residuo histórico: es una trampa que sigue armada para la tabla que se cree mañana. Comprobado con dos casos que no dejan lugar a duda. `ponderacion_comprobacion` la creó la migración 0022, que sobre permisos escribió una sola línea —`revoke all ... from anon`, en su renglón 159— y no le concedió nada a `authenticated`; la base en vivo le tiene `GRANT ALL ... TO authenticated`. Y la vista `directorio`, a la que ninguna migración le concedió jamás nada y a la que la 0021 sólo le revocó la consulta, en vivo conserva para `anon` y para `authenticated` los otros siete permisos, `TRUNCATE` incluido. Encima, revocar tampoco es hábito: sobre tablas se hizo una sola vez en toda la historia del esquema, con `profiles`. Lo que entra de más y no debería es `TRUNCATE`, que **la RLS no filtra**: no mira filas, vacía la tabla entera, así que ninguna política de aislamiento lo detiene. **Hoy no es alcanzable desde afuera** y conviene decirlo para no asustar de más: PostgREST no publica `TRUNCATE`, así que hace falta una conexión directa a la base con ese rol, y eso es una credencial, no una sesión. Pero el permiso está de más igual, y «mínimo privilegio siempre» no pide que haya daño para sacarlo. En el mismo lote entra `intentos_evaluacion`, que le concede consulta a `anon` sin tener ninguna política para `anon`: hoy niega —falla cerrado, que es lo correcto—, pero el permiso no hace falta. **Y volviendo sobre el esquema en vivo el 26 de agosto de 2026 apareció que la trampa es más ancha de lo que decía este renglón:** el permiso por omisión no está puesto sólo sobre las tablas. Están los tres —`ON TABLES`, `ON SEQUENCES` y `ON FUNCTIONS`—, cada uno con `GRANT ALL` a `anon` y a `authenticated`. **El de las funciones es el que más importa, porque es la causa de un síntoma que la regla de la empresa ya describe sin nombrarla:** `celtatech\CLAUDE.md`, en «La base de datos: sólo por migraciones», avisa que revocarle a `PUBLIC` no alcanza porque el permiso de `anon` es una concesión aparte — esta línea es esa concesión. Así que toda función nueva nace al alcance de cualquiera que tenga la clave pública, sin que ninguna migración lo pida. **Medido:** hoy hay cuatro funciones con la ejecución concedida a `anon`, y tres son a propósito —`directorio_de`, `perfil_del_directorio` y `prestadora_por_slug`, que son las puertas del directorio que se ve sin sesión—. La cuarta, `la_ponderacion_suma_cien`, no la quiso nadie. **No es alcanzable, y conviene decirlo entero para no asustar de más:** devuelve `trigger`, y PostgREST no publica las funciones de disparador; tampoco es `SECURITY DEFINER`, así que aunque se la pudiera llamar correría con los permisos de quien llama. Pero la concedió la misma línea que va a conceder la próxima. De secuencias hoy no hay ninguna —las 24 tablas tienen clave `uuid`—, así que esa tercera pata está armada y vacía. **Y hay una parte que ningún chequeo de los dieciséis puede ver:** `esquema` lee las 27 migraciones, no la base, así que una concesión que la base tiene y ninguna migración pide le resulta invisible. Estas cuatro aparecieron leyendo el volcado del esquema en vivo, no corriendo la red | Dos cosas, y la segunda es la que cierra el agujero de verdad. Una: cada una de las diecisiete tablas conceda solamente lo que usa, con una migración que revoque primero y conceda después. Dos: el permiso por omisión deje de dar todo, para que la tabla que se cree mañana no vuelva a nacer abierta. **La comprobación ya está escrita y hoy da rojo a propósito:** `scripts/probar_permisos_en_vivo.mjs`, que le pide el esquema real a la base —enlazada, o local con `--local`— en vez de leer las migraciones, que es donde esta clase de diferencia no se ve. Mira cuatro cosas y las cuatro están en rojo: que ninguna tabla, secuencia ni función nueva nazca con permiso para `anon` ni `authenticated` (hay seis líneas de `ALTER DEFAULT PRIVILEGES`, las tres patas por los dos roles); que ninguna tabla ni vista le conceda nada a `anon` (hay cinco: `intentos_evaluacion`, `directorio`, `cursos`, `evaluaciones` y `preguntas_evaluacion`); que ninguna le conceda **todo** a `authenticated` (hay diecisiete); y que no haya funciones al alcance anónimo fuera de las tres del directorio (hay una, `la_ponderacion_suma_cien`). **La quinta sale verde, y es la que hoy aguanta todo lo anterior:** las 34 políticas piden sesión, ninguna apunta a otro rol que `authenticated`. Por eso la prueba dice al final que lo que está mal no es que hoy entre alguien —no entra— sino que la puerta queda lista para el día que se escriba una política más floja. **Y trae dos comprobaciones de sostén**, que se cuentan aparte porque no miden la base sino si la prueba puede medir: que el volcado traiga tablas y políticas, y que se vean las tres puertas del directorio; sin ellas, un volcado vacío dejaría las cinco en verde. Se cierra cuando las cinco den verde sin que se toque la prueba |
| 68 | **No hay ningún registro de auditoría, y la regla de la empresa lo pide por nombre.** `celtatech\CLAUDE.md`, en «Seguridad, privacidad y auditoría», dice: *«Se audita toda acción sensible, y el registro tiene que permitir reconstruir quién, cuándo, sobre qué y qué cambió»*, y enumera el mínimo: entrada administrativa, cambios de permisos o de membresía, borrado de datos, modificaciones críticas, cualquier acción con consecuencia económica, y toda acción hecha durante una sesión de soporte. **Comprobado el 26 de agosto de 2026 contra la base en vivo:** ninguna de las 24 tablas es un registro de auditoría, ninguna migración crea uno, y la única «bitácora» que hubo era el cuaderno de cuidados, que la migración 0019 renombró a `reportes` — es del negocio, no de la seguridad. **Lo que sí existe, y conviene decirlo para no exagerar el agujero:** la plataforma trae `auth.audit_log_entries`, que está viva, es de `supabase_auth_admin` y no se alcanza desde el producto porque PostgREST no publica el esquema `auth`. Registra entradas, altas y cambios de clave, o sea **una astilla de «entrada administrativa» y nada de lo demás**. **El agujero más claro es la misma columna del pendiente 66:** `caregivers.verification_status` es el veredicto que publica a una persona en el directorio público como comprobada, y la tabla no tiene `validado_por`, ni `validado_el`, ni siquiera `updated_at` — las columnas se leyeron del esquema vivo, no de las migraciones. La escribe un `PATCH` pelado (`js/apiClient.js:226`). Así que hoy, cuando alguien aparece en el directorio como comprobado, **no hay forma de saber quién lo validó ni cuándo**, y por el pendiente 66 puede que no lo haya validado nadie. **Y otra vez se nota que es un olvido y no una decisión:** `verificaciones_asistente` —la evidencia de cada control— sí tiene `verificado_por` y `verificado_el`. Se pensó el rastro de cada papel y no el del dictamen que los resume. **Con una salvedad medida, no supuesta:** esas dos columnas tampoco las escribe nadie, porque `verificaciones_asistente` no aparece en un solo `.html` ni `.js` del proyecto — la pantalla que carga verificaciones no está construida. El rastro está diseñado y vacío. De ahí sale además que la lista `comprobaciones` del directorio (`supabase/migrations/0026_el_directorio_dice_que_se_comprobo.sql:66` a `:80`) hoy sólo puede salir vacía o con `curso_aprobado`. **Del borrado, la medición da tranquilidad a medias:** no hay un solo `DELETE` en `js/`, así que el producto no borra; pero `authenticated` tiene el permiso y las políticas de `caregivers` son `for all`, así que un pedido directo a la API borra la fila propia y no deja rastro. No es un agujero de aislamiento —son datos de esa persona— sino de reconstrucción. **Y la sesión de soporte todavía no existe**, así que su parte de la regla no está incumplida; pero construirla sin el rastro sería nacer incumpliendo, porque la regla pide que **todo** lo que se haga adentro quede auditado. **Por qué no se arregla solo:** hay que decidir dónde vive el rastro. Si es una tabla de este producto, se escribe acá; si es del módulo compartido de identificación —el primero de los dos que se decidieron el 26 de agosto de 2026— no se puede escribir antes que el módulo. Y hay una trampa de diseño que conviene dejar escrita: **un registro que el rol auditado pueda modificar no es un rastro**, así que la tabla tiene que poder recibir altas y no aceptar modificaciones ni bajas de `authenticated`, cosa que el permiso por omisión del pendiente 67 hoy no permitiría. **Va con plan y aprobación antes de código** | Exista un rastro que permita reconstruir quién, cuándo, sobre qué y qué cambió, para al menos: el cambio de `verification_status`, los cambios de rol o de membresía, el borrado de datos y todo lo que se haga en una sesión de soporte. Que el rol auditado pueda insertar en él y no pueda modificarlo ni borrarlo. Y una prueba con dos Organizaciones que valide un legajo y encuentre exactamente una fila con el autor correcto — **que tiene que fallar con el rastro sacado**, porque una comprobación que sólo lee la tabla pasa igual mientras esté vacía |

---

## Antes de portar la primera pantalla

Se hacen una sola vez, y si no se hacen, la decisión la termina tomando quien porta.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 6 | **`mockup-app.html` es el único lugar del proyecto donde existe un chat** (`screen-chat`, con suscripción en tiempo real y confirmación optimista) y la única pantalla de alta que sirve a una Familia: `pwa-familia` no tiene ninguna. Ninguna de las dos PWAs tiene chat. Su código **no se copia**: lee `messages?order=created_at.asc&limit=50` sin ningún filtro, se suscribe a la tabla entera sin filtro, evita `apiClient.js` y, sin sesión, intenta escribir con la clave pública —y no puede: la tabla `messages` contesta 401 a quien no inió sesión, así que **ningún mensaje del chat llegó nunca a guardarse**. Hasta el 25 de agosto de 2026 la pantalla no lo decía, porque `fetch` no falla cuando el servidor rechaza el pedido; ahora la burbuja avisa que no se pudo enviar. El mensaje recibido lo mete con `innerHTML`, pero **sí lo escapa** con `Texto.escapar()` (`mockup-app.html:775`), así que por ahí no entra una etiqueta; lo que no tiene es filtro ni sesión. **Para qué existe el chat** (lo recordó el Desarrollador el 24 de agosto de 2026): es la forma en que la Familia y el Asistente se comunican **antes** de la contratación. De ahí sale una condición que no estaba escrita en ningún lado: **adentro del chat no se pueden filtrar datos de contacto**. Si se filtran, la conversación sigue por afuera y la plataforma cobra una vez y nunca más, que es exactamente la razón por la que el teléfono no se muestra (`docs/CATALOGO.md`). Anotada ahí como la tercera puerta, y **cerrada del lado de la pantalla el 26 de agosto de 2026**: el chat reconoce un teléfono, un correo o un domicilio y no manda el mensaje, dice por qué, y deja el texto en el campo para que se pueda corregir. Las reglas están en `data/patrones-contacto.json` y no adentro del guion, porque son una regla operativa; el reconocedor está en `js/contacto.js`; y `scripts/verificar_contacto.mjs` lo corre contra dos listas —trece mensajes que no pueden pasar y trece que no pueden quedar bloqueados—, que es lo que hace que la prueba pueda fallar. **Lo que falta es la mitad que importa**: el control del lado del servidor, que hoy no tiene dónde ir porque la tabla del chat no existe. Es el pendiente 62. Y el diseño de las tablas ya existe en `docs/TABLAS_QUE_FALTAN.md` —`conversaciones` y `mensajes`, atadas a Familia, Asistente y aviso—, que **no coincide** con la tabla `messages` que hay hoy en la base, colgada de una búsqueda de cuidado y no del vínculo entre dos personas | Las dos capacidades estén anotadas como requisito para las PWAs y reescritas contra el esquema nuevo, y `css/mockup-app.css` haya pasado al sistema de diseño. Recién entonces se borran él y `mockup-app.html`. Y el chat no deje pasar un teléfono, un correo ni una dirección |
| 46 | **Un perfil publicado no tiene todavía con qué empezar una conversación con esa persona.** `perfil.html:148` dice la verdad —sólo una Familia registrada puede comunicarse, y por la plataforma— y ofrece las dos puertas que existen: entrar como Familia (`acceso.html`) y publicar un aviso (`solicitar-asistente.html`). Ninguna de las dos lleva a **esa** persona: el aviso lo contesta quien quiera, y entrar como Familia no abre nada contra ese Asistente. El mecanismo es el chat, que hoy existe solamente en `mockup-app.html` (punto 6), y las tablas diseñadas para eso —`conversaciones` y `mensajes`, atadas a Familia, Asistente y aviso, `docs/TABLAS_QUE_FALTAN.md`— **no están en la base**: lo que hay es la tabla `messages`, colgada de una búsqueda de cuidado y no del vínculo entre dos personas | Desde el perfil de un Asistente publicado, una Familia con sesión pueda abrir una conversación con esa persona y el Asistente la reciba. Va detrás del punto 6: primero las tablas y el chat reescrito, después el botón. **Con qué condiciones una Familia puede escribirle a alguien que no contestó ningún aviso lo decide el Desarrollador**: es regla comercial, y `docs/ALCANCE.md` §4 la tiene congelada |
| 48 | **Diez textos no llegan al mínimo legible, y los tres motivos son decisiones de diseño, no del código.** Medido el 25 de agosto de 2026 sobre los 763 textos de las dieciséis pantallas, contra el fondo que de verdad tienen debajo, de día y de noche. **(a)** Letra blanca sobre `--azul-medio` llega a 3.21:1 y hace falta 4.5:1, en cinco lugares: el redondel del paso activo del formulario (`formulario-integral.html` y `registrar-asistente.html`), la banda con el nombre adentro del teléfono dibujado (`index.html` y `registrar-asistente.html`) y el botón de ingresar de `mockup-app.html` (`css/mockup-app.css:58`). Ese último de noche se lee, pero de casualidad: sus letras salen de `--superficie`, que de noche se vuelve gris oscuro, así que el botón azul termina con letra oscura sin que nadie lo haya decidido. El arreglo **cambia cómo se ve**: o el fondo pasa a `--azul-oscuro` —y entonces el paso activo se confunde con el paso ya hecho, que ya usa ese color— o la letra pasa a oscura. **(b)** Los colores que elige una Prestadora no tienen versión de noche: los de la Prestadora de ejemplo salen de `primary_color` y `accent_color` de su fila en `tenants`, `js/apiClient.js` los escribe encima de los tokens al abrir sesión, y de noche el enlace «Registrarse como Asistente» de `pwa-asistente` queda en 1.43:1 —azul marino sobre gris oscuro—. De día el mismo par de colores deja seis textos entre 4.13:1 y 4.2:1. El problema de fondo no es esta Prestadora: es que **nada valida el color que elige una Prestadora**, y cuando haya cientos va a haber cientos de colores ilegibles. **(c)** Las estrellas de calificación quedan en 4.4996:1, que es 4.5 raspando por abajo: `--naranja-alerta-texto` promete 4.5:1 sobre fondo claro y se queda a cuatro diezmilésimas. Ése es un token de Careonys, así que el arreglo va allá | Los tres estén decididos: (a) qué se cambia en el redondel y en la banda, (b) qué hace el sistema con un color de Prestadora que no se lee —rechazarlo al cargarlo, o calcularle una versión de noche—, y (c) `--naranja-alerta-texto` una pizca más oscuro en Careonys. Cerrado cuando los 763 textos midan 4.5:1 o más en los dos modos |
| 7 | **El catálogo ya lo leen las pantallas; falta que viva en tablas.** Las 22 listas (133 opciones), los 9 servicios y los 6 cursos salen de `data/catalogo-vocabularios.json` y `data/catalogo-oferta.json` a través de `js/catalogo.js`, y las nueve pantallas portadas los declaran en vez de copiarlos: 35 listas y 5 grillas de tarjetas. Lo que falta es el otro medio: **el destino de los vocabularios son las tablas de Careonys, no unas propias** (`docs/MODULOS.md:36`), y esa fusión todavía no ocurrió. Los cursos sí tienen tablas propias y legítimas (migración 0008), pero `cursos.html` lee del archivo porque la política de `cursos` exige sesión y esa pantalla es pública | El catálogo viva en tablas: los vocabularios en las de Careonys al fusionar, y los cursos acá cuando se decida si `anon` puede leer los de `tenant_id` vacío (ver punto 25). Una pantalla portada contra una tabla vacía no da error: queda vacía y nadie se entera |
| 50 | **Los dos botones de «Ingresar con Google» y «Ingresar con Facebook» no hacen absolutamente nada.** Están en `mockup-app.html:74` y `:76`, debajo del botón de ingresar, separados por una línea que dice «O». No tienen identificador, no tienen ningún guion que los escuche, y en `supabase/config.toml` el único proveedor de acceso externo que aparece es Apple, apagado (`supabase/config.toml:355`): quien los toca no recibe error ni aviso, la pantalla se queda igual y parece que la aplicación se colgó. Son «los cuatro estados» al revés —ni cargando, ni error, ni vacío, ni listo—. Son además los dos únicos colores exentos de `scripts/verificar_paleta.mjs`, porque son las marcas de otras dos empresas. **Ofrecer entrar con la cuenta de otro es una decisión, no una tarea**: obliga a configurar cada proveedor, a decidir qué pasa cuando el correo ya existe con contraseña, y a mirar qué datos entrega cada uno | El Desarrollador decida: se configuran los proveedores de verdad, **o** los dos botones se sacan. Mientras tanto, un botón que no hace nada es peor que no tenerlo |
| 51 | **El único importe del esquema se guarda sin moneda.** `caregivers.hourly_rate` (`supabase/migrations/0001_esquema_inicial.sql:102`) es un `numeric` a secas, y no hay ninguna columna de moneda en ninguna tabla. Se había comprobado el 25 de agosto de 2026 recorriendo las quince migraciones, que entonces eran veintidós tablas; el 26 se volvió a comprobar contra el esquema en vivo, que hoy tiene veinticuatro, y sigue sin haber ni una. **La segunda comprobación vale más que la primera**, porque las migraciones dicen qué se quiso y el esquema dice qué quedó. La regla «todo importe se guarda con su moneda» no lo admite —un número solo, leído un año después, no se sabe cuánto vale— y «multiidioma desde el día uno» pide `es-AR`, `en` y `pt-BR` desde el primer día, así que más de una moneda no es una hipótesis lejana. El chequeo `scripts/verificar_esquema.mjs` lo tiene anotado como exención con su motivo, así que está a la vista y no puede entrar un importe nuevo sin moneda. **Toca una columna que ya tiene datos escritos, así que lo decide el Desarrollador** (la regla de la empresa «se consulta cuando hay que borrar o pisar datos»): son filas inventadas, pero la migración tiene que decir qué moneda se le pone a lo que ya está guardado | `caregivers` tenga columna de moneda, obligatoria y poblada, y la exención se borre del chequeo |
| 52 | **El reporte de cuidado cuelga del aviso, y Careonys ya decidió hace rato que cuelga de la guardia.** Acá `reportes.aviso_id` ata cada anotación al aviso que dio origen al trabajo. En el repositorio de Careonys, `docs/DATA_MODEL.md`, la misma tabla se llama igual, `reportes`, y cuelga de `guardia_id`: la **guardia** es una jornada concreta de trabajo —un Asistente, un Paciente, una fecha, un horario, un check-in y un check-out—, y el reporte diario es de esa jornada. El aviso no aparece en ninguna parte de esa cadena, y es correcto que no aparezca: **el aviso es cómo empezó la relación, no lo que es el trabajo**. Por eso en prestación directa hay reporte igual, sin que nadie haya publicado nada. **Y no es sólo esta tabla.** `reportes` de este repositorio **es** `reportes` de Careonys, desde la 0019 también en el nombre, y `clock_ins` **es** el `checkin_at`/`checkout_at` de `guardias`: son las mismas cosas con otro nombre y otra forma, en dos bases distintas | Se traiga acá la `guardia` de Careonys, que ya existe diseñada y con RLS, y los reportes y los mensajes cuelguen de ella y no del aviso. No se decide nada nuevo: se deja de decidir dos veces lo mismo. Toca datos ya guardados, así que la ordena el Desarrollador. **Desde la migración 0020 esto dejó de ser prolijidad y pasó a costar una pantalla**: la de reportes de la Familia está vacía a propósito, porque un reporte no guarda a qué cuidado corresponde y no hay forma de darle los suyos sin darle los de todos. La cláusula que se la devuelve ya está escrita en la política y empieza a funcionar sola el día que el reporte cuelgue de la guardia |
| 55 | **El renombre de `verificaciones_asistente` en Careonys.** La decisión ya está tomada: el Desarrollador la aceptó el 25 de agosto de 2026 y está escrita entera en `docs/MODULOS.md`, sección «Verificar un papel y incorporar a una persona son dos cosas». En resumen: la de este repositorio se queda como está, porque es el módulo Verificación y porque tiene el vencimiento, que es lo que distingue un papel de una etapa; **la de Careonys pasa a llamarse `etapas_incorporacion`** y guarda **sólo la entrevista**, porque las otras cuatro etapas ya tienen quién las guarde. **Acá no queda nada por hacer.** El renombre corre sobre el repositorio de Careonys y sobre datos ya guardados, y este repositorio sólo lee los otros proyectos | Se ejecute con la fusión, del lado de Careonys. Queda pendiente sólo por eso, no porque falte decidir algo |
| 56 | **Qué suma en el legajo comprobado, y cuánto.** El 25 de agosto de 2026 quedó decidido que las estrellas son sólo de la calificación de las Familias y que el legajo lleva escudo (`docs/DISENO.md`, «Tres cosas que se miden, tres formas distintas de dibujarlas»). Falta el número. **Primero hubo que contar, porque no eran dos:** al lado de `domicilio` y `referencia` —las dos con `puerta: ninguna`— están **`matricula` y `titulo`, que son condicionales**: para un `cuidador_domiciliario` no se exigen (`tipo_asistente.requiere_matricula = false`), así que quien igual las tiene está acreditando algo que nadie le pidió. Y afuera del catálogo de verificaciones el legajo guarda cuatro cosas más: los cursos aprobados **en el sistema** (`intentos_evaluacion.aprobado`), las cinco certificaciones que la persona **declara** (lista `certificacion`), los estudios (`estudios_asistente`, con `nivel_educativo`) y la experiencia (`experiencia_laboral_asistente`, que tiene fechas y por lo tanto años). Son ocho fuentes, no dos. **Propuesta de la línea de comandos:** (1) **el puntaje cuenta sólo lo comprobado**; lo que la persona declara y nadie miró se muestra en el perfil pero no suma, porque si sumara, el escudo mediría cuánto escribió alguien y no cuánto se le comprobó; (2) **las obligatorias no entran en el puntaje**, porque son la puerta: quien está en la modalidad ya las pasó, y contarlas le daría puntos a todos por lo mismo; (3) queda una lista de cinco, **igual para todos**: domicilio comprobado, referencia comprobada, matrícula comprobada aunque no se la exijan, título comprobado aunque no se lo exijan, y al menos un curso aprobado en el sistema; (4) **todas arrancan valiendo lo mismo, y cada Prestadora lo cambia desde su panel**: el argumento de los pesos iguales —que nadie puede defender por qué el domicilio vale menos que la referencia— valía para *nuestro* criterio, y el Desarrollador corrigió que el criterio que hay que defender es el de cada Prestadora, que puede no ser el nuestro; **y puede incluso no querer calificar**, así que el puntaje entero se apaga con una llave. Aplicado el 25 de agosto de 2026 en `supabase/migrations/0018_cada_prestadora_pondera_su_puntaje.sql`: `puntaje_prestadora.califica` es la llave y `ponderacion_comprobacion.ponderacion` es cuánto suma cada una. **La columna se llamaba `peso` y el Desarrollador la leyó como dinero**, así que la migración `0022_la_ponderacion_no_es_plata_y_suma_cien.sql` la renombró y, con el mismo sí, agregó la regla que él puso: **las ponderaciones de una Prestadora suman 100 y la base no acepta otra cosa**. Arrancan en 20 las cinco, que es 100 repartido en partes iguales. Subirle a una obliga a bajarle a otra, y agregar una comprobación sexta deja de poder hacerse a escondidas: el total queda en 120 y no entra hasta que alguien reacomode; (5) **se muestra como fracción y con la lista al lado** —«4 de 5 comprobaciones»— porque un número suelto invita a comparar personas y la lista invita a decidir. **La fracción ya está muerta, no en riesgo:** sólo se lee bien si las cinco valen lo mismo, y desde la 0022 valen lo que cada Prestadora diga. El número es un porcentaje, que es además lo que la suma a 100 vuelve natural —domicilio y referencia comprobadas, 40 %—. La lista al lado se queda | Queda una decisión y una pantalla. **(a)** Si «al menos un curso aprobado» es una sola comprobación o una por curso —la línea de comandos recomienda una sola, para que el puntaje no se vuelva una carrera de cursos—. **(b) se disolvió el 25 de agosto de 2026:** preguntaba qué ve una Familia en un directorio donde conviven Prestadoras que ponderan distinto, y ese directorio dejó de existir —cada Prestadora tiene el suyo y no se mezcla nada, migración 0021—, así que los dos Asistentes que se comparaban lado a lado nunca están lado a lado. **(c)** Falta la pantalla para tocar las ponderaciones: la tabla existe y la regla del 100 también, el formulario no. El Desarrollador agregó que **forma parte del legajo de cada Asistente**, y eso hay que entenderlo antes de dibujarlo: la ponderación es una por Prestadora y no una por persona, así que lo que cuelga del legajo es el resultado —qué se le comprobó y cuánto suma— y no el formulario que reparte los 100. **Y esa pantalla es donde va el aviso legal**, agregado el 2026-08-26: encender `puntaje_prestadora.califica` es encender una función de las que traen riesgo —puntuar y ordenar personas—, y la regla de los productos Careonys pide avisarle a quien la enciende, que acá es la Prestadora. Sin pantalla no hay aviso, y sin aviso la llave no se enciende en ninguna Prestadora real |
| ~~58~~ | **Cerrado el 25 de agosto de 2026: la puerta existe.** El Desarrollador había escrito que cada Prestadora «sólo puede verse y habilitarse desde el panel de control de CeltaTech, que es al fin y al cabo el dueño del software y con quien cada una de las prestadoras contrata su uso», y de este lado no había por dónde. La línea de comandos abrió el pendiente diciendo además que ese panel no existía, y **eso era falso**: existe, está publicado y tiene su base. El pendiente pedía siete cosas y terminó siendo dos, porque el Desarrollador puso el límite entre las dos mitades —*«una cosa es el producto, y otra es su manejo comercial, no mezclemos o hacemos líos»*— y cinco de las siete eran comerciales: se le anotaron a CeltaTech en `../../docs/SUGERENCIAS_DESDE_EL_MARKETPLACE.md` y no se construyeron. **Lo construido son las migraciones 0023 y 0024 y la función de borde `alta-y-baja`**, que verifica la firma de cada pedido y sabe hacer dos cosas: dar de alta una Prestadora y dejarla activa, suspendida o cancelada. Probada contra el servidor desplegado con doce comprobaciones (`scripts/probar_alta_y_baja.mjs`), y la primera corrida falló siete de las doce por un error real de la 0023, que es lo que arregló la 0024. De regalo quedó cumplida la regla que el modelo comercial declara no negociable: un producto que no le pregunta nada a CeltaTech no se cae cuando CeltaTech se cae | **Hecho.** Lo único que falta es del otro lado: que el panel de CeltaTech firme con la misma clave, que está en la caja fuerte local de este repositorio |
| 59 | **La puerta reconoce a un cliente por su nombre, y por eso puede juntar dos en uno.** `alta_de_prestadora` (migración 0024) deduce el nombre corto de la razón social y, si ya hay una fila con ese nombre corto, da por hecho que es el mismo cliente y devuelve la que estaba. Eso rompe de las dos maneras: **dos empresas distintas que se llamen igual terminan siendo una sola Organización y compartiendo datos** —que es exactamente lo que prohíbe la regla de la empresa «aislamiento entre Organizaciones»— y **un cliente al que CeltaTech le corrija el nombre entra como uno nuevo**. El identificador no puede salir del nombre porque el nombre no es único y además cambia. **Y CeltaTech ya manda uno**: `suscripcion_id`, en el cuerpo del alta de `../../docs/MODELO_COMERCIAL_CELTATECH.md` §6.1, que la puerta hoy recibe y descarta. De paso cae el argumento con el que se justificó no llevar la lista de mensajes atendidos que pide §6.2: esa lista se evitó apoyándose en el nombre. **Encimado hay un segundo problema, de vocabulario:** las dos funciones y sus mensajes de error dicen «Prestadora», y el 25 de agosto de 2026 el Desarrollador ya había dicho que a estos clientes no se los puede llamar así porque pueden no prestar ningún cuidado (`../../docs/GLOSARIO_PRODUCTOS_CAREONYS.md:18` lo confirma: Prestadora es el cliente que contrata Careonys, no cualquier cliente). Se escribió igual, y quedó guardado en la base, que es lo que «lo que se guarda para siempre se nombra por lo que hace» manda no hacer | El Desarrollador resuelva las dos, que van juntas en una misma migración porque las dos tocan las mismas funciones. **(a) Por dónde se reconoce a un cliente:** la línea de comandos recomienda guardar el `suscripcion_id` que manda CeltaTech, como texto opaco que el producto nunca interpreta —espejo exacto de lo que CeltaTech hace con el `tenant_ref`—, y deduplicar por ahí en vez de por el nombre. **(b) Cómo se llaman las funciones**, que es el §5 de `../../docs/SUGERENCIAS_DESDE_EL_MARKETPLACE.md` todavía abierto: no se propone ninguna palabra nueva; la recomendación es usar la que ya está aprobada y ya nombra esto mismo, **Organización**, que la regla de la empresa «las cuatro entidades que no se mezclan nunca» define como el espacio de datos aislado adentro de un producto y que `../../docs/GLOSARIO_PRODUCTOS_CAREONYS.md:21` separa a propósito de «Prestadora». **Hasta que se arregle, ninguna alta real debe pasar por la puerta**: hoy no pasó ninguna, y la única que hubo la creó y la borró la prueba |
| 60 | **El Tipo de Asistente está escrito en masculino y ahora choca con el género al lado.** Desde el 26 de agosto de 2026 la tarjeta del directorio dice «Enfermero universitario · Femenino · Zona Norte», y las dos palabras se contradicen a la vista. El desajuste ya existía —las etiquetas de `tipo_asistente` en `data/catalogo-vocabularios.json` están todas en masculino—, pero recién se ve ahora que el género aparece pegado. Las salidas son tres y **ninguna la elige la línea de comandos**, porque son decisiones de redacción: dejar la etiqueta neutra («Enfermería universitaria»), guardar dos formas por clave y elegir según el género, o separar las dos cosas en la tarjeta para que no se lean como una frase | El Desarrollador elija una de las tres y el catálogo quede escrito según esa decisión |
| 63 | **El precio y la fecha se escriben siempre a la argentina, aunque la pantalla esté en inglés.** Las tres funciones que les dan forma en `js/texto.js` tienen el idioma escrito adentro: `fechaCorta()` (`js/texto.js:95`), `horaCorta()` (`js/texto.js:112`) e `importe()` (`js/texto.js:129`) pedían todas `Intl` con `'es-AR'`, e `importe()` además anteponía `$` a mano. Se vio el 26 de agosto de 2026 al comprobar `directorio.html` en los tres idiomas: la tarjeta en inglés dice **«$4.500 per hour»**, que en inglés se lee «cuatro dólares con medio», mil veces menos de lo que vale; y «12/08/2026» es el 12 de agosto acá, el 12 de agosto en el Reino Unido y el 8 de diciembre en los Estados Unidos, sin que nada avise cuál. No es cosmético: es un importe y una fecha dichos mal. **Se arregla con el idioma que ya resuelve `Catalogo.idioma`** —está a mano, es el mismo punto único de verdad— y la moneda tiene que salir del dato y no del signo escrito a mano, que es justo lo que pide el pendiente 51. Los 16 chequeos no lo ven, porque para ellos `Intl.NumberFormat('es-AR')` es código y no texto visible. **El 26 de agosto de 2026 se hizo la mitad del idioma.** Las tres funciones piden ahora el idioma a `Catalogo` (`js/texto.js:68`), `importe()` dejó de anteponer `$` a mano y recibe la moneda por parámetro, y el chequeo de frases tiene una regla 6 que se queja de cualquier `Intl` con el idioma escrito adentro (`scripts/verificar_frases.mjs:116`), probada rompiendo el archivo a propósito. Comprobado en el navegador, en las tres: 4500 sale «$ 4.500», «ARS 4,500» y «ARS 4.500»; el 26/08/2026 sale «26/08/2026», «08/26/2026» y «26/08/2026». **La mitad de la moneda sigue abierta y espera al pendiente 51**: mientras `caregivers.hourly_rate` sea un número a secas, la moneda sale de `MONEDA_POR_OMISION` (`js/texto.js:85`), que es un solo lugar pero es igual una moneda supuesta. **Y probándolo apareció algo más:** el arreglo vale al abrir la pantalla, no al cambiar de idioma con la pantalla abierta. `Catalogo.traducir()` sólo toca los elementos que llevan `data-frase` (`js/catalogo.js:631`), y un precio dibujado con `textContent` no lleva ninguno. Comprobado el 26 de agosto de 2026 en `directorio.html`: pasando de castellano a inglés en vivo, «$ 4.500 por hora» queda en «$ 4.500 per hour» —la unidad se tradujo, el número no—, que es justo el «$» que se lee como dólares. Pasa igual con el precio de `perfil.html:323` y con las horas y fechas de `mockup-app.html:533`, `pwa-familia/index.html:923` y `pwa-asistente/index.html:686`. Hoy no lo sufre nadie porque todavía no hay selector de idioma a la vista, y ponerlo es la decisión de diseño que sigue esperando; pero el día que lo haya, hace falta que las pantallas que dibujan datos se vuelvan a dibujar cuando el idioma cambia, y eso toca `js/catalogo.js` en sus tres copias más `directorio.html`, `perfil.html`, `mockup-app.html`, `pwa-familia/index.html` y `pwa-asistente/index.html`. Es un cambio de mecanismo repartido, así que va con plan antes de código | Tres cosas, y las tres son distintas. El idioma: **hecho**. El chequeo que lo cuida: **hecho**. Falta que la moneda salga del dato —depende del pendiente 51— y que cambiar de idioma con la pantalla abierta vuelva a escribir los números y las fechas ya dibujados, que necesita plan aprobado antes de tocar código |
| 9 | **El multiidioma ya funciona; falta mudar 39 de las 45 pantallas.** La regla «multiidioma desde el día uno» pide `es-AR`, `en` y `pt-BR` desde el primer día, y el 26 de agosto de 2026 se construyó el mecanismo entero: `data/catalogo-frases.json` con **155 frases en los tres idiomas**, los atributos `data-frase`, `data-frase-<atributo>` y `data-huecos` leídos por `js/catalogo.js`, `Catalogo.traducir()` para lo que arma el código, el idioma resuelto por `?idioma=`, por lo guardado o por el navegador, y `scripts/verificar_frases.mjs` con cinco reglas corriendo antes de cada `commit`. Convertidas: los mensajes de error (`js/texto.js`, que ahora clasifica con `claveDeError()` y deja la frase al catálogo), el campo de contraseña (`js/clave.js`, cuyo `revisar()` también devuelve la clave del aviso y no la frase) y el camino entero de la contraseña —`acceso.html`, `recuperar-clave.html` y `nueva-clave.html`—, para que quien no puede entrar pida el enlace y elija una contraseña nueva sin cambiar de idioma en el medio; y `examen.html` entera, que fue la primera con singular y plural y dejó la regla para las que faltan —**dos frases distintas y no una armada con pedazos**—; y `directorio.html` entera el 26 de agosto de 2026, que además convirtió **la barra de arriba y el pie**, los mismos que repiten otras siete pantallas: las claves `nav.*` y `pie.*` que salieron de ahí no se vuelven a escribir, y con ellas se lleva convertida la frase que más se repite en todo el proyecto. La cuenta de resultados (`js/main.js`) escribe ahora la clave y no el texto, así que cambia sola al cambiar de idioma. Aparecieron además dos cosas que no se veían: `scripts/texto_visible.mjs` leía `alt` adentro de `data-frase-alt` y daba por texto a mano la clave del catálogo —arreglado con el mismo `(?<![-\w])` que ya usaba `despejar()`—, y el botón del banner final se lee en mayúsculas, que es una decisión de diseño y pasó al CSS para que la frase viva una sola vez. Y `perfil.html` entera el mismo día, que es la pantalla que se abre al tocar «Ver perfil»: dejarla en castellano rompía el idioma en el clic siguiente, que es la pantalla convertida a medias de `docs/PLAN_MULTIIDIOMA.md` §6 estirada entre dos archivos. Trece claves nuevas y nada más: la barra, el pie, la insignia, los reemplazos urgentes, el precio por hora y «Reintentar» son los mismos de la grilla y se piden con las claves que ya existían. El título de la pestaña deja de ser una frase apenas hay un nombre real —se le saca el `data-frase`—, porque si no el catálogo lo pisaba de vuelta al cambiar de idioma. Apareció además que `&gt;`, el signo que separa las migas, se informaba como texto escrito a mano: una entidad de HTML es un signo y no una palabra, y el chequeo de frases ahora las saca antes de mirar. **Y el inventario estaba midiendo mal desde que existe la primera pantalla convertida**: `scripts/inventario_textos.mjs` no sabía nada de `data-frase`, así que el castellano que queda adentro de un elemento ya convertido —el que se ve mientras el catálogo viaja— se seguía contando como trabajo por hacer, y las seis pantallas listas figuraban enteras. Se arregló despejándolas con la misma función que ya usaba el chequeo, que para eso pasó a `scripts/texto_visible.mjs`, el módulo que existe justo para lo que miran dos guiones. **Lo que falta no eran 733 frases distintas sino 665**, y las seis pantallas convertidas bajaron de 168 apariciones a 3, que son tres cadenas del guion que el inventario confunde con texto —dos claves del catálogo y el nombre de un vocabulario—. **El chequeo dice cuánto falta**: «6 de 45 archivos ya convertidos». Las fechas, en cambio, ya estaban en un solo lugar: `Texto.fechaCorta()` (`js/texto.js:60`) es el único lugar del proyecto donde se le da forma a una fecha. **Sale de este grupo el 26 de agosto de 2026**: la decisión 3 del plan se tomó como se recomendaba —esto se hace **antes** de React, porque cada pantalla nueva lo encarece y la migración va a agregar pantallas. **Y ahí se frenó, en 6 de 45.** Las diez pantallas que quedan con texto están todas trabadas por algo que decide el Desarrollador, y ninguna se puede convertir a medias: la regla 4a de `scripts/verificar_frases.mjs` pide que en una pantalla convertida no quede texto escrito a mano. `cursos.html`, `index.html`, `soporte-remoto.html`, `formulario-integral.html` y `solicitar-asistente.html` están en el pendiente 64 —contestan que mandaron algo que no mandaron—; `pwa-familia/index.html` y `mockup-app.html`, en el 65 —afirman un estado que no consultaron—; `pwa-asistente/index.html:542` y `registrar-asistente.html:522` piden aceptar unos Términos y Condiciones que sólo existen en castellano y que el pendiente 49 dice que ni siquiera habría que estar mostrando; y `panel-prestadora.html:133` trae un aviso de responsabilidad legal, que la regla del riesgo legal no deja escribir en otros dos idiomas sin el documento de cada país. Traducir cualquiera de esos textos lo multiplica por tres, así que se paró y se anotó | Las 45 pantallas lean del catálogo y el chequeo diga «45 de 45». **Falta además el selector visible de idioma**, que no se puso porque dónde va en el encabezado de catorce pantallas es una decisión de diseño y ésas se consultan; y **quién traduce las 665 frases que faltan**, que es la única decisión abierta del plan (`docs/PLAN_MULTIIDIOMA.md`, sección 5, punto 2). Las cinco palabras del negocio que la línea de comandos tradujo sola —Prestadora, Asistente, legajo, Aviso y Acompañamiento— están en la sección 5.1 de ese archivo para que el Desarrollador las confirme |
| 64 | **Cinco formularios públicos contestan que mandaron algo, y no mandan nada.** Salió al ir a convertir `cursos.html` al multiidioma: un cartel falso traducido a tres idiomas cuesta tres veces más sacarlo. El que engancha es `js/main.js:46`, que escucha a `#contact-form` y a `#form-solicitud-familia` en cualquier pantalla donde estén, y cuando el primer desplegable del formulario no vale `busco_asistente` prende el cartel verde y vacía los campos (`js/main.js:80`) sin llamar a nadie. **(a)** `cursos.html:143` tiene de primer desplegable `#curso`, que nunca puede valer `busco_asistente`: la inscripción a un curso contesta **siempre** «✓ ¡Solicitud enviada! Recibirá información del curso en su email.» (`cursos.html:156`) y no manda nada a ninguna parte. **(b)** `index.html:281` y **(c)** `soporte-remoto.html:153` hacen lo mismo con cualquier motivo distinto de «Busco un Asistente»: con ése van al formulario integral, que es real; con los otros contestan «✓ ¡Gracias!…» y la consulta se pierde. Ninguna de las dos pantallas carga el cliente de datos, así que ahí no hay a dónde mandarla. **(d)** `formulario-integral.html:291`, `#form-solicitud-familia`, cae siempre en la rama del enlace —`js/main.js:72` la da por verdadera por el nombre del formulario— y **la manda a sí misma**: se pierde lo escrito y el cartel de éxito de `formulario-integral.html:316` no se ve nunca. **(e)** `formulario-integral.html:337`, `#form-registro-asistente`, no lo escucha nadie: el navegador lo manda por omisión y la página se recarga igual. No viaja ningún dato personal en la dirección —los campos no tienen `name`—, pero lo escrito se pierde y tampoco llega a ninguna parte. **Lo que sí funciona, comprobado el 26 de agosto de 2026**: el paso a paso de `formulario-integral.html:97` publica el aviso de verdad (`js/main.js:284`), y `solicitar-asistente.html:363` y `registrar-asistente.html` guardan los dos con guion propio. Pero a `solicitar-asistente.html` **`js/main.js` le sigue enganchado a la misma etiqueta**, así que con el motivo «Busco un Asistente» navega a `formulario-integral.html` con el guardado por la mitad, y con cualquier otro motivo prende el cartel de éxito **antes** de que el guardado termine: el cartel aparece igual aunque después falle y salte el aviso de error de `solicitar-asistente.html:404`. Es la misma falsedad que ya se sacó de `perfil.html`, donde quedó escrito el motivo (`perfil.html:142`). **(f)** Y hay un sexto que no abre nadie: `js/main.js:298` arma un panel de videollamada que dice «Conectando sala segura 8x8 Encryption…» y nombra a una Asistente inventada, sin conectar nada con nadie. Lo que tendría que dispararlo —`a[href*="video"], .btn-video-call`— no casa con ningún elemento de ninguna pantalla, comprobado el 26 de agosto de 2026. Son doce de las 665 frases que faltan traducir, para algo que no se puede abrir, y una afirmación de seguridad que no se cumple **Medido uno por uno el 26 de agosto de 2026, y lo que apareció es peor que «no manda».** Tres de los cinco están rotos de formas distintas, y ninguna es la que decía este renglón. **El de `formulario-integral.html:291` navega a sí mismo:** `js/main.js:72` lo da por «busca un Asistente» por el nombre del formulario, sin mirar qué eligió la persona, y manda a `formulario-integral.html`, que es la pantalla donde ya está. Lo escrito se pierde y el cartel de éxito de `:316` no se ve nunca. **El de `formulario-integral.html:337` no lo escucha nadie:** `#form-registro-asistente` aparece una sola vez en todo el proyecto, no tiene `action`, y sus campos no tienen `name`, así que el navegador lo manda por omisión, recarga la página y no viaja ni un dato. Encima es la copia de un camino que sí funciona, `registrar-asistente.html`, que crea la cuenta y el legajo. **Y el de `solicitar-asistente.html:296` sí guardaba, pero mentía igual:** tenía dos escuchas encima —la de `js/main.js` y la propia del final de la pantalla—, y la de `main.js` prendía el cartel verde antes de que el guardado terminara, así que decía «enviado» aunque el guardado fallara. **Ese está arreglado**: el formulario lleva ahora `data-guion-propio` (`solicitar-asistente.html:296`) y `js/main.js:46` no engancha los que lo traen. **Y hay dos cosas que ninguna decisión puede saltearse.** Una: **hoy no existe ninguna puerta para guardar algo sin sesión.** Ninguna tabla le concede escritura a quien no inició sesión, y `avisos` —la única que hoy recibe una consulta— tiene su política `for all to authenticated` (`supabase/migrations/0020_la_barrera_tambien_va_entre_familias.sql:77`). Si la respuesta es «se guarda», hace falta abrir una puerta, y hay dos formas: una tabla nueva con política de alta para quien no inició sesión, o una función de borde como la que ya existe en `supabase/functions/alta-y-baja/index.ts`. Dos: **los cinco preguntan si la persona quiere recibir novedades y ninguna tabla lo recibe** (`index.html:276`, `cursos.html:151`, `soporte-remoto.html:148`, `formulario-integral.html:311` y `solicitar-asistente.html:314`). Eso es un consentimiento, y un consentimiento guardado a medias es peor que no preguntarlo. | El Desarrollador decida qué tiene que hacer cada formulario —a dónde va una consulta que no es «busco un Asistente», qué pasa con una inscripción a un curso, y qué hacen los dos formularios sueltos del formulario integral—, y ninguna pantalla conteste que mandó algo que no mandó. Aparte de eso, que una pantalla con guion propio deje de tener además el de `js/main.js` encima, y que el panel de videollamada se construya o se borre: mientras no lo abra nadie, no se traduce. **Mientras no esté decidido no se convierte al multiidioma ninguna pantalla que traiga uno de esos carteles** —`cursos.html`, `index.html`, `soporte-remoto.html`, `formulario-integral.html` y `solicitar-asistente.html`—: traducirlo lo multiplica por tres |
| 65 | **Dos pantallas muestran el estado de una persona sin haberlo consultado, y una tercera cosa se pregunta y se tira.** Salió al elegir la próxima pantalla del multiidioma. **(a)** `pwa-familia/index.html:594` y `mockup-app.html:142` traen escrito «Complete las Verificaciones — Email: ✓ Validado | Celular: ⌛ Pendiente». Ahí no escribe ningún guion: comprobado el 26 de agosto de 2026, `verification-widget` sólo aparece en el CSS de esas dos pantallas y en su propio HTML. Quien entra lee que su correo está validado y su celular no, diga lo que diga la base. **(b)** `pwa-familia/index.html:686` y `mockup-app.html:350` traen una ficha de paciente entera escrita a mano —«Abuela Rosa», «Diagnóstico: Alzheimer Moderado»— que tampoco escribe nadie. En `mockup-app.html` el tercer renglón dice «Asistente: María Gómez» y en `pwa-familia` dice «Familiar a cargo: Abuela Rosa»: al copiar se cambió el rótulo y no el valor, así que la paciente figura como su propio familiar a cargo. Y justo debajo, la lista de reportes sí sale de la base y explica bien por qué está vacía (`pwa-familia/index.html:915`): la pantalla muestra un diagnóstico inventado arriba y la verdad abajo. **Esto desmiente lo que daba por comprobado el pendiente 14**, que ya quedó corregido. **(c)** Y `pwa-familia/index.html:735` pregunta la edad del paciente en `pb-edad`, que no lee nadie: el guion que publica el aviso (`pwa-familia/index.html:1017`) manda nombre, zona, modalidad, franjas, patologías y descripción, y la edad se pierde al enviar. `pb-edad` aparece una sola vez en todo el proyecto | El Desarrollador decida qué muestra cada uno de esos dos recuadros —el estado real de las verificaciones, el Paciente real de esa cuenta, o ninguno de los dos— y qué se hace con la edad: que viaje al aviso o que no se pregunte. Ninguna pantalla afirme un estado que no consultó. **Mientras tanto no se convierten al multiidioma `pwa-familia/index.html` ni `mockup-app.html`**: la regla 4a de `scripts/verificar_frases.mjs` no deja convertir media pantalla, así que traducir el resto obliga a traducir también lo inventado |
| 70 | **El directorio promete decir qué se le comprobó a cada persona, y no hay pantalla que lo cargue.** La vista `directorio` arma la lista `comprobaciones` con las filas de `verificaciones_asistente` que están en estado `verificado` (`supabase/migrations/0026_el_directorio_dice_que_se_comprobo.sql:75`), y el consentimiento que firma cada Asistente promete esa lista. **Comprobado el 26 de agosto de 2026 recorriendo el proyecto entero con `scripts/recorrido.mjs`:** la tabla no la escribe ni una pantalla ni un guion. La palabra `verificaciones_asistente` aparece en ocho archivos y ninguno es `.html` ni `.js`: está en las migraciones que la crean y la protegen, en la documentación y en `data/catalogo-verificaciones.json`. Los únicos renglones que insertan en ella están en `supabase/migrations/0027_comprobaciones_en_los_legajos_ficticios.sql:35`, que es la siembra de la Organización de pruebas. En el panel hay una sola mención, `panel-prestadora.html:291` —«Documentos adjuntos para verificar»—, y lo único que el botón de auditar escribe es el veredicto entero del legajo, con `cambiarEstadoAspirante` (`js/apiClient.js:226`). **La consecuencia se dice en una línea:** en cualquier Organización real, por prolija que sea la Prestadora revisando papeles, la tarjeta del directorio va a decir siempre que no se le comprobó nada —salvo `curso_aprobado`, que sale de un examen que corrige la base y no de un papel que alguien entregó—. **Y explica por qué la primera prueba de la migración 0026 no probaba nada** y hubo que sembrar a mano: no había con qué cargar una comprobación. **Está construida la mitad de abajo y falta la de arriba**: la tabla, sus columnas de rastro —`verificado_por` y `verificado_el`—, sus políticas y la vista que las publica existen desde las migraciones 0004, 0005 y 0026; lo que no existe es dónde apretar. Esas dos columnas de rastro tampoco las escribe nadie, por el mismo motivo, y ahí se cruza con el pendiente 68. **Y quien la construya se va a encontrar con que la maqueta y lo decidido no coinciden:** el CSS dibuja cuatro `badge-verified` (`css/styles.css:1771` a `:1786`) y una de las cuatro —`urgent`— ni siquiera es una verificación sino la disponibilidad urgente, mientras que el catálogo que el Desarrollador decidió el 24 de agosto de 2026 tiene siete (`data/catalogo-verificaciones.json`). La pantalla se hace contra el catálogo, no contra la maqueta | Exista la pantalla donde el personal de la Prestadora marca cada verificación del catálogo como presentada, comprobada, rechazada o vencida, y que al marcarla escriba quién y cuándo. Probado con las dos Organizaciones ficticias: que lo marcado en una salga en su tarjeta del directorio y no en la de la otra. **La prueba tiene que poder fallar**: se hace sobre una persona que arranque sin ninguna comprobación cargada, porque sobre una ya sembrada la pantalla rota y la sana contestan lo mismo |
| 72 | **La regla de los cuatro estados no la comprueba ningún chequeo, y de veintidós puntos de carga la cumplen seis.** «Todo componente que carga datos maneja cuatro estados: cargando, error, vacío, listo» es regla de la empresa, y de los diecisiete chequeos el único que la roza es `scripts/verificar_arranque.mjs`, que mira una sola cosa: que un arranque asincrónico tenga quien atrape el fallo. **Su propia cabecera avisa de lo que no hace** —un `catch` vacío le pasa igual, y no sigue las llamadas—, así que hoy informa diez arranques todos aprobados mientras deja afuera los dos peores: `registrar-asistente.html:674` es `inicializarLegajo();` a secas, que encadena cinco cargas sin un solo `try`, y `pwa-asistente/index.html:646` llama a `montarDisponibilidad()` sin `await` y sin `catch`, y la función tampoco lo trae. De cargando, vacío y listo no se ocupa nadie. **Medido con `scripts/recorrido.mjs`:** 39 puntos de carga distintos en 19 archivos —54 apariciones contando las copias byte a byte de las dos aplicaciones, que `verificar_copias` mantiene iguales—, de los cuales 24 traen datos para mostrar en pantalla. **Los cuatro estados, con texto del catálogo, los cumplen seis:** `directorio.html:334`, `perfil.html:358`, `examen.html:209`, `js/catalogo.js:600`, `js/catalogo.js:727` y `pwa-asistente/index.html:769`. **Siete no manejan más que el estado listo:** `js/autorizaciones.js:125`, `js/fichas-legajo.js:17`, `registrar-asistente.html:674`, `pwa-asistente/index.html:984`, `mockup-app.html:804`, `js/catalogo.js:697` y `js/apiClient.js:745`. **Y cinco los manejan con castellano escrito a mano**, o sea que además no existen en `en` ni en `pt-BR`: `js/disponibilidad.js:106`, `panel-prestadora.html:171`, `panel-prestadora.html:387`, `pwa-asistente/index.html:813` y `pwa-familia/index.html:910`. **Dos lugares donde el comentario afirma lo que el código no hace:** `js/autorizaciones.js:62` dice «Los cuatro estados que este paso puede tener, dichos una sola vez» y el objeto que sigue tiene uno solo, `error`; y `mockup-app.html:813` es un `catch` con el comentario «sin mensajes previos, ok», que confunde «no hay mensajes» con «no se pudieron traer». **Y una prueba que no puede fallar:** `js/apiClient.js:93` se traga el fallo de resolver la Prestadora, así que `initTenant()` no rechaza nunca y todo `try` que las pantallas ponen alrededor suyo es un control que no se dispara. **Del texto crudo de la base a la pantalla, cero**, y conviene decirlo para no asustar de más: no hay ninguna aparición de `err.message` ni de `details`, `hint` o `code` llegando a un `alert` o a la pantalla. El portador está y es uno: `js/apiClient.js:577` arma el error con el cuerpo crudo de PostgREST adentro, que nombra tablas, columnas y restricciones; hoy no sale porque `js/texto.js:173` lo clasifica en una clave del catálogo antes de mostrarlo. Pero los cinco puntos que escriben su propio texto no pasan por ahí | Exista `scripts/verificar_estados.mjs` y lo levante `scripts/verificar_todo.mjs`. **La prueba de que sirve es que se corra antes de arreglar nada y encuentre los doce casos medidos**; si arranca en verde, no probó nada. Después: los doce corregidos, cada texto de estado saliendo del catálogo en los tres idiomas, y el chequeo en verde |
| 73 | **Dos vocabularios del catálogo no los usa ninguna pantalla.** `verificacion` y `discapacidad` están definidos en `data/catalogo-vocabularios.json`, con sus opciones en los tres idiomas, y no los pide nadie: ni por atributo del marcado, ni desde una ficha del legajo, ni desde el guion. Eso no es un defecto —por eso `scripts/verificar_usos.mjs` los informa y no rompe la construcción—, es una pregunta sin contestar: o falta la pantalla que los iba a usar, o sobran ellos. **La otra mitad de este pendiente ya está hecha:** la lista `usado_en` se calcula desde el código y hay un chequeo que rompe la construcción cuando el catálogo dice una cosa y el marcado hace otra. **Y de paso se corrigió una cuenta de este mismo renglón:** decía que eran cinco los vocabularios sin usar, y son dos. Los otros tres —`tarea_hogar`, `tarea_acompanamiento` y `puesto_experiencia`— sí se usan, desde `data/catalogo-fichas.json`, que es por donde las fichas del legajo declaran sus campos; la primera medición no miraba ahí | El Desarrollador decida, para cada uno de los dos: se borra del catálogo, o se anota qué pantalla lo va a pedir y queda como pendiente aparte |
| 74 | **Un coordinador puede mudar el legajo de otra persona a su propia cuenta.** La política del personal de la Prestadora (`supabase/migrations/0005_acceso_por_sesion.sql:169` a `:172`) es `for all` y su `with check` exige dos cosas: que la Prestadora sea la suya y que quien pide sea personal. **No nombra `user_id`**, así que nada impide cambiar de quién es un legajo, siempre que quede adentro de la misma Prestadora. Quien lo haga se queda con el legajo entero de otra persona: sus matrículas, sus estudios, sus papeles. Es la misma familia de agujero que el pendiente 66 —una columna que la política no mira— y conviene cerrarlo con la misma herramienta, en la misma migración | Cambiar el dueño de un legajo esté prohibido para todo el mundo salvo quien tenga un permiso pensado para eso. Probado con dos cuentas ficticias de la misma Prestadora: la segunda intenta quedarse con el legajo de la primera y recibe 403 |
| 75 | **Los papeles del legajo se pueden cambiar después de que la Prestadora lo validó, y el sello no se cae.** La columna `documents` de `caregivers` la escribe la propia persona por la política «Su propio legajo» (`supabase/migrations/0005_acceso_por_sesion.sql:177` a `:180`), y `verification_status` no depende de ella: se puede presentar un papel, esperar el sello, y después reemplazarlo por otro. **El sello sigue diciendo que está validado, sobre un papel que nadie miró.** Es lo mismo que el pendiente 66 visto al revés: aquel deja ponerse el sello solo, éste deja cambiar lo que el sello aprobó | Cambiar un papel de un legajo ya validado baje el sello, o quede prohibido, o quede asentado — las tres son defendibles y hay que elegir una. Probado con una cuenta ficticia validada que cambia su `documents` y comprueba qué pasó con el sello |
| 76 | **La fecha de alta de un legajo la escribe la propia persona.** `created_at` de `caregivers` no la pone la base: entra en el alta, por la misma política «Su propio legajo» que deja escribir el resto, y se puede poner cualquier valor, pasado o futuro. **Hoy no se ve en ninguna pantalla** —`js/apiClient.js:633` la traduce a `fechaRegistro` y ese nombre no aparece en ningún otro archivo del proyecto—, así que no hay consecuencia visible todavía. Se anota igual porque la antigüedad es exactamente la clase de dato que después se usa para ordenar un directorio o para decidir a quién se muestra primero, y ese día el agujero pasa a ser una ventaja que alguien se dio a sí mismo | `created_at` la ponga la base con `default now()` y no se pueda escribir desde afuera. Probado intentando crear un legajo ficticio con una fecha inventada y comprobando qué quedó guardado |

### Reparto de las 22 tablas, aplicado el 25 de agosto de 2026

`docs/MODULOS.md` reparte **módulos**, no tablas, y por eso la línea no se podía recorrer con una
búsqueda. Esto la baja a tabla por tabla. **Era una propuesta y el Desarrollador la aprobó
entera**: las tres de esta modalidad ya llevan el prefijo en el repositorio, a la espera de que
las dos migraciones se peguen en el servidor (pendiente 52).

| Lado | Tablas |
|---|---|
| **De esta modalidad** (llevan el prefijo de la modalidad) | `avisos`, `franjas_aviso`, y la vista `directorio` |
| **Compartidas** (se quedan como están) | `tenants`, `profiles`, `caregivers`, `clock_ins`, `reportes`, `matriculas_asistente`, `estudios_asistente`, `experiencia_laboral_asistente`, `referencias_asistente`, `documentos_asistente`, `verificaciones_asistente`, `autorizaciones_asistente`, `cursos`, `evaluaciones`, `preguntas_evaluacion`, `opciones_pregunta`, `intentos_evaluacion`, `disponibilidad_asistente`, `franjas_asistente` |
| **La única dudosa** | `messages` |

**Por qué `messages` queda aparte.** `docs/MODULOS.md:60` pone «Contacto y su costo» de este lado,
y el chat es cómo una Familia llega a un Asistente. Pero en prestación directa la Prestadora
también le escribe a su personal, y esa tabla ya existe desde la 0001 con ese uso. Las dos
lecturas son defendibles y la pregunta que decide —*¿esto seguiría teniendo sentido en prestación
directa?*— acá contesta que sí. Se deja marcada en vez de resolverse sola.

**La palabra ya está: son dos palabras distintas y nombran dos cosas distintas.** El Desarrollador
lo aclaró el 25 de agosto de 2026. El **Aviso** es lo que la Familia publica y queda guardado. La
**Búsqueda** es la acción de buscar, y la hacen los dos lados de tres maneras: la Familia busca
entre los Asistentes disponibles, la Familia busca también publicando un aviso, y el Asistente
busca cuando revisa los avisos publicados hasta encontrar uno que le cuadre. **De ahí sale el
nombre de la tabla sin discusión**: lo que `avisos` guarda son avisos, no búsquedas, así
que le corresponde `avisos`; y `franjas_aviso`, que guarda los días y turnos de ese
aviso, es `franjas_aviso` —igual que su hermana `franjas_asistente`, que guarda los del
Asistente—. Las dos palabras quedaron propuestas en `docs/GLOSARIO.md` §3 y **esperan aprobación**
antes de tocar una línea, que es lo que manda §3. Nada se renombró todavía.

**Dos tablas compartidas apuntan al aviso, y eso pesa más que las palabras.** `reportes` y
`messages` tienen una columna `aviso_id` que referencia `avisos`
(`supabase/migrations/0001_esquema_inicial.sql:242` y `:247`). Los reportes y el chat existen igual
en prestación directa, pero ahí no hay ningún aviso al que apuntar: esa columna quedaría siempre
vacía. Es exactamente la filtración que `docs/MODULOS.md` quiere evitar, y ninguna búsqueda de
texto la ve, porque `aviso_id` no contiene ninguna de las siete palabras. **También decide la
duda sobre `messages`**: si el chat cuelga de un aviso, el chat es de este lado. Queda para la
misma decisión, y son dos preguntas más: si un reporte tiene que poder colgar de un aviso o si le
alcanza con colgar del Vínculo, y lo mismo para el chat.

**El costo de renombrar `avisos` es menor de lo que suena.** En Postgres es un `alter table
… rename to …`: las políticas, los índices, las claves foráneas y la función de la 0002 siguen
apuntando solas al mismo objeto, y no se mueve un solo dato. Lo que sí hay que cambiar a la par
son los 11 archivos que la nombran, porque el nombre de la tabla es además la dirección web por la
que la aplicación la consulta. No es una migración de datos; es un renglón y un reemplazo
parejo.

**Lo que este reparto arregla y la búsqueda de texto no.** Puesto el prefijo, «buscar la palabra
en lo compartido» pasa a ser una orden que se puede correr. Sin él, la prueba al revés de
`docs/MODULOS.md` —«Cómo se comprueba que la línea está bien puesta»— no distingue una filtración de la frase que enuncia la regla: se midió el 25
de agosto de 2026 y de once apariciones de esas palabras en las migraciones compartidas, once eran
comentarios y varias eran el propio enunciado. Está contado en `docs/ALCANCE.md`.

---

## Aparecidos al comparar con un competidor real

Salieron del relevamiento del 24 de agosto de 2026. El detalle está en `docs/CATALOGO.md`,
sección «Lo que no es una lista».

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 20 | **Las claves viejas de Tipo de Asistente no estaban donde decía este renglón.** Decía que `caregivers.profession` guardaba `domiciliaria`, `enfermera`, `auxiliar` y `at`. Revisado el código el 24 de agosto de 2026, esas palabras estaban escritas en pantallas y no en la base: `directorio.html` las tenía en los cuatro filtros y en las ocho tarjetas de muestra, y `formulario-integral.html` en las tarjetas del paso 3: **las dos están corregidas y hoy salen del catálogo** (`formulario-integral.html:166`, comprobado el 25 de agosto de 2026). La siembra sí escribió claves malas, y eso lo repara la migración 0010 (pendiente 27, cerrado). **De la base no se puede afirmar nada desde acá**: `caregivers` le niega la lectura a quien no inició sesión, así que no se sabe si quedó alguna fila con una clave vieja escrita antes de que las pantallas tomaran el catálogo | Se borren los datos de personas antes de producción, que es cuando desaparecen esas filas. Son todas inventadas, así que **no se migran: se descartan** |
| 25 | **`cursos.html` lee los cursos del archivo y no de la base**, aunque las tablas existen (migración 0008). Las seis filas están hoy en dos lugares: la tabla `cursos` y `data/catalogo-oferta.json`. El motivo es una decisión de seguridad que no está tomada: la política de `cursos` es `for select to authenticated`, y esa pantalla es pública. Darle lectura a `anon` sobre las filas de `tenant_id` vacío es ampliar el acceso anónimo, y la regla de los productos Careonys «ser exploratorio no relaja la seguridad» no lo admite. **La pantalla ya explica por qué lee del archivo** (`cursos.html:196`), así que el próximo que la abra no vuelve a investigarlo | El Desarrollador decida si la oferta general de cursos es pública. **Si sí**, una migración agrega la política para `anon` restringida a `tenant_id is null` y `cursos.html` pasa a leer de la base. **Si no**, la decisión se escribe en `docs/ALCANCE.md`, esta fila se borra y con ella se cierra la parte de cursos del punto 7 |
| 31 | **Dos columnas guardan catálogo sin tener catálogo.** `avisos.schedule_type` tiene hoy cuatro formas para lo mismo: la siembra escribe `turno_manana` y `guardia_12`, `js/main.js:270` escribe `flexible` y `solicitar-asistente.html:393` escribe `A coordinar`. Ninguna es una clave de vocabulario: `guardia_12` es de `modalidad_contratacion` y `turno_manana` no es de ninguno. `caregivers.nationality` tampoco tiene vocabulario. Mientras no lo tengan, `scripts/verificar_claves.mjs` no las puede mirar. **La tercera, `cursos.nivel`, se cerró el 26 de agosto de 2026** y no esperaba una decisión: sus tres claves ya estaban guardadas desde la migración 0008, así que sólo faltaba el texto para mostrarlas (`data/catalogo-vocabularios.json`, vocabulario `nivel_curso`) | Se decida qué vocabulario gobierna cada una de las dos que quedan —si `schedule_type` es un turno, una modalidad de contratación o las dos cosas, que entonces son dos columnas—, se agregue a `data/catalogo-vocabularios.json` lo que falte, y las dos columnas entren en `COLUMNAS` de `scripts/verificar_claves.mjs` |
| 35 | **Los seis cursos están escritos para Familias y se los ofrece como capacitación de Asistentes.** `cursos.html` se titula «Cursos para Familias», encabeza con «Herramientas prácticas para familiares que cuidan» y describe cada curso desde ese lugar —«acompañar a un familiar», «la persona cuidada»—. Los mismos seis son los que la migración 0008 siembra en `cursos`, los que ahora lee «Mis Capacitaciones» en la PWA del Asistente (`pwa-asistente/index.html:572`) y los que la evaluación certifica **dentro del legajo, a la vista de la Prestadora** (`cursos.html:111`). Una misma oferta le habla a las dos puntas y el catálogo no distingue: ni `data/catalogo-oferta.json` ni la tabla `cursos` dicen a quién está dirigido ningún curso | El catálogo diga a quién está dirigido cada curso y cada pantalla muestre los que le tocan. **Qué cursos son para Familias y cuáles para Asistentes lo dice el Desarrollador**: es oferta, no configuración, y no se inventa desde acá. Con eso resuelto, la lista se parte: la vidriera pública muestra los de Familias y «Mis Capacitaciones» los de Asistentes, que hoy muestra todos. El punto 25 también se apoya en esta decisión |
| 40 | **Queda una columna vieja en `avisos` que ninguna pantalla vuelve a escribir.** El título anterior decía que la tabla de franjas no estaba en el servidor, y su propio cuerpo ya decía lo contrario; se corrigió el 26 de agosto de 2026 después de ver el esquema en vivo, donde `franjas_aviso` está y `grid_schedule_7x3` también. El 25 de agosto de 2026 las dos pantallas —`pwa-familia/index.html` y `formulario-integral.html`— pasaron a preguntar la misma grilla de veintiún casilleros que usa el Asistente, y `js/apiClient.js` escribe una fila por casillero en `franjas_aviso`. **La migración 0016 ya corrió**: la línea de comandos quedó enlazada con el proyecto de Supabase y aplica las migraciones sola, y el 25 de agosto de 2026 el servidor contestó que están las veinte. Así que la tabla existe y las franjas se guardan. Lo único que queda es **`avisos.grid_schedule_7x3`**, que sigue ahí con las dos formas viejas adentro y que ninguna pantalla vuelve a escribir. Lo hecho está en `docs/ALCANCE.md` | `grid_schedule_7x3` esté vaciada y sacada con `alter table public.avisos drop column grid_schedule_7x3;`. Toca datos guardados, así que la ordena el Desarrollador |
| 41 | **El nombre del paciente guarda cuatro datos pegados en un solo texto.** `js/main.js` arma `patient_name` como `Paciente de 82 años (femenino) - Acompañamiento de tarde`: edad, género, título del aviso y la palabra «Paciente», todo en la columna que debería tener un nombre. Ninguno de los cuatro se puede filtrar ni contar, y la edad y el género ya tienen vocabulario propio. La pantalla del teléfono, en cambio, guarda ahí sólo el nombre, así que la misma columna tiene hoy dos formatos | `avisos` tenga columna para la edad y para el género del paciente, y otra para el título del aviso, y `patient_name` guarde el nombre y nada más. **Toca dato ya guardado, así que lo decide el Desarrollador** (la regla de la empresa «se consulta cuando hay que borrar o pisar datos»): son cuatro filas ficticias, pero la migración tiene que decir qué se hace con lo que ya está escrito |

---

## Se resuelve dentro de la migración a React

Nada de esto conviene arreglar antes: la migración reescribe cada pantalla y el arreglo se tiraría.

**Dos condiciones necesarias, no comodidades** —remarcado por el Desarrollador el 2026-08-26—:
multiidioma y que el modo claro/oscuro siga el del dispositivo. Las dos están **construidas**: la
del modo en `css/tokens.css` (ver `docs/DISENO.md`), y la del idioma en `data/catalogo-frases.json`
con `js/catalogo.js`. **Por eso el pendiente 9 ya no está en este grupo**: se mudó a «antes de
portar la primera pantalla» el 26 de agosto de 2026, porque cada pantalla nueva escrita a mano lo
encarece y la migración va a agregar pantallas. Ninguna pantalla nueva se da por terminada
mientras lea texto escrito a mano en vez del catálogo de frases, y
`scripts/verificar_frases.mjs` lo comprueba antes de cada `commit`.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 8 | **2.166 declaraciones de estilo pegadas al HTML**, en 687 atributos `style=`. **La parte de colores ya está cerrada**: los 434 colores escritos a mano se fueron el 25 de agosto de 2026, `scripts/verificar_paleta.mjs` impide que vuelvan, y ese mismo día se encendió el modo oscuro automático con `scripts/verificar_temas.mjs` cuidando que nada lo vuelva a romper (las dos cosas están contadas en `docs/ALCANCE.md`). Lo que queda es el resto de las declaraciones —tamaños, márgenes, alineaciones—, que no rompen el modo oscuro pero sí hacen que cada pantalla se porte a mano. Reparto por archivo abajo | Ningún atributo `style=` quede en las pantallas. Se saca pantalla por pantalla, al portarla |
| 69 | **La red de dieciséis chequeos no sobrevive a la migración, y no lo diría.** Es el paso 1 de «antes de un cambio grande» —el inventario— hecho sobre la red que sostiene al producto en vez de sobre el producto. **Medido el 26 de agosto de 2026 leyendo los dieciséis:** ninguno nombra `.jsx` ni `.tsx`, así que el día que la primera pantalla deja de ser un `.html`, **nueve dejan de mirarla y siguen diciendo ✔**, con un número más chico que nadie mira; `guiones` se queda revisando cero bloques, porque revisa los `<script>` de adentro del HTML y en Vite no hay ninguno. No es que la migración rompa la red: **la desarma en silencio**, que es peor, porque el `commit` sigue saliendo verde. Y no alcanza con agregarles la extensión: cuatro buscan una forma que en React no existe —el color adentro de `style="..."`, las plantillas que arman HTML, los manejadores escritos en el marcado, y el atributo `data-frase` que marca una pantalla como convertida—. **Lo que sí avisa fuerte son dos**, `referencias` y `copias`, porque nombran caminos exactos y al renombrarse las pantallas fallan; conviene no apagarlos. **Y apareció algo que se midió al revés de lo esperado:** `contacto` e `identidad` corren el mismo código que corre la pantalla, cargándolo con `require()`, y eso funciona porque hoy no hay `package.json`. Levantar Vite crea uno con `"type": "module"`, y ahí `require()` **no falla: devuelve un objeto vacío**, probado, no supuesto. Que se note depende de una casualidad: los dos usan lo que cargaron enseguida y sin preguntar, así que revientan en el acto; si alguno hubiera preguntado antes «¿tiene nombre?», habría pasado en verde sin comprobar nada — la trampa del `undefined` que nombra la regla de la empresa. El detalle entero, chequeo por chequeo, está en `docs/ALCANCE.md`, al final de la hoja de ruta | La red se adapte **antes** de la primera pantalla portada y no después: los nueve que sólo cambian de extensión la tengan, los cuatro que cambian de forma la tengan, `guiones` se jubile y `copias` se jubile recién cuando las tres copias dejen de existir. Y que ninguno pueda quedar mirando cero archivos y decir ✔: cada uno que se toque se arma un ejemplo malo y comprueba que lo agarra, como ya hacen `cajas` y `referencias` |
| 11 | **La Prestadora de ejemplo está escrita a mano, 102 veces en 23 archivos** (contadas el 25 de agosto de 2026). «PresDemo» aparece en cada `<title>`, en cada encabezado, en cada pie, en los avisos legales de `panel-prestadora.html` y `registrar-asistente.html`, y con tres grafías distintas —`PresDemo`, `PrestDemo`, `Presdemo`—. Su logotipo está enlazado como `assets/images/logo_presdemo.png` en 27 lugares de 15 archivos, uno de ellos como valor de fábrica del cliente de datos (`js/apiClient.js:125`). Lo que sí se arregló: el remiendo que tenía escritos el identificador y el UUID de esta Prestadora ya no está: y desde la migración 0021 tampoco hay respaldo que lo reemplace: cuando la dirección no nombra ninguna Prestadora, no se muestra ninguna. **Es «toda decisión técnica se valida contra cientos de Prestadoras», no una prolijidad**: cuando haya cientos de Prestadoras, cada uno de esos 102 lugares muestra la Prestadora equivocada. El nombre del producto ya no es parte de este problema: se cerró | El nombre, el logotipo y el color de la Prestadora salgan de la Organización de la sesión, como ya hace `apiClient.js` con el logotipo del encabezado, y ninguna pantalla los tenga escritos. **El inventario y el plan ya están hechos**, en `docs/PLAN_PRESTADORA.md`, que contó 111 apariciones el día que se escribió: 32 ya se reemplazan solas al cargar y 43 quedan sueltas. Falta que el Desarrollador apruebe, y decida las cuatro cosas de su sección 4 |
| 12 | **Las dos PWAs enlazan al directorio padre** para imágenes y para pantallas del sitio: 3 enlaces en `pwa-asistente/index.html` y 13 en `pwa-familia/index.html`, más los que arma el guion (`../perfil.html` y `../examen.html`). Su service worker no alcanza esas rutas, así que fuera de línea no cargan | Haya un solo service worker en la raíz del sitio, que es lo que emite `vite-plugin-pwa`. El problema se disuelve, no se parcha |
| 13 | **`apiClient.js` y `auth.js` están triplicados byte a byte** —921 renglones en tres lugares— y `styles-pwa.css` duplicado. `css/tokens.css` está en tres lugares por la misma razón: el service worker de cada PWA solo alcanza su propia carpeta, así que fuera de línea no puede leer el del padre. **Y hay una tercera copia, más chica y más peligrosa**: el interruptor que enciende uno de «los cuatro estados» está escrito ocho veces, una por pantalla (`acceso.html:89`, `directorio.html:225`, `examen.html:283`, `mockup-app.html:444`, `nueva-clave.html:133`, `perfil.html:233`, `pwa-familia/index.html:851` y `recuperar-clave.html:123`). Ya se habían separado: cuatro imponían `display: 'block'` y dos devolvían `display: ''`, que no es lo mismo cuando el panel es grid o flex. El 25 de agosto de 2026 se igualaron a la forma segura y se les escribió el motivo al lado, pero siguen siendo ocho | Existan imports reales. Desaparece solo al migrar. Mientras tanto `scripts/verificar_copias.mjs` compara las cinco copias byte a byte y falla si alguna se separó: no las une, pero avisa, y corre solo antes de cada `git commit` |
| 14 | **Dos fuentes de datos conviviendo**: Supabase y datos escritos en el HTML. Eran cuatro. El 24 de agosto de 2026 se fueron dos: el almacenamiento del navegador —el interruptor `useSupabase` valía siempre `true`, así que cada método de `js/apiClient.js` arrastraba una copia muerta que escribía en el navegador, y `js/main.js` sí escribía ahí de verdad y anunciaba éxito— y `data/cuidadores.json`, cuatro perfiles de muestra que no leía nadie. El 25 de agosto de 2026 se fueron las dos últimas: las tarjetas de Asistente escritas a mano en `mockup-app.html` y las ocho fichas de `perfil.html`, que hoy salen de `directorio` —contado en `docs/ALCANCE.md:735`—. **Ninguna pantalla tiene ya datos escritos adentro**, se dio por comprobado el 25 de agosto de 2026, **y el 26 de agosto de 2026 apareció que no era cierto**: `pwa-familia/index.html` y `mockup-app.html` traen escritas a mano una ficha de paciente y un recuadro de verificaciones, que son el pendiente 65. Lo que sigue habiendo es una segunda fuente que no es el HTML: los seis `data/catalogo-*.json`, que son el pendiente 7 | Quede una sola. El punto 2 ya se decidió el 24 de agosto de 2026, así que estas pantallas se pueden tocar: el directorio se ve sin sesión y tiene que leer de la base |
| 16 | **Las imágenes traen la paleta ajena adentro.** Las nueve ilustraciones de `assets/images/` están dibujadas con los colores del sitio de origen. Cambiar las variables CSS no cambia un `.png`: las pantallas van a quedar con la paleta de Careonys alrededor y la vieja en el medio | Cada ilustración se rehaga con la paleta propia o se reemplace. Va con el mapeo del sistema de diseño, no antes |

### Reparto de los estilos pegados al HTML

Sirve para saber qué pantalla va a costar más y en qué orden conviene portar.

Contada el 25 de agosto de 2026, y cambió bastante: las dos pantallas que se reescribieron son
hoy las más limpias del proyecto, y aparecieron cuatro archivos que la cuenta anterior no miraba.

| Archivo | Atributos `style=` | Declaraciones |
|---|---:|---:|
| `registrar-asistente.html` | 155 | 533 |
| `mockup-app.html` | 101 | 329 |
| `pwa-familia/index.html` | 76 | 229 |
| `formulario-integral.html` | 73 | 202 |
| `solicitar-asistente.html` | 58 | 229 |
| `pwa-asistente/index.html` | 58 | 156 |
| `index.html` | 54 | 235 |
| `panel-prestadora.html` | 30 | 86 |
| `cursos.html` | 14 | 43 |
| `nueva-clave.html` | 13 | 32 |
| `perfil.html` | 13 | 24 |
| `examen.html` | 12 | 14 |
| `recuperar-clave.html` | 11 | 26 |
| `directorio.html` | 10 | 16 |
| `acceso.html` | 6 | 7 |
| `soporte-remoto.html` | 3 | 5 |

Las dos PWAs tienen además 763 renglones de CSS en bloques `<style>` adentro del HTML: 255 la del
Asistente y 508 la de la Familia.
