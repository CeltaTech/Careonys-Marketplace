# Pendientes

> Lo que está abierto, con **condición de cierre** para cada punto. Sin condición de cierre un
> pendiente no se puede dar por resuelto, así que no entra sin ella.
>
> Cuando algo se cierra **se borra de acá**, no se tacha. Si era una afirmación de estado, pasa a
> `docs/ALCANCE.md` con archivo y renglón.

---

## Seguridad

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 24 | **La evaluación de muestra se puede aprobar adivinando.** El mecanismo ya está bien: la corrige la base, la respuesta correcta no sale de ahí y el intento no se puede escribir a mano (migración 0008, comprobado en `scripts/probar_aislamiento.mjs`, puntos 21 a 27). Lo que falla es el contenido. Son **2 preguntas de 2 opciones cada una**, hay que acertar las dos y se dan **3 intentos**: quien no sepa nada acierta una vuelta con probabilidad 1 de 4, y en tres vueltas aprueba con probabilidad 37 de 64, **más de la mitad de las veces**. Un certificado que sale así sigue sin decir nada sobre quién va a entrar a una casa a cuidar a una persona | Cada evaluación publicada tenga preguntas suficientes como para que adivinar deje de ser un camino: con 10 preguntas de 4 opciones y 70% para aprobar, la probabilidad de acertar de casualidad baja a menos de 1 en 300. **El contenido no lo escribe la línea de comandos**: las preguntas las tiene que redactar alguien que sepa del tema, y la línea de comandos las carga. Mientras tanto la única evaluación cargada es la de muestra que sembró la migración 0008, y así está descrita ahí |
| 21 | **Falta decidir si el alta de Asistente sigue pidiendo confirmar el correo.** Hoy lo pide: `/auth/v1/settings` devuelve `mailer_autoconfirm: false` y `supabase/config.toml:253` dice lo mismo desde el 24 de agosto de 2026. **La parte que costaba un legajo entero ya está resuelta**: `signUp` sigue devolviendo la cuenta sin sesión, pero la pantalla ya no manda a nadie a empezar de nuevo —guarda lo cargado, muestra el panel «La cuenta quedó creada. Falta un paso» y ofrece **«Ya confirmé, continuar»**, que entra con la contraseña que la persona todavía tiene escrita y guarda el legajo desde donde se había frenado (`registrar-asistente.html:542` el panel, `:1009` el tramo que escribe)—. Lo que queda es una decisión de negocio, y es del Desarrollador. | Se decida si la confirmación se queda encendida. **Encendida** —como está hoy— el alta es de dos tiempos, pero nadie puede darse de alta con el correo de otra persona. **Apagada**, el alta es de un solo paso y esa puerta queda abierta. Se apaga con `enable_confirmations = false` en `supabase/config.toml` más un `supabase config push`, sin tocar el tablero. |
| 45 | **El proyecto alojado manda los correos con el servicio de fábrica, y ese servicio tiene un tope bajo.** Cada alta de Asistente o de Familia dispara un correo de confirmación, y a los pocos seguidos el servidor contesta *«email rate limit exceeded»* a todos por igual, incluso a `gmail.com`. No es un problema de pruebas solamente: un día con varias altas de verdad deja gente afuera, y lo que esa gente ve es que el alta falló. Comprobado el 24 y el 25 de agosto de 2026 intentando registrar cuentas ficticias contra el servidor alojado. **Esto lo abre el Desarrollador**: es configuración del proyecto alojado, `supabase/config.toml` no tiene llave para eso y `supabase config push` no la alcanza — se carga un servidor de correo propio en Authentication → Emails → SMTP Settings | El proyecto alojado mande los correos por un servidor propio, y varias altas seguidas dejen de fallar por el tope |
| 47 | **No hay ninguna cuenta de Asistente con la que se pueda entrar.** Este pendiente eran dos problemas y queda uno. **El cerrado**: las pantallas de acceso de las dos aplicaciones del teléfono traían un correo y una contraseña ya escritos en el HTML, cosa que no sobrevive a la primera persona real; los cuatro campos se vaciaron el 25 de agosto de 2026 y hoy arrancan con su indicación adentro (`pwa-asistente/index.html:310` y `:313`, `pwa-familia/index.html:571` y `:574`). **El abierto**: esa cuenta tampoco existía —contra el servidor alojado contesta «Invalid login credentials», comprobado el 25 de agosto de 2026—, así que nada de lo que vive detrás de una sesión se puede probar de punta a punta: «Mis Capacitaciones» se cerró sin haber podido ver la lista con los cursos de verdad. Crear una cuenta nueva choca con el tope de correos del pendiente 45 | Exista una cuenta de Asistente ficticia con la que se pueda entrar, lo que depende del pendiente 45 |
| 43 | **El directorio público muestra dos cosas que el consentimiento no nombra, y esconde una que la pantalla pedía.** El texto que la persona acepta promete «su nombre, su foto, su zona, qué atiende y su precio por hora» (`data/catalogo-autorizaciones.json`, `perfil_publicado`). La vista `directorio` devuelve además `gender` —la tarjeta no lo dibuja, pero está en la respuesta y cualquiera la lee— y la pantalla mostraba cuatro insignias de verificación inventadas más un filtro «Verificación», que se sacaron el 24 de agosto de 2026 porque `verificaciones_asistente` no es pública y no hay con qué contestarlos. Son tres decisiones del Desarrollador y ninguna es técnica: **(a)** si el género se muestra —una Familia suele pedir preferencia, y el vocabulario `genero_preferido` ya existe— y entonces el consentimiento lo dice, o no se muestra y sale de la vista; **(b)** si el directorio puede decir qué se le controló a un legajo —antecedentes, salud, domicilio— y entonces el consentimiento lo dice y hace falta una vista pública que lo devuelva sin fechas ni números; **(c)** si vuelve el filtro «Verificación», que depende de (b) | El consentimiento y lo que la vista devuelve digan lo mismo, en los tres idiomas, y `directorio` no tenga ni una columna de más |
| 49 | **Los dos borradores legales dicen que no se muestran a nadie, y las pantallas los muestran con una casilla obligatoria.** `docs/terminos_y_condiciones_asistentes.md` y `docs/terminos_y_condiciones_familias.md` empiezan los dos con el mismo aviso: son un borrador sin revisión profesional, necesitan un abogado —sobre todo el deslinde de relación laboral, que es el punto más sensible del rubro en Argentina— y **no se muestran a ninguna persona usuaria hasta esa revisión**. Pero `registrar-asistente.html:522` y `formulario-integral.html:417` los enlazan cada uno junto a una casilla `required`: nadie se da de alta sin marcar que los leyó y los acepta. Están además en el pie de `index.html:508`, `formulario-integral.html:464` y `registrar-asistente.html:598`. Hoy no hay ninguna persona real y no le pasa nada a nadie; el día que la haya, la plataforma va a estar haciendo aceptar un texto que ella misma declara no revisado. **Esto no lo puede cerrar la línea de comandos**: hace falta que un abogado lea los dos documentos | Un abogado haya revisado los dos textos y el aviso de arriba se haya sacado, **o** las pantallas dejen de enlazarlos y la casilla no se pueda marcar. Lo que no puede seguir es que el documento diga una cosa y la pantalla haga la contraria |

---

## Antes de portar la primera pantalla

Se hacen una sola vez, y si no se hacen, la decisión la termina tomando quien porta.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 6 | **`mockup-app.html` es el único lugar del proyecto donde existe un chat** (`screen-chat`, con suscripción en tiempo real y confirmación optimista) y la única pantalla de alta que sirve a una Familia: `pwa-familia` no tiene ninguna. Ninguna de las dos PWAs tiene chat. Su código **no se copia**: lee `messages?order=created_at.asc&limit=50` sin ningún filtro, se suscribe a la tabla entera sin filtro, evita `apiClient.js` y, sin sesión, intenta escribir con la clave pública —y no puede: la tabla `messages` contesta 401 a quien no inió sesión, así que **ningún mensaje del chat llegó nunca a guardarse**. Hasta el 25 de agosto de 2026 la pantalla no lo decía, porque `fetch` no falla cuando el servidor rechaza el pedido; ahora la burbuja avisa que no se pudo enviar. El mensaje recibido lo mete con `innerHTML`, pero **sí lo escapa** con `Texto.escapar()` (`mockup-app.html:775`), así que por ahí no entra una etiqueta; lo que no tiene es filtro ni sesión. **Para qué existe el chat** (lo recordó el Desarrollador el 24 de agosto de 2026): es la forma en que la Familia y el Asistente se comunican **antes** de la contratación. De ahí sale una condición que no estaba escrita en ningún lado: **adentro del chat no se pueden filtrar datos de contacto**. Si se filtran, la conversación sigue por afuera y la plataforma cobra una vez y nunca más, que es exactamente la razón por la que el teléfono no se muestra (`docs/CATALOGO.md`). Anotada ahí como la tercera puerta. Y el diseño de las tablas ya existe en `docs/TABLAS_QUE_FALTAN.md` —`conversaciones` y `mensajes`, atadas a Familia, Asistente y aviso—, que **no coincide** con la tabla `messages` que hay hoy en la base, colgada de una búsqueda de cuidado y no del vínculo entre dos personas | Las dos capacidades estén anotadas como requisito para las PWAs y reescritas contra el esquema nuevo, y `css/mockup-app.css` haya pasado al sistema de diseño. Recién entonces se borran él y `mockup-app.html`. Y el chat no deje pasar un teléfono, un correo ni una dirección |
| 46 | **Un perfil publicado no tiene todavía con qué empezar una conversación con esa persona.** `perfil.html:136` dice la verdad —sólo una Familia registrada puede comunicarse, y por la plataforma— y ofrece las dos puertas que existen: entrar como Familia (`acceso.html`) y publicar un aviso (`solicitar-asistente.html`). Ninguna de las dos lleva a **esa** persona: el aviso lo contesta quien quiera, y entrar como Familia no abre nada contra ese Asistente. El mecanismo es el chat, que hoy existe solamente en `mockup-app.html` (punto 6), y las tablas diseñadas para eso —`conversaciones` y `mensajes`, atadas a Familia, Asistente y aviso, `docs/TABLAS_QUE_FALTAN.md`— **no están en la base**: lo que hay es la tabla `messages`, colgada de una búsqueda de cuidado y no del vínculo entre dos personas | Desde el perfil de un Asistente publicado, una Familia con sesión pueda abrir una conversación con esa persona y el Asistente la reciba. Va detrás del punto 6: primero las tablas y el chat reescrito, después el botón. **Con qué condiciones una Familia puede escribirle a alguien que no contestó ningún aviso lo decide el Desarrollador**: es regla comercial, y `docs/ALCANCE.md` §4 la tiene congelada |
| 48 | **Diez textos no llegan al mínimo legible, y los tres motivos son decisiones de diseño, no del código.** Medido el 25 de agosto de 2026 sobre los 763 textos de las dieciséis pantallas, contra el fondo que de verdad tienen debajo, de día y de noche. **(a)** Letra blanca sobre `--azul-medio` llega a 3.21:1 y hace falta 4.5:1, en cinco lugares: el redondel del paso activo del formulario (`formulario-integral.html` y `registrar-asistente.html`), la banda con el nombre adentro del teléfono dibujado (`index.html` y `registrar-asistente.html`) y el botón de ingresar de `mockup-app.html` (`css/mockup-app.css:58`). Ese último de noche se lee, pero de casualidad: sus letras salen de `--superficie`, que de noche se vuelve gris oscuro, así que el botón azul termina con letra oscura sin que nadie lo haya decidido. El arreglo **cambia cómo se ve**: o el fondo pasa a `--azul-oscuro` —y entonces el paso activo se confunde con el paso ya hecho, que ya usa ese color— o la letra pasa a oscura. **(b)** Los colores que elige una Prestadora no tienen versión de noche: los de la Prestadora de ejemplo salen de `primary_color` y `accent_color` de su fila en `tenants`, `js/apiClient.js` los escribe encima de los tokens al abrir sesión, y de noche el enlace «Registrarse como Asistente» de `pwa-asistente` queda en 1.43:1 —azul marino sobre gris oscuro—. De día el mismo par de colores deja seis textos entre 4.13:1 y 4.2:1. El problema de fondo no es esta Prestadora: es que **nada valida el color que elige una Prestadora**, y cuando haya cientos va a haber cientos de colores ilegibles. **(c)** Las estrellas de calificación quedan en 4.4996:1, que es 4.5 raspando por abajo: `--naranja-alerta-texto` promete 4.5:1 sobre fondo claro y se queda a cuatro diezmilésimas. Ése es un token de Careonys, así que el arreglo va allá | Los tres estén decididos: (a) qué se cambia en el redondel y en la banda, (b) qué hace el sistema con un color de Prestadora que no se lee —rechazarlo al cargarlo, o calcularle una versión de noche—, y (c) `--naranja-alerta-texto` una pizca más oscuro en Careonys. Cerrado cuando los 763 textos midan 4.5:1 o más en los dos modos |
| 7 | **El catálogo ya lo leen las pantallas; falta que viva en tablas.** Las 22 listas (133 opciones), los 9 servicios y los 6 cursos salen de `data/catalogo-vocabularios.json` y `data/catalogo-oferta.json` a través de `js/catalogo.js`, y las nueve pantallas portadas los declaran en vez de copiarlos: 35 listas y 5 grillas de tarjetas. Lo que falta es el otro medio: **el destino de los vocabularios son las tablas de Careonys, no unas propias** (`docs/MODULOS.md:36`), y esa fusión todavía no ocurrió. Los cursos sí tienen tablas propias y legítimas (migración 0008), pero `cursos.html` lee del archivo porque la política de `cursos` exige sesión y esa pantalla es pública | El catálogo viva en tablas: los vocabularios en las de Careonys al fusionar, y los cursos acá cuando se decida si `anon` puede leer los de `tenant_id` vacío (ver punto 25). Una pantalla portada contra una tabla vacía no da error: queda vacía y nadie se entera |
| 50 | **Los dos botones de «Ingresar con Google» y «Ingresar con Facebook» no hacen absolutamente nada.** Están en `mockup-app.html:74` y `:76`, debajo del botón de ingresar, separados por una línea que dice «O». No tienen identificador, no tienen ningún guion que los escuche, y en `supabase/config.toml` el único proveedor de acceso externo que aparece es Apple, apagado (`supabase/config.toml:355`): quien los toca no recibe error ni aviso, la pantalla se queda igual y parece que la aplicación se colgó. Es la regla 3 al revés —ni cargando, ni error, ni vacío, ni listo—. Son además los dos únicos colores exentos de `scripts/verificar_paleta.mjs`, porque son las marcas de otras dos empresas. **Ofrecer entrar con la cuenta de otro es una decisión, no una tarea**: obliga a configurar cada proveedor, a decidir qué pasa cuando el correo ya existe con contraseña, y a mirar qué datos entrega cada uno | El Desarrollador decida: se configuran los proveedores de verdad, **o** los dos botones se sacan. Mientras tanto, un botón que no hace nada es peor que no tenerlo |
| 51 | **El único importe del esquema se guarda sin moneda.** `caregivers.hourly_rate` (`supabase/migrations/0001_esquema_inicial.sql:102`) es un `numeric` a secas, y no hay ninguna columna de moneda en ninguna de las 22 tablas: comprobado el 25 de agosto de 2026 recorriendo las quince migraciones. La regla 11 de `CLAUDE.md` no lo admite —un número solo, leído un año después, no se sabe cuánto vale— y la 2 pide `es-AR`, `en` y `pt-BR` desde el primer día, así que más de una moneda no es una hipótesis lejana. El chequeo `scripts/verificar_esquema.mjs` lo tiene anotado como exención con su motivo, así que está a la vista y no puede entrar un importe nuevo sin moneda. **Toca una columna que ya tiene datos escritos, así que lo decide el Desarrollador** (`CLAUDE.md` §7): son filas inventadas, pero la migración tiene que decir qué moneda se le pone a lo que ya está guardado | `caregivers` tenga columna de moneda, obligatoria y poblada, y la exención se borre del chequeo |
| 52 | **El cuaderno de cuidado cuelga del aviso, y Careonys ya decidió hace rato que cuelga de la guardia.** Acá `logbook_entries.aviso_id` ata cada anotación al aviso que dio origen al trabajo. En el repositorio de Careonys, `docs/DATA_MODEL.md`, la misma cosa se llama `reportes` y cuelga de `guardia_id`: la **guardia** es una jornada concreta de trabajo —un Asistente, un Paciente, una fecha, un horario, un check-in y un check-out—, y el reporte diario es de esa jornada. El aviso no aparece en ninguna parte de esa cadena, y es correcto que no aparezca: **el aviso es cómo empezó la relación, no lo que es el trabajo**. Por eso en prestación directa hay reporte igual, sin que nadie haya publicado nada. **Y no es sólo esta tabla.** `logbook_entries` de este repositorio **es** `reportes` de Careonys, y `clock_ins` **es** el `checkin_at`/`checkout_at` de `guardias`: son las mismas cosas con otro nombre y otra forma, en dos bases distintas | Se traiga acá la `guardia` de Careonys, que ya existe diseñada y con RLS, y el cuaderno y los mensajes cuelguen de ella y no del aviso. No se decide nada nuevo: se deja de decidir dos veces lo mismo. Toca datos ya guardados, así que la ordena el Desarrollador |
| 53 | **El segundo nivel de Asistente no tiene nombre.** El Desarrollador dijo el 25 de agosto de 2026 que hay dos clases de Asistente: el que se anotó y todavía nadie revisó —invisible para las Familias— y el que ya pasó todos los controles. La base ya los distingue: `caregivers.verification_status` arranca en `en_revision` (`supabase/migrations/0001_esquema_inicial.sql:91`) y pasa a `validado_prestadora` cuando la Prestadora revisó los papeles (`panel-prestadora.html:335`). Y el directorio exige dos cosas a la vez, no una: que el legajo esté validado **y** que la persona haya marcado `perfil_publicado`, así que un Asistente puede ser invisible por dos motivos distintos —todavía no lo controlaron, o lo controlaron y él no quiere aparecer— y conviene no confundirlos. **Lo que falta es el nombre.** «Validado» dice qué le pasó al legajo, no qué es la persona, y en las pantallas hace falta nombrar a la persona. El propio Desarrollador escribió «o como le llamemos», así que no está decidido y no se inventa | El Desarrollador diga cómo se llama un Asistente que ya pasó todos los controles |
| 55 | **El renombre de `verificaciones_asistente` en Careonys.** La decisión ya está tomada: el Desarrollador la aceptó el 25 de agosto de 2026 y está escrita entera en `docs/MODULOS.md`, sección «Verificar un papel y incorporar a una persona son dos cosas». En resumen: la de este repositorio se queda como está, porque es el módulo Verificación y porque tiene el vencimiento, que es lo que distingue un papel de una etapa; **la de Careonys pasa a llamarse `etapas_incorporacion`** y guarda **sólo la entrevista**, porque las otras cuatro etapas ya tienen quién las guarde. **Acá no queda nada por hacer.** El renombre corre sobre el repositorio de Careonys y sobre datos ya guardados, y este repositorio sólo lee los otros proyectos | Se ejecute con la fusión, del lado de Careonys. Queda pendiente sólo por eso, no porque falte decidir algo |
| 56 | **Qué suma en el legajo comprobado, y cuánto.** El 25 de agosto de 2026 quedó decidido que las estrellas son sólo de la calificación de las Familias y que el legajo lleva escudo (`docs/DISENO.md`, «Tres cosas que se miden, tres formas distintas de dibujarlas»). Falta el número. **Primero hubo que contar, porque no eran dos:** al lado de `domicilio` y `referencia` —las dos con `puerta: ninguna`— están **`matricula` y `titulo`, que son condicionales**: para un `cuidador_domiciliario` no se exigen (`tipo_asistente.requiere_matricula = false`), así que quien igual las tiene está acreditando algo que nadie le pidió. Y afuera del catálogo de verificaciones el legajo guarda cuatro cosas más: los cursos aprobados **en el sistema** (`intentos_evaluacion.aprobado`), las cinco certificaciones que la persona **declara** (lista `certificacion`), los estudios (`estudios_asistente`, con `nivel_educativo`) y la experiencia (`experiencia_laboral_asistente`, que tiene fechas y por lo tanto años). Son ocho fuentes, no dos. **Propuesta de la línea de comandos:** (1) **el puntaje cuenta sólo lo comprobado**; lo que la persona declara y nadie miró se muestra en el perfil pero no suma, porque si sumara, el escudo mediría cuánto escribió alguien y no cuánto se le comprobó; (2) **las obligatorias no entran en el puntaje**, porque son la puerta: quien está en la modalidad ya las pasó, y contarlas le daría puntos a todos por lo mismo; (3) queda una lista de cinco, **igual para todos**: domicilio comprobado, referencia comprobada, matrícula comprobada aunque no se la exijan, título comprobado aunque no se lo exijan, y al menos un curso aprobado en el sistema; (4) **todas valen lo mismo**, porque cualquier otro reparto obliga a defender por qué el domicilio vale menos que la referencia, y eso no lo puede defender nadie; (5) **se muestra como fracción y con la lista al lado** —«4 de 5 comprobaciones»— porque un número suelto invita a comparar personas y la lista invita a decidir | El Desarrollador acepte o corrija la propuesta. Si acepta, hay que agregar a `data/catalogo-verificaciones.json` cuáles de las cinco entran, y decidir si «al menos un curso aprobado» es una sola comprobación o una por curso |


### Reparto de las 22 tablas, aplicado el 25 de agosto de 2026

`docs/MODULOS.md` reparte **módulos**, no tablas, y por eso la línea no se podía recorrer con una
búsqueda. Esto la baja a tabla por tabla. **Era una propuesta y el Desarrollador la aprobó
entera**: las tres de esta modalidad ya llevan el prefijo en el repositorio, a la espera de que
las dos migraciones se peguen en el servidor (pendiente 52).

| Lado | Tablas |
|---|---|
| **De esta modalidad** (llevan el prefijo de la modalidad) | `avisos`, `franjas_aviso`, y la vista `directorio` |
| **Compartidas** (se quedan como están) | `tenants`, `profiles`, `caregivers`, `clock_ins`, `logbook_entries`, `matriculas_asistente`, `estudios_asistente`, `experiencia_laboral_asistente`, `referencias_asistente`, `documentos_asistente`, `verificaciones_asistente`, `autorizaciones_asistente`, `cursos`, `evaluaciones`, `preguntas_evaluacion`, `opciones_pregunta`, `intentos_evaluacion`, `disponibilidad_asistente`, `franjas_asistente` |
| **La única dudosa** | `messages` |

**Por qué `messages` queda aparte.** `docs/MODULOS.md:60` pone «Contacto y su costo» de este lado,
y el chat es cómo una Familia llega a un Asistente. Pero en prestación directa la Prestadora
también le escribe a su personal, y esa tabla ya existe desde la 0001 con ese uso. Las dos
lecturas son defendibles y la pregunta que decide —*¿esto seguiría teniendo sentido en prestación
directa?*— acá contesta que sí. Se deja marcada en vez de resolverse sola.

**La palabra ya está: son dos palabras distintas y nombran dos cosas distintas.** El Desarrollador
lo aclaró el 25 de agosto de 2026. El **Aviso** es lo que la Familia publica y queda guardado. La
**Búsqueda** es la acción de buscar, y la hacen los dos lados de tres maneras: la Familia busca
entre los Asistentes disponibles, la Familia busca también publicando un aviso, y el Asistente
busca cuando revisa los avisos publicados hasta encontrar uno que le cuadre. **De ahí sale el
nombre de la tabla sin discusión**: lo que `avisos` guarda son avisos, no búsquedas, así
que le corresponde `avisos`; y `franjas_aviso`, que guarda los días y turnos de ese
aviso, es `franjas_aviso` —igual que su hermana `franjas_asistente`, que guarda los del
Asistente—. Las dos palabras quedaron propuestas en `docs/GLOSARIO.md` §3 y **esperan aprobación**
antes de tocar una línea, que es lo que manda §3. Nada se renombró todavía.

**Dos tablas compartidas apuntan al aviso, y eso pesa más que las palabras.** `logbook_entries` y
`messages` tienen una columna `aviso_id` que referencia `avisos`
(`supabase/migrations/0001_esquema_inicial.sql:242` y `:247`). La bitácora y el chat existen igual
en prestación directa, pero ahí no hay ningún aviso al que apuntar: esa columna quedaría siempre
vacía. Es exactamente la filtración que `docs/MODULOS.md` quiere evitar, y ninguna búsqueda de
texto la ve, porque `aviso_id` no contiene ninguna de las siete palabras. **También decide la
duda sobre `messages`**: si el chat cuelga de un aviso, el chat es de este lado. Queda para la
misma decisión, y son dos preguntas más: si la bitácora tiene que poder colgar de un aviso o si le
alcanza con colgar del Vínculo, y lo mismo para el chat.

**El costo de renombrar `avisos` es menor de lo que suena.** En Postgres es un `alter table
… rename to …`: las políticas, los índices, las claves foráneas y la función de la 0002 siguen
apuntando solas al mismo objeto, y no se mueve un solo dato. Lo que sí hay que cambiar a la par
son los 11 archivos que la nombran, porque el nombre de la tabla es además la dirección web por la
que la aplicación la consulta. No es una migración de datos; es un renglón y un reemplazo
parejo.

**Lo que este reparto arregla y la búsqueda de texto no.** Puesto el prefijo, «buscar la palabra
en lo compartido» pasa a ser una orden que se puede correr. Sin él, la prueba al revés de
`docs/MODULOS.md:87` no distingue una filtración de la frase que enuncia la regla: se midió el 25
de agosto de 2026 y de once apariciones de esas palabras en las migraciones compartidas, once eran
comentarios y varias eran el propio enunciado. Está contado en `docs/ALCANCE.md`.

---

## Aparecidos al comparar con un competidor real

Salieron del relevamiento del 24 de agosto de 2026. El detalle está en `docs/CATALOGO.md`,
sección «Lo que no es una lista».

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 20 | **Las claves viejas de Tipo de Asistente no estaban donde decía este renglón.** Decía que `caregivers.profession` guardaba `domiciliaria`, `enfermera`, `auxiliar` y `at`. Revisado el código el 24 de agosto de 2026, esas palabras estaban escritas en pantallas y no en la base: `directorio.html` las tenía en los cuatro filtros y en las ocho tarjetas de muestra, y `formulario-integral.html` en las tarjetas del paso 3: **las dos están corregidas y hoy salen del catálogo** (`formulario-integral.html:166`, comprobado el 25 de agosto de 2026). La siembra sí escribió claves malas, y eso lo repara la migración 0010 (pendiente 27, cerrado). **De la base no se puede afirmar nada desde acá**: `caregivers` le niega la lectura a quien no inició sesión, así que no se sabe si quedó alguna fila con una clave vieja escrita antes de que las pantallas tomaran el catálogo | Se borren los datos de personas antes de producción, que es cuando desaparecen esas filas. Son todas inventadas, así que **no se migran: se descartan** |
| 25 | **`cursos.html` lee los cursos del archivo y no de la base**, aunque las tablas existen (migración 0008). Las seis filas están hoy en dos lugares: la tabla `cursos` y `data/catalogo-oferta.json`. El motivo es una decisión de seguridad que no está tomada: la política de `cursos` es `for select to authenticated`, y esa pantalla es pública. Darle lectura a `anon` sobre las filas de `tenant_id` vacío es ampliar el acceso anónimo, y `CLAUDE.md` §4 dice que la seguridad no se relaja mientras se explora. **La pantalla ya explica por qué lee del archivo** (`cursos.html:196`), así que el próximo que la abra no vuelve a investigarlo | El Desarrollador decida si la oferta general de cursos es pública. **Si sí**, una migración agrega la política para `anon` restringida a `tenant_id is null` y `cursos.html` pasa a leer de la base. **Si no**, la decisión se escribe en `docs/ALCANCE.md`, esta fila se borra y con ella se cierra la parte de cursos del punto 7 |
| 31 | **Tres columnas guardan catálogo sin tener catálogo.** `avisos.schedule_type` tiene hoy cuatro formas para lo mismo: la siembra escribe `turno_manana` y `guardia_12`, `js/main.js:255` escribe `flexible` y `solicitar-asistente.html:393` escribe `A coordinar`. Ninguna es una clave de vocabulario: `guardia_12` es de `modalidad_contratacion` y `turno_manana` no es de ninguno. `caregivers.nationality` tampoco tiene vocabulario. Y `cursos.nivel` guarda `basico`, `intermedio` y `avanzado` (migración 0008), que tampoco son claves de ninguno: por eso «Mis Capacitaciones» no muestra el nivel del curso, porque no hay de dónde sacar cómo se escribe en la pantalla e inventarlo sería inventar una palabra. Mientras no lo tengan, `scripts/verificar_claves.mjs` no las puede mirar | Se decida qué vocabulario gobierna cada una —si `schedule_type` es un turno, una modalidad de contratación o las dos cosas, que entonces son dos columnas—, se agregue a `data/catalogo-vocabularios.json` lo que falte, y las tres columnas entren en `COLUMNAS` de `scripts/verificar_claves.mjs`. Con `nivel` resuelto, «Mis Capacitaciones» muestra el nivel de cada curso |
| 35 | **Los seis cursos están escritos para Familias y se los ofrece como capacitación de Asistentes.** `cursos.html` se titula «Cursos para Familias», encabeza con «Herramientas prácticas para familiares que cuidan» y describe cada curso desde ese lugar —«acompañar a un familiar», «la persona cuidada»—. Los mismos seis son los que la migración 0008 siembra en `cursos`, los que ahora lee «Mis Capacitaciones» en la PWA del Asistente (`pwa-asistente/index.html:572`) y los que la evaluación certifica **dentro del legajo, a la vista de la Prestadora** (`cursos.html:111`). Una misma oferta le habla a las dos puntas y el catálogo no distingue: ni `data/catalogo-oferta.json` ni la tabla `cursos` dicen a quién está dirigido ningún curso | El catálogo diga a quién está dirigido cada curso y cada pantalla muestre los que le tocan. **Qué cursos son para Familias y cuáles para Asistentes lo dice el Desarrollador**: es oferta, no configuración, y no se inventa desde acá. Con eso resuelto, la lista se parte: la vidriera pública muestra los de Familias y «Mis Capacitaciones» los de Asistentes, que hoy muestra todos. El punto 25 también se apoya en esta decisión |
| 40 | **Las franjas de un aviso ya se preguntan y ya tienen dónde ir, pero la tabla todavía no está en el servidor.** El 25 de agosto de 2026 las dos pantallas —`pwa-familia/index.html` y `formulario-integral.html`— pasaron a preguntar la misma grilla de veintiún casilleros que usa el Asistente, y `js/apiClient.js` escribe una fila por casillero en `franjas_aviso`. Lo que falta no lo puede hacer la línea de comandos: **la migración que crea esa tabla no corrió** —la base contesta que no existe, comprobado ese mismo día; ese mismo día a la tarde la migración se renumeró a 0016, porque la 0015 pasó a ser el renombre— y **`avisos.grid_schedule_7x3` sigue ahí**, con las dos formas viejas adentro. Mientras tanto publicar un aviso guarda el aviso y no sus días y turnos, y la pantalla lo dice sin pedir que se publique de nuevo. Lo hecho está en `docs/ALCANCE.md` | La migración `supabase/migrations/0016_franjas_de_un_aviso.sql` haya corrido en el panel de Supabase, y `grid_schedule_7x3` esté vaciada y sacada con `alter table public.avisos drop column grid_schedule_7x3;`. Las dos son del Desarrollador: la segunda toca datos guardados |
| 41 | **El nombre del paciente guarda cuatro datos pegados en un solo texto.** `js/main.js` arma `patient_name` como `Paciente de 82 años (femenino) - Acompañamiento de tarde`: edad, género, título del aviso y la palabra «Paciente», todo en la columna que debería tener un nombre. Ninguno de los cuatro se puede filtrar ni contar, y la edad y el género ya tienen vocabulario propio. La pantalla del teléfono, en cambio, guarda ahí sólo el nombre, así que la misma columna tiene hoy dos formatos | `avisos` tenga columna para la edad y para el género del paciente, y otra para el título del aviso, y `patient_name` guarde el nombre y nada más. **Toca dato ya guardado, así que lo decide el Desarrollador** (`CLAUDE.md` §7): son cuatro filas ficticias, pero la migración tiene que decir qué se hace con lo que ya está escrito |

---

## Se resuelve dentro de la migración a React

Nada de esto conviene arreglar antes: la migración reescribe cada pantalla y el arreglo se tiraría.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 8 | **2.166 declaraciones de estilo pegadas al HTML**, en 687 atributos `style=`. **La parte de colores ya está cerrada**: los 434 colores escritos a mano se fueron el 25 de agosto de 2026, `scripts/verificar_paleta.mjs` impide que vuelvan, y ese mismo día se encendió el modo oscuro automático con `scripts/verificar_temas.mjs` cuidando que nada lo vuelva a romper (las dos cosas están contadas en `docs/ALCANCE.md`). Lo que queda es el resto de las declaraciones —tamaños, márgenes, alineaciones—, que no rompen el modo oscuro pero sí hacen que cada pantalla se porte a mano. Reparto por archivo abajo | Ningún atributo `style=` quede en las pantallas. Se saca pantalla por pantalla, al portarla |
| 9 | **No hay multiidioma.** La regla 2 pide `es-AR`, `en` y `pt-BR` desde el primer día, y todo el texto visible está escrito a mano dentro del HTML — incluida `acceso.html`, que nació el 24 de agosto de 2026 con sus avisos y sus mensajes de error en un solo idioma. Las fechas, en cambio, ya están en un solo lugar: `Texto.fechaCorta()` (`js/texto.js:57`) es el único lugar del proyecto donde se le da forma a una fecha, así que el idioma le entra por ahí y no hay que ir a buscarlo pantalla por pantalla. La hora del chat, que era el otro lugar donde una fecha se armaba sola, ya no existe: `toLocaleTimeString` no aparece en ningún archivo del proyecto, comprobado el 25 de agosto de 2026 | Exista el archivo de traducciones con los tres idiomas y las pantallas lean de ahí. **Cada pantalla nueva lo encarece** |
| 11 | **La Prestadora de ejemplo está escrita a mano, 102 veces en 23 archivos** (contadas el 25 de agosto de 2026). «PresDemo» aparece en cada `<title>`, en cada encabezado, en cada pie, en los avisos legales de `panel-prestadora.html` y `registrar-asistente.html`, y con tres grafías distintas —`PresDemo`, `PrestDemo`, `Presdemo`—. Su logotipo está enlazado como `assets/images/logo_presdemo.png` en 27 lugares de 15 archivos, uno de ellos como valor de fábrica del cliente de datos (`js/apiClient.js:134`). Lo que sí se arregló: el remiendo que tenía escritos el identificador y el UUID de esta Prestadora ya no está: cuando la dirección no nombra ninguna, `_prestadoraDeRespaldo()` (`js/apiClient.js:105`) pide la primera de `tenants` y no inventa nada. **Es la regla 2 del `CLAUDE.md`, no una prolijidad**: cuando haya cientos de Prestadoras, cada uno de esos 102 lugares muestra la Prestadora equivocada. El nombre del producto ya no es parte de este problema: se cerró | El nombre, el logotipo y el color de la Prestadora salgan de la Organización de la sesión, como ya hace `apiClient.js` con el logotipo del encabezado, y ninguna pantalla los tenga escritos. **El inventario y el plan ya están hechos**, en `docs/PLAN_PRESTADORA.md`, que contó 111 apariciones el día que se escribió: 32 ya se reemplazan solas al cargar y 43 quedan sueltas. Falta que el Desarrollador apruebe, y decida las cuatro cosas de su sección 4 |
| 12 | **Las dos PWAs enlazan al directorio padre** para imágenes y para pantallas del sitio: 3 enlaces en `pwa-asistente/index.html` y 13 en `pwa-familia/index.html`, más los que arma el guion (`../perfil.html` y `../examen.html`). Su service worker no alcanza esas rutas, así que fuera de línea no cargan | Haya un solo service worker en la raíz del sitio, que es lo que emite `vite-plugin-pwa`. El problema se disuelve, no se parcha |
| 13 | **`apiClient.js` y `auth.js` están triplicados byte a byte** —921 renglones en tres lugares— y `styles-pwa.css` duplicado. `css/tokens.css` está en tres lugares por la misma razón: el service worker de cada PWA solo alcanza su propia carpeta, así que fuera de línea no puede leer el del padre. **Y hay una tercera copia, más chica y más peligrosa**: el interruptor que enciende uno de los cuatro estados de la regla 3 está escrito ocho veces, una por pantalla (`acceso.html:87`, `directorio.html:225`, `examen.html:282`, `mockup-app.html:438`, `nueva-clave.html:112`, `perfil.html:233`, `pwa-familia/index.html:851` y `recuperar-clave.html:101`). Ya se habían separado: cuatro imponían `display: 'block'` y dos devolvían `display: ''`, que no es lo mismo cuando el panel es grid o flex. El 25 de agosto de 2026 se igualaron a la forma segura y se les escribió el motivo al lado, pero siguen siendo ocho | Existan imports reales. Desaparece solo al migrar. Mientras tanto `scripts/verificar_copias.mjs` compara las cinco copias byte a byte y falla si alguna se separó: no las une, pero avisa, y corre solo antes de cada `git commit` |
| 14 | **Dos fuentes de datos conviviendo**: Supabase y datos escritos en el HTML. Eran cuatro. El 24 de agosto de 2026 se fueron dos: el almacenamiento del navegador —el interruptor `useSupabase` valía siempre `true`, así que cada método de `js/apiClient.js` arrastraba una copia muerta que escribía en el navegador, y `js/main.js` sí escribía ahí de verdad y anunciaba éxito— y `data/cuidadores.json`, cuatro perfiles de muestra que no leía nadie. El 25 de agosto de 2026 se fueron las dos últimas: las tarjetas de Asistente escritas a mano en `mockup-app.html` y las ocho fichas de `perfil.html`, que hoy salen de `directorio` —contado en `docs/ALCANCE.md:735`—. **Ninguna pantalla tiene ya datos escritos adentro**, comprobado el 25 de agosto de 2026. Lo que sigue habiendo es una segunda fuente que no es el HTML: los seis `data/catalogo-*.json`, que son el pendiente 7 | Quede una sola. El punto 2 ya se decidió el 24 de agosto de 2026, así que estas pantallas se pueden tocar: el directorio se ve sin sesión y tiene que leer de la base |
| 15 | **`messages` se consulta directo desde el HTML**, fuera del cliente de datos, en `mockup-app.html:789`, `:802` y `:828` | Toda consulta pase por `apiClient.js` |
| 16 | **Las imágenes traen la paleta ajena adentro.** Las nueve ilustraciones de `assets/images/` están dibujadas con los colores del sitio de origen. Cambiar las variables CSS no cambia un `.png`: las pantallas van a quedar con la paleta de Careonys alrededor y la vieja en el medio | Cada ilustración se rehaga con la paleta propia o se reemplace. Va con el mapeo del sistema de diseño, no antes |

### Reparto de los estilos pegados al HTML

Sirve para saber qué pantalla va a costar más y en qué orden conviene portar.

Contada el 25 de agosto de 2026, y cambió bastante: las dos pantallas que se reescribieron son
hoy las más limpias del proyecto, y aparecieron cuatro archivos que la cuenta anterior no miraba.

| Archivo | Atributos `style=` | Declaraciones |
|---|---:|---:|
| `registrar-asistente.html` | 155 | 533 |
| `mockup-app.html` | 101 | 329 |
| `pwa-familia/index.html` | 76 | 229 |
| `formulario-integral.html` | 73 | 202 |
| `solicitar-asistente.html` | 58 | 229 |
| `pwa-asistente/index.html` | 58 | 156 |
| `index.html` | 54 | 235 |
| `panel-prestadora.html` | 30 | 86 |
| `cursos.html` | 14 | 43 |
| `nueva-clave.html` | 13 | 32 |
| `perfil.html` | 13 | 24 |
| `examen.html` | 12 | 14 |
| `recuperar-clave.html` | 11 | 26 |
| `directorio.html` | 10 | 16 |
| `acceso.html` | 6 | 7 |
| `soporte-remoto.html` | 3 | 5 |

Las dos PWAs tienen además 763 renglones de CSS en bloques `<style>` adentro del HTML: 255 la del
Asistente y 508 la de la Familia.
