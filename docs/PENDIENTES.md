# Pendientes

> Lo que está abierto, con **condición de cierre** para cada punto. Sin condición de cierre un
> pendiente no se puede dar por resuelto, así que no entra sin ella.
>
> Cuando algo se cierra **se borra de acá**, no se tacha. Si era una afirmación de estado, pasa a
> `docs/ALCANCE.md` con archivo y renglón.

---

## Seguridad

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 2 | **El directorio se ve sin sesión, y la base no sabe de qué Prestadora es.** La parte que había que decidir se decidió: **el Desarrollador resolvió el 24 de agosto de 2026 que el directorio se ve sin iniciar sesión**, con nombre y foto, porque es lo que convence a una Familia que todavía no es clienta; los datos de contacto no se muestran nunca y comunicarse es sólo para Familias registradas. El texto del consentimiento se reescribió ese mismo día en los tres idiomas (`data/catalogo-banderas.json`, `perfil_publicado`), porque el anterior prometía «las Familias de su Prestadora, dentro de la plataforma» y la pantalla no lo cumplía. **Lo que queda es técnico:** `caregivers_publicos` devuelve las dos Prestadoras en la misma respuesta si nadie filtra, y sin sesión la base no puede saber cuál corresponde; hoy lo elige el enlace (`js/apiClient.js:55`) y, si no hay ninguno que valga, muestra la primera que aparece | El directorio filtre por la Prestadora que dice la dirección y no muestre nunca dos mezcladas, y `directorio.html` lea de la base en vez de las ocho tarjetas escritas a mano (punto 14) |
| 24 | **La evaluación de muestra se puede aprobar adivinando.** El mecanismo ya está bien: la corrige la base, la respuesta correcta no sale de ahí y el intento no se puede escribir a mano (migración 0008, comprobado en `scripts/probar_aislamiento.mjs`, puntos 21 a 27). Lo que falla es el contenido. Son **2 preguntas de 2 opciones cada una**, hay que acertar las dos y se dan **3 intentos**: quien no sepa nada acierta una vuelta con probabilidad 1 de 4, y en tres vueltas aprueba con probabilidad 37 de 64, **más de la mitad de las veces**. Un certificado que sale así sigue sin decir nada sobre quién va a entrar a una casa a cuidar a una persona | Cada evaluación publicada tenga preguntas suficientes como para que adivinar deje de ser un camino: con 10 preguntas de 4 opciones y 70% para aprobar, la probabilidad de acertar de casualidad baja a menos de 1 en 300. **El contenido no lo escribe la línea de comandos**: las preguntas las tiene que redactar alguien que sepa del tema, y la línea de comandos las carga. Mientras tanto la única evaluación cargada es la de muestra que sembró la migración 0008, y así está descrita ahí |
| 21 | **El servidor remoto exige confirmar el correo, y el repositorio dice que no.** `supabase/config.toml:242` declara `enable_confirmations = false`, pero `/auth/v1/settings` del proyecto remoto devuelve `mailer_autoconfirm: false`: manda el servidor. La consecuencia es que `signUp` devuelve la cuenta creada pero **sin sesión**, y el alta de Asistente no puede guardar nada hasta que la persona abra el enlace del correo. La pantalla ya lo dice en vez de fallar en silencio, pero el recorrido queda partido en dos. **No se arregla con `supabase config push`**: ese comando empuja el archivo entero, y `site_url` y `additional_redirect_urls` apuntan al servidor de pruebas de esta máquina (`localhost:5599`, corregido el 24 de agosto de 2026 — antes decían `127.0.0.1:3000`), así que dejaría la aplicación publicada sin poder volver de un enlace de correo. **Etapa 2 hecha el 24 de agosto de 2026**: existen `recuperar-clave.html` y `nueva-clave.html`, y `acceso.html` enlaza a la primera. Falta la etapa 3, que es este interruptor | El Desarrollador decida cuál de las dos es la verdad. Si se apaga la confirmación, es un interruptor del tablero (Authentication → Sign In / Providers → Email → Confirm email). Si se deja encendida, el alta se parte a propósito: cuenta primero, legajo después de confirmar. En cualquier caso `config.toml` tiene que decir lo mismo que el servidor, y `site_url` apuntar a la dirección publicada |

---

## Antes de portar la primera pantalla

Se hacen una sola vez, y si no se hacen, la decisión la termina tomando quien porta.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 6 | **`mockup-app.html` es el único lugar del proyecto donde existe un chat** (`screen-chat`, con suscripción en tiempo real y confirmación optimista) y la única pantalla de alta que sirve a una Familia: `pwa-familia` no tiene ninguna. Ninguna de las dos PWAs tiene chat. Su código **no se copia**: lee `messages?order=created_at.asc&limit=50` sin ningún filtro, se suscribe a la tabla entera sin filtro, inserta el texto recibido con `innerHTML`, evita `apiClient.js` y, sin sesión, escribe con la clave pública **Para qué existe el chat** (lo recordó el Desarrollador el 24 de agosto de 2026): es la forma en que la Familia y el Asistente se comunican **antes** de la contratación. De ahí sale una condición que no estaba escrita en ningún lado: **adentro del chat no se pueden filtrar datos de contacto**. Si se filtran, la conversación sigue por afuera y la plataforma cobra una vez y nunca más, que es exactamente la razón por la que el teléfono no se muestra (`docs/CATALOGO.md`). Anotada ahí como la tercera puerta. Y el diseño de las tablas ya existe en `docs/CAREONYS_PRESDEMO_Plan_Tecnico.md` §4.12 —`conversaciones` y `mensajes`, atadas a Familia, Asistente y aviso—, que **no coincide** con la tabla `messages` que hay hoy en la base, colgada de una búsqueda de cuidado y no del vínculo entre dos personas | Las dos capacidades estén anotadas como requisito para las PWAs y reescritas contra el esquema nuevo, y `css/mockup-app.css` haya pasado al sistema de diseño. Recién entonces se borran él y `mockup-app.html`. Y el chat no deje pasar un teléfono, un correo ni una dirección |
| 7 | **El catálogo ya lo leen las pantallas; falta que viva en tablas.** Las 22 listas (133 opciones), los 9 servicios y los 6 cursos salen de `data/catalogo-vocabularios.json` y `data/catalogo-oferta.json` a través de `js/catalogo.js`, y las nueve pantallas portadas los declaran en vez de copiarlos: 35 listas y 5 grillas de tarjetas. Lo que falta es el otro medio: **el destino de los vocabularios son las tablas de Careonys, no unas propias** (`docs/MODULOS.md:36`), y esa fusión todavía no ocurrió. Los cursos sí tienen tablas propias y legítimas (migración 0008), pero `cursos.html` lee del archivo porque la política de `cursos` exige sesión y esa pantalla es pública | El catálogo viva en tablas: los vocabularios en las de Careonys al fusionar, y los cursos acá cuando se decida si `anon` puede leer los de `tenant_id` vacío (ver punto 25). Una pantalla portada contra una tabla vacía no da error: queda vacía y nadie se entera |

---

## Aparecidos al comparar con un competidor real

Salieron del relevamiento del 24 de agosto de 2026. El detalle está en `docs/CATALOGO.md`,
sección «Lo que no es una lista».

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 20 | **Las claves viejas de Tipo de Asistente no estaban donde decía este renglón.** Decía que `caregivers.profession` guardaba `domiciliaria`, `enfermera`, `auxiliar` y `at`. Revisado el código el 24 de agosto de 2026, esas palabras estaban escritas en pantallas y no en la base: `directorio.html` las tenía en los cuatro filtros y en las ocho tarjetas de muestra —**corregido, ahora salen del catálogo**— y `formulario-integral.html:191` todavía las tiene en las tarjetas del paso 3, que nadie lee ni guarda (pendiente 32). La siembra sí escribió claves malas, y eso lo repara la migración 0010 (pendiente 27, cerrado). **De la base no se puede afirmar nada desde acá**: `caregivers` le niega la lectura a quien no inició sesión, así que no se sabe si quedó alguna fila con una clave vieja escrita antes de que las pantallas tomaran el catálogo | Se borren los datos de personas antes de producción, que es cuando desaparecen esas filas. Son todas inventadas, así que **no se migran: se descartan** |
| 23 | **La grilla de disponibilidad se pregunta y se tira.** El paso 6 de `postulacion-asistente.html` arma una grilla de 7 días por 3 turnos, la recolecta en `grillaHorarios` y la manda dentro del alta. `caregivers` no tiene ninguna columna donde ponerla, así que `ClienteDatos._mapToDatabase()` la descarta sin avisar: la persona marca veintiún casilleros y no se guarda ninguno | La disponibilidad tenga su tabla —es un dato del Asistente, no de esta modalidad, así que es módulo compartido (`docs/MODULOS.md`)— y el alta escriba ahí. Mientras tanto **la pantalla promete algo que no cumple** |
| 25 | **`cursos.html` lee los cursos del archivo y no de la base**, aunque las tablas existen (migración 0008). El motivo es una decisión de seguridad que no está tomada: la política de `cursos` es `for select to authenticated`, y esa pantalla es pública. Darle lectura a `anon` sobre las filas de `tenant_id` vacío es ampliar el acceso anónimo, y `CLAUDE.md` §4 dice que la seguridad no se relaja mientras se explora | El Desarrollador decida si la oferta general de cursos es pública. Si sí, una migración agrega la política para `anon` restringida a `tenant_id is null` y `cursos.html` pasa a leer de la base |
| 26 | **«Reemplazos urgentes, menos de 2 h» se perdió al portar.** Estaba como casilla de modalidad en `postulacion-asistente.html` y en `pwa-asistente/index.html`. No entró al catálogo porque incorporar un término de negocio nuevo se consulta antes (`CLAUDE.md` §3), y no entró al HTML porque ahí ya no se escriben catálogos | El Desarrollador diga si la disponibilidad para reemplazos urgentes es un dato que la Prestadora quiere guardar. Si sí, entra como vocabulario propio —es disponibilidad, no modalidad de contratación— |
| 29 | **La zona y la descripción de una búsqueda se preguntan y se tiran.** `pwa-familia/index.html` pide zona y descripción al publicar una búsqueda, y el atajo `crearBusqueda` de `js/apiClient.js:217` traduce sólo cinco campos: los otros dos se pierden ahí, antes de llegar a la base. `care_searches` tampoco tiene columnas donde ponerlos. Es la misma falla que la 23, en otra pantalla: la persona escribe y no se guarda | `care_searches` tenga columna para las dos cosas y el atajo las traduzca. La zona sale del vocabulario `zonas`, así que es una clave de catálogo y no texto libre. Se resuelve junto con el pendiente 2, que decide cómo se busca |
| 30 | **Las migraciones 0009 y 0010 están escritas y no aplicadas.** `supabase migration list` dice que el servidor tiene hasta la 0008. `js/apiClient.js` ya escribe `contact_info` en `care_searches`, así que hasta que la 0009 corra, el formulario público de `solicitar-asistente.html` manda una columna que la base no tiene. La 0010 corrige las claves de catálogo de las cuatro filas ficticias, y hasta que corra, la base sigue mostrando «enfermero» donde la pantalla espera «Enfermero/a universitario/a». Se intentó aplicarlas desde la línea de comandos y el permiso quedó denegado | El Desarrollador la aplique con `npx supabase db push` desde `productos/Careonys-Marketplace`, o habilite ese permiso. La 0009 es aditiva —`add column if not exists`— y arrastra un `update` que reacomoda las filas que hubieran quedado con el contacto adentro de las patologías; la 0010 sólo actualiza cuatro filas inventadas, por identificador |
| 31 | **Dos columnas guardan catálogo sin tener catálogo.** `care_searches.schedule_type` tiene hoy cuatro formas para lo mismo: la siembra escribe `turno_manana` y `guardia_12`, `js/main.js:199` escribe `flexible` y `solicitar-asistente.html:389` escribe `A coordinar`. Ninguna es una clave de vocabulario: `guardia_12` es de `modalidad_contratacion` y `turno_manana` no es de ninguno. `caregivers.nationality` tampoco tiene vocabulario. Mientras no lo tengan, `scripts/verificar_claves.mjs` no las puede mirar | Se decida qué vocabulario gobierna cada una —si `schedule_type` es un turno, una modalidad de contratación o las dos cosas, que entonces son dos columnas—, se agregue a `data/catalogo-vocabularios.json` lo que falte, y las dos columnas entren en `COLUMNAS` de `scripts/verificar_claves.mjs` |
| 32 | **El asistente de seis pasos pregunta seis cosas y guarda tres.** `formulario-integral.html` recolecta tareas de cuidado (paso 2) y Tipo de Asistente (paso 3), y `js/main.js:210` arma la búsqueda con `patologias: []` y `horarios: 'flexible'` escritos a mano: a `care_searches` llegan el título, la edad y el género, y nada más. Las dos grillas de tarjetas son además un catálogo escrito adentro de la pantalla (regla 5.1), con claves que el vocabulario no tiene: `domiciliario`, `at` y `enfermero` en lugar de `cuidador_domiciliario`, `acompanante_terapeutico` y `enfermero_universitario`; `movilidad` en lugar de `movilizacion`; `cognitivo` no existe. Como nadie las lee, hoy no ensucian ninguna fila | Las tarjetas salgan del catálogo igual que los desplegables, y lo que la persona elige llegue a la base en vez de descartarse. Es el mismo problema que los pendientes 23 y 29 |
| 33 | **«Cuidador» se usa como término general, y el glosario dice que es un tipo.** `docs/GLOSARIO.md:27` fija **Asistente** como el único término genérico y nombra expresamente «cuidador» entre lo que no se debe usar así. Aparece 176 veces en 19 archivos: `perfil.html` 39, `directorio.html` 31, `postulacion-asistente.html` 21, `index.html` 16, y el resto repartido. La palabra prohibida «especialidad» ya salió entera el 24 de agosto de 2026; esta es la que queda | Todo texto visible diga **Asistente** donde hoy dice «cuidador» como genérico, y «cuidador» quede sólo donde se nombre el tipo. Conviene hacerlo pantalla por pantalla al portarla, no de una sola vez. La clase CSS `caregiver-card` es aparte: no es texto visible |

---

## Se resuelve dentro de la migración a React

Nada de esto conviene arreglar antes: la migración reescribe cada pantalla y el arreglo se tiraría.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 8 | **2.566 declaraciones de estilo pegadas al HTML**, en 772 atributos `style=`. Incluye 304 colores escritos a mano, 48 distintos, ninguno de los cuales sale de un token — contra la regla 6. **Mientras quede alguno, el modo oscuro no se enciende solo**: los tokens están listos, pero un color escrito a mano no cambia con el modo. El motivo está en `css/tokens.css`, arriba del bloque del modo oscuro. Reparto por archivo abajo | Ningún archivo tenga atributos `style=` con colores fuera de la paleta. Se saca pantalla por pantalla, al portarla |
| 9 | **No hay multiidioma.** La regla 2 pide `es-AR`, `en` y `pt-BR` desde el primer día; en el código hay un solo `toLocaleDateString('es-AR')` en `js/main.js:221` y todo el texto visible está escrito a mano dentro del HTML — incluida `acceso.html`, que nació el 24 de agosto de 2026 con sus avisos y sus mensajes de error en un solo idioma | Exista el archivo de traducciones con los tres idiomas y las pantallas lean de ahí. **Cada pantalla nueva lo encarece** |
| 11 | **La Prestadora de ejemplo está escrita a mano, 111 veces en 18 archivos.** «PresDemo» aparece en cada `<title>`, en cada encabezado, en cada pie, en los avisos legales de `panel-prestadora.html` y `postulacion-asistente.html`, y con tres grafías distintas —`PresDemo`, `PrestDemo`, `Presdemo`—. Su logotipo está enlazado como `assets/images/logo_presdemo.png` en 17 lugares, y su identificador y su UUID están escritos en el remiendo de `js/apiClient.js:55-58`. **Es la regla 2 del `CLAUDE.md`, no una prolijidad**: cuando haya cientos de Prestadoras, cada uno de esos 111 lugares muestra la Prestadora equivocada. El nombre del producto ya no es parte de este problema: se cerró | El nombre, el logotipo y el color de la Prestadora salgan de la Organización de la sesión, como ya hace `apiClient.js` con el logotipo del encabezado, y ninguna pantalla los tenga escritos. **El inventario y el plan ya están hechos**, en `docs/PLAN_PRESTADORA.md`: 32 de las 111 apariciones ya se reemplazan solas al cargar y 43 quedan sueltas. Falta que el Desarrollador apruebe, y decida las cuatro cosas de su sección 4 |
| 12 | **Las dos PWAs enlazan al directorio padre** para imágenes y para cuatro pantallas (`pwa-asistente/index.html:270, 277, 289, 315, 371`; `pwa-familia/index.html:540, 546, 548`). Su service worker no alcanza esas rutas, así que fuera de línea no cargan | Haya un solo service worker en la raíz del sitio, que es lo que emite `vite-plugin-pwa`. El problema se disuelve, no se parcha |
| 13 | **`apiClient.js` y `auth.js` están triplicados byte a byte** —1.082 renglones en tres lugares— y `styles-pwa.css` duplicado. `css/tokens.css` está en tres lugares por la misma razón: el service worker de cada PWA solo alcanza su propia carpeta, así que fuera de línea no puede leer el del padre | Existan imports reales. Desaparece solo al migrar. Mientras tanto `scripts/verificar_copias.mjs` compara las cinco copias byte a byte y falla si alguna se separó: no las une, pero avisa. Como se corre a mano, vale el pendiente 17 |
| 14 | **Dos fuentes de datos conviviendo**: Supabase y datos escritos en el HTML. Eran cuatro. El 24 de agosto de 2026 se fueron dos: el almacenamiento del navegador —la bandera `useSupabase` valía siempre `true`, así que cada método de `js/apiClient.js` arrastraba una copia muerta que escribía en el navegador, y `js/main.js` sí escribía ahí de verdad y anunciaba éxito— y `data/cuidadores.json`, cuatro perfiles de muestra que no leía nadie. Lo que queda escrito a mano son las fichas de `perfil.html` (8 asistentes, unos 230 renglones) y las 9 tarjetas de `directorio.html` | Quede una sola. El punto 2 ya se decidió el 24 de agosto de 2026, así que estas dos pantallas se pueden tocar: el directorio se ve sin sesión y tiene que leer de la base |
| 15 | **`messages` se consulta directo desde el HTML**, fuera del cliente de datos, en `mockup-app.html:702` y `:737` | Toda consulta pase por `apiClient.js` |
| 16 | **Las imágenes traen la paleta ajena adentro.** Las nueve ilustraciones de `assets/images/` están dibujadas con los colores del sitio de origen. Cambiar las variables CSS no cambia un `.png`: las pantallas van a quedar con la paleta de Careonys alrededor y la vieja en el medio | Cada ilustración se rehaga con la paleta propia o se reemplace. Va con el mapeo del sistema de diseño, no antes |
| 17 | **Los chequeos se corren a mano.** Ya son cinco: `verificar_identidad.mjs`, `verificar_copias.mjs`, `verificar_guiones.mjs`, `verificar_trato.mjs` y `verificar_escapado.mjs`. Cada uno funciona y falla cuando tiene que fallar —se probó rompiendo a propósito cada cosa que vigilan—, pero este proyecto no tiene compilación: es HTML servido tal cual, sin `package.json`. Nadie lo corre solo. Un chequeo que hay que acordarse de correr protege hasta que alguien se olvida, que es exactamente como el nombre del producto llegó a 273 apariciones | Corra solo: enganchado a la compilación que traiga la migración a React, o antes en un `pre-commit`. **No se agrega un `package.json` ahora nada más que para esto**: el sitio se publica como archivos estáticos y meterle uno cambia cómo lo detecta el servicio de publicación |

### Reparto de los estilos pegados al HTML

Sirve para saber qué pantalla va a costar más y en qué orden conviene portar.

| Archivo | Atributos `style=` | Declaraciones |
|---|---:|---:|
| `postulacion-asistente.html` | 197 | 745 |
| `mockup-app.html` | 101 | 329 |
| `pwa-familia/index.html` | 94 | 293 |
| `formulario-integral.html` | 78 | 224 |
| `solicitar-asistente.html` | 58 | 229 |
| `index.html` | 56 | 240 |
| `pwa-asistente/index.html` | 47 | 122 |
| `perfil.html` | 42 | 120 |
| `directorio.html` | 37 | 108 |
| `panel-prestadora.html` | 32 | 85 |
| `cursos.html` | 27 | 66 |
| `soporte-remoto.html` | 3 | 5 |

Las dos PWAs tienen además 767 renglones de CSS en bloques `<style>` adentro del HTML.
