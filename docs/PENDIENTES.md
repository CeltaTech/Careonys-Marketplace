# Pendientes

> Lo que está abierto, con **condición de cierre** para cada punto. Sin condición de cierre un
> pendiente no se puede dar por resuelto, así que no entra sin ella.
>
> Cuando algo se cierra **se borra de acá**, no se tacha. Si era una afirmación de estado, pasa a
> `docs/ALCANCE.md` con archivo y renglón.

---

## Seguridad

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 24 | **La evaluación de muestra se puede aprobar adivinando.** El mecanismo ya está bien: la corrige la base, la respuesta correcta no sale de ahí y el intento no se puede escribir a mano (migración 0008, comprobado en `scripts/probar_aislamiento.mjs`, puntos 21 a 27). Lo que falla es el contenido. Son **2 preguntas de 2 opciones cada una**, hay que acertar las dos y se dan **3 intentos**: quien no sepa nada acierta una vuelta con probabilidad 1 de 4, y en tres vueltas aprueba con probabilidad 37 de 64, **más de la mitad de las veces**. Un certificado que sale así sigue sin decir nada sobre quién va a entrar a una casa a cuidar a una persona | Cada evaluación publicada tenga preguntas suficientes como para que adivinar deje de ser un camino: con 10 preguntas de 4 opciones y 70% para aprobar, la probabilidad de acertar de casualidad baja a menos de 1 en 300. **El contenido no lo escribe la línea de comandos**: las preguntas las tiene que redactar alguien que sepa del tema, y la línea de comandos las carga. Mientras tanto la única evaluación cargada es la de muestra que sembró la migración 0008, y así está descrita ahí |
| 21 | **Falta decidir si el alta de Asistente sigue pidiendo confirmar el correo.** Hoy lo pide: `/auth/v1/settings` devuelve `mailer_autoconfirm: false` y `supabase/config.toml:253` dice lo mismo desde el 24 de agosto de 2026. **La parte que costaba un legajo entero ya está resuelta**: `signUp` sigue devolviendo la cuenta sin sesión, pero la pantalla ya no manda a nadie a empezar de nuevo —guarda lo cargado, muestra el panel «La cuenta quedó creada. Falta un paso» y ofrece **«Ya confirmé, continuar»**, que entra con la contraseña que la persona todavía tiene escrita y guarda el legajo desde donde se había frenado (`postulacion-asistente.html:621` el panel, `:908` el tramo que escribe)—. Lo que queda es una decisión de negocio, y es del Desarrollador. | Se decida si la confirmación se queda encendida. **Encendida** —como está hoy— el alta es de dos tiempos, pero nadie puede darse de alta con el correo de otra persona. **Apagada**, el alta es de un solo paso y esa puerta queda abierta. Se apaga con `enable_confirmations = false` en `supabase/config.toml` más un `supabase config push`, sin tocar el tablero. |
| 43 | **El directorio público muestra dos cosas que el consentimiento no nombra, y esconde una que la pantalla pedía.** El texto que la persona acepta promete «su nombre, su foto, su zona, qué atiende y su precio por hora» (`data/catalogo-autorizaciones.json`, `perfil_publicado`). La vista `caregivers_publicos` devuelve además `gender` —la tarjeta no lo dibuja, pero está en la respuesta y cualquiera la lee— y la pantalla mostraba cuatro insignias de verificación inventadas más un filtro «Verificación», que se sacaron el 24 de agosto de 2026 porque `verificaciones_asistente` no es pública y no hay con qué contestarlos. Son tres decisiones del Desarrollador y ninguna es técnica: **(a)** si el género se muestra —una Familia suele pedir preferencia, y el vocabulario `genero_preferido` ya existe— y entonces el consentimiento lo dice, o no se muestra y sale de la vista; **(b)** si el directorio puede decir qué se le controló a un legajo —antecedentes, salud, domicilio— y entonces el consentimiento lo dice y hace falta una vista pública que lo devuelva sin fechas ni números; **(c)** si vuelve el filtro «Verificación», que depende de (b) | El consentimiento y lo que la vista devuelve digan lo mismo, en los tres idiomas, y `caregivers_publicos` no tenga ni una columna de más |
| 44 | **`perfil.html` sigue con ocho personas escritas adentro, numeradas del 1 al 8.** El directorio ya enlaza con el identificador de la base (pendiente 2), así que «Ver perfil» abre una pantalla que avisa que muestra un ejemplo y ofrece volver. Es el mismo trabajo que se hizo con el directorio, y lo que la vista pública puede llenar es poco: nombre, foto, zona, qué atiende, precio por hora y reemplazos urgentes. Todo lo demás que la pantalla dibuja —estrellas, puntaje, nivel ORO, certificaciones, referencias, estudios, descripción y la grilla de días— no tiene de dónde salir, y lo que sí existiría —las verificaciones— es el pendiente 43 | `perfil.html` lea de la base la persona que dice la dirección, no muestre ningún dato que la vista pública no devuelva, y cubra los cuatro estados |

---

## Antes de portar la primera pantalla

Se hacen una sola vez, y si no se hacen, la decisión la termina tomando quien porta.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 6 | **`mockup-app.html` es el único lugar del proyecto donde existe un chat** (`screen-chat`, con suscripción en tiempo real y confirmación optimista) y la única pantalla de alta que sirve a una Familia: `pwa-familia` no tiene ninguna. Ninguna de las dos PWAs tiene chat. Su código **no se copia**: lee `messages?order=created_at.asc&limit=50` sin ningún filtro, se suscribe a la tabla entera sin filtro, inserta el texto recibido con `innerHTML`, evita `apiClient.js` y, sin sesión, escribe con la clave pública **Para qué existe el chat** (lo recordó el Desarrollador el 24 de agosto de 2026): es la forma en que la Familia y el Asistente se comunican **antes** de la contratación. De ahí sale una condición que no estaba escrita en ningún lado: **adentro del chat no se pueden filtrar datos de contacto**. Si se filtran, la conversación sigue por afuera y la plataforma cobra una vez y nunca más, que es exactamente la razón por la que el teléfono no se muestra (`docs/CATALOGO.md`). Anotada ahí como la tercera puerta. Y el diseño de las tablas ya existe en `docs/CAREONYS_PRESDEMO_Plan_Tecnico.md` §4.12 —`conversaciones` y `mensajes`, atadas a Familia, Asistente y aviso—, que **no coincide** con la tabla `messages` que hay hoy en la base, colgada de una búsqueda de cuidado y no del vínculo entre dos personas | Las dos capacidades estén anotadas como requisito para las PWAs y reescritas contra el esquema nuevo, y `css/mockup-app.css` haya pasado al sistema de diseño. Recién entonces se borran él y `mockup-app.html`. Y el chat no deje pasar un teléfono, un correo ni una dirección |
| 7 | **El catálogo ya lo leen las pantallas; falta que viva en tablas.** Las 22 listas (133 opciones), los 9 servicios y los 6 cursos salen de `data/catalogo-vocabularios.json` y `data/catalogo-oferta.json` a través de `js/catalogo.js`, y las nueve pantallas portadas los declaran en vez de copiarlos: 35 listas y 5 grillas de tarjetas. Lo que falta es el otro medio: **el destino de los vocabularios son las tablas de Careonys, no unas propias** (`docs/MODULOS.md:36`), y esa fusión todavía no ocurrió. Los cursos sí tienen tablas propias y legítimas (migración 0008), pero `cursos.html` lee del archivo porque la política de `cursos` exige sesión y esa pantalla es pública | El catálogo viva en tablas: los vocabularios en las de Careonys al fusionar, y los cursos acá cuando se decida si `anon` puede leer los de `tenant_id` vacío (ver punto 25). Una pantalla portada contra una tabla vacía no da error: queda vacía y nadie se entera |

---

## Aparecidos al comparar con un competidor real

Salieron del relevamiento del 24 de agosto de 2026. El detalle está en `docs/CATALOGO.md`,
sección «Lo que no es una lista».

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 20 | **Las claves viejas de Tipo de Asistente no estaban donde decía este renglón.** Decía que `caregivers.profession` guardaba `domiciliaria`, `enfermera`, `auxiliar` y `at`. Revisado el código el 24 de agosto de 2026, esas palabras estaban escritas en pantallas y no en la base: `directorio.html` las tenía en los cuatro filtros y en las ocho tarjetas de muestra —**corregido, ahora salen del catálogo**— y `formulario-integral.html:191` todavía las tiene en las tarjetas del paso 3, que nadie lee ni guarda (pendiente 32). La siembra sí escribió claves malas, y eso lo repara la migración 0010 (pendiente 27, cerrado). **De la base no se puede afirmar nada desde acá**: `caregivers` le niega la lectura a quien no inició sesión, así que no se sabe si quedó alguna fila con una clave vieja escrita antes de que las pantallas tomaran el catálogo | Se borren los datos de personas antes de producción, que es cuando desaparecen esas filas. Son todas inventadas, así que **no se migran: se descartan** |
| 25 | **`cursos.html` lee los cursos del archivo y no de la base**, aunque las tablas existen (migración 0008). Las seis filas están hoy en dos lugares: la tabla `cursos` y `data/catalogo-oferta.json`. El motivo es una decisión de seguridad que no está tomada: la política de `cursos` es `for select to authenticated`, y esa pantalla es pública. Darle lectura a `anon` sobre las filas de `tenant_id` vacío es ampliar el acceso anónimo, y `CLAUDE.md` §4 dice que la seguridad no se relaja mientras se explora. **La pantalla ya explica por qué lee del archivo** (`cursos.html:196`), así que el próximo que la abra no vuelve a investigarlo | El Desarrollador decida si la oferta general de cursos es pública. **Si sí**, una migración agrega la política para `anon` restringida a `tenant_id is null` y `cursos.html` pasa a leer de la base. **Si no**, la decisión se escribe en `docs/ALCANCE.md`, esta fila se borra y con ella se cierra la parte de cursos del punto 7 |
| 31 | **Dos columnas guardan catálogo sin tener catálogo.** `care_searches.schedule_type` tiene hoy cuatro formas para lo mismo: la siembra escribe `turno_manana` y `guardia_12`, `js/main.js:199` escribe `flexible` y `solicitar-asistente.html:389` escribe `A coordinar`. Ninguna es una clave de vocabulario: `guardia_12` es de `modalidad_contratacion` y `turno_manana` no es de ninguno. `caregivers.nationality` tampoco tiene vocabulario. Mientras no lo tengan, `scripts/verificar_claves.mjs` no las puede mirar | Se decida qué vocabulario gobierna cada una —si `schedule_type` es un turno, una modalidad de contratación o las dos cosas, que entonces son dos columnas—, se agregue a `data/catalogo-vocabularios.json` lo que falte, y las dos columnas entren en `COLUMNAS` de `scripts/verificar_claves.mjs` |
| 33 | **«Cuidador» se usa como término general, y el glosario dice que es un tipo.** `docs/GLOSARIO.md:27` fija **Asistente** como el único término genérico y nombra expresamente «cuidador» entre lo que no se debe usar así. Aparece 176 veces en 19 archivos: `perfil.html` 39, `directorio.html` 31, `postulacion-asistente.html` 21, `index.html` 16, y el resto repartido. La palabra prohibida «especialidad» ya salió entera el 24 de agosto de 2026; esta es la que queda | Todo texto visible diga **Asistente** donde hoy dice «cuidador» como genérico, y «cuidador» quede sólo donde se nombre el tipo. Conviene hacerlo pantalla por pantalla al portarla, no de una sola vez. La clase CSS `caregiver-card` es aparte: no es texto visible |
| 34 | **«Mis Capacitaciones» lleva a la vidriera pública.** `pwa-asistente/index.html:281` y `:375` enlazan a `../cursos.html`, que es la oferta general —escrita para Familias, punto 35— y no muestra un solo dato de quien inició sesión: ni qué cursó, ni qué rindió, ni qué le falta. Las tablas están y la base ya sabe filtrarlas —`cursos`, `evaluaciones` e `intentos_evaluacion`, migración 0008, con política `for select to authenticated`—, y el cliente de datos ya pide las evaluaciones y los intentos (`js/apiClient.js:266` y `:319`), pero **no tiene `getCursos`** y ninguna pantalla lista los cursos de la base. El único camino a rendir sale de la pantalla pública (`cursos.html:112`) | El Asistente con sesión tenga su propia pantalla de capacitaciones leída de la base: los cursos con un `getCursos` nuevo en el cliente de datos, y al lado de cada uno lo que esa persona ya rindió, que sale de `getIntentosEvaluacion`. La vidriera pública se queda como está |
| 35 | **Los seis cursos están escritos para Familias y se los ofrece como capacitación de Asistentes.** `cursos.html` se titula «Cursos para Familias», encabeza con «Herramientas prácticas para familiares que cuidan» y describe cada curso desde ese lugar —«acompañar a un familiar», «la persona cuidada»—. Los mismos seis son los que la migración 0008 siembra en `cursos`, los que la PWA del Asistente ofrece como «Mis Capacitaciones» (`pwa-asistente/index.html:281`) y los que la evaluación certifica **dentro del legajo, a la vista de la Prestadora** (`cursos.html:111`). Una misma pantalla le habla a las dos puntas y el catálogo no distingue: `data/catalogo-oferta.json` no dice a quién está dirigido ningún curso | El catálogo diga a quién está dirigido cada curso y cada pantalla muestre los que le tocan. **Qué cursos son para Familias y cuáles para Asistentes lo dice el Desarrollador**: es oferta, no configuración, y no se inventa desde acá. Con eso resuelto, la lista se parte y los puntos 25 y 34 se apoyan en ella |
| 36 | **El alta del teléfono da de alta sin legajo, y por eso nadie que se postule desde ahí puede aparecer en el directorio.** `pwa-asistente/index.html` llama a `guardarLegajoAsistente` con una sola cosa: la disponibilidad (`:766`). No manda las cuatro fichas repetibles —matrícula, estudio, experiencia y referencia— porque esa pantalla no las dibuja, y no manda el consentimiento de publicación porque tampoco tiene el paso de cierre. Lo segundo no es un detalle: `caregivers_publicos` exige una fila en `autorizaciones_asistente` con un `join`, no con un `left join` (`supabase/migrations/0012_autorizaciones_y_disponibilidad.sql:186`), así que sin esa fila la persona **no se muestra nunca**, aunque la Prestadora valide su legajo. La referencia sí se guarda, pero como texto suelto adentro de `caregivers.reference_info`, que es el formato viejo | La pantalla del teléfono tenga los dos pasos que le faltan, dibujados desde `data/catalogo-fichas.json` y `data/catalogo-autorizaciones.json` con los mismos módulos que usa el portal (`js/fichas-legajo.js`), y mande las cuatro fichas y la autorización junto con la disponibilidad |
| 39 | **La prueba de aislamiento ya no llega a arrancar contra el servidor alojado.** `scripts/probar_aislamiento.mjs` es la comprobación de la que depende `CLAUDE.md` §2 —27 controles con dos Prestadoras— y empieza creando dos cuentas ficticias (`:165`). El servidor las rechaza y la prueba corta ahí sin comprobar nada. Lo comprobado el 24 de agosto de 2026, sin suponer nada: **(a)** el alta con `@ejemplo.invalid` y con `@example.com` devuelve *«Email address … is invalid»*; son los dos dominios que se pueden usar sin riesgo, porque la prueba **nunca borra las cuentas que crea** y un dominio que sí reciba correo puede terminar escribiéndole a una persona de verdad. **(b)** Probar otros dominios para saber cuál acepta **no se pudo**: a los pocos intentos el servidor pasa a contestar *«email rate limit exceeded»* a todos por igual, incluso a `gmail.com`. Ese segundo tope es del servicio de correo de fábrica y es un problema por sí solo: la prueba crea tres cuentas cada vez que corre y el alta manda un correo de confirmación por cada una. Ninguna de las dos cosas se toca desde la línea de comandos: `supabase/config.toml` no tiene llave para ninguna y `supabase config push` no las alcanza | Se pueda volver a correr entera. Hay tres caminos y **los dos primeros los abre el Desarrollador desde el panel de Supabase**, porque son configuración del proyecto alojado: **(a)** cargar un servidor de correo propio en Authentication → Emails → SMTP Settings, que levanta el tope por hora; **(b)** apagar la confirmación por correo mientras se explora (Authentication → Sign In / Providers → Email), que es lo que hace que el alta mande un correo — es gratis pero toca el sitio publicado, así que la decisión es suya y además choca con el pendiente 21. **(c)** El tercero sí es de la línea de comandos y **queda por probar**: levantar la base local con `supabase start` y correr la prueba con `--local`, que no usa el correo del servidor alojado. Necesita Docker andando en esta máquina, que no se comprobó |
| 40 | **La grilla de días y turnos de una búsqueda tiene el problema que la del Asistente ya no tiene.** `care_searches.grid_schedule_7x3` es una columna `jsonb` y cada pantalla le escribe una forma distinta: `pwa-familia/index.html` manda `{ turnos: [...] }` y `formulario-integral.html` pregunta días y no manda nada, porque no había forma de mandarlos sin inventar una tercera. Nadie lee esa columna todavía, así que hoy no rompe nada; el día que alguien quiera cruzar la disponibilidad de un Asistente con la necesidad de una Familia, de un lado va a tener filas y del otro un `jsonb` con dos formas | Una búsqueda guarde sus franjas como las guarda un Asistente desde la migración 0012: una fila por casillero, con clave de `dia_semana` y clave de `turno`. Es la tabla espejo de `franjas_asistente`, y una vez que exista `grid_schedule_7x3` se vacía y se saca. Conviene resolverlo junto con el pendiente 31, porque las dos preguntas son la misma: cuándo se necesita el cuidado |
| 41 | **El nombre del paciente guarda cuatro datos pegados en un solo texto.** `js/main.js` arma `patient_name` como `Paciente de 82 años (femenino) - Acompañamiento de tarde`: edad, género, título de la búsqueda y la palabra «Paciente», todo en la columna que debería tener un nombre. Ninguno de los cuatro se puede filtrar ni contar, y la edad y el género ya tienen vocabulario propio. La pantalla del teléfono, en cambio, guarda ahí sólo el nombre, así que la misma columna tiene hoy dos formatos | `care_searches` tenga columna para la edad y para el género del paciente, y otra para el título de la búsqueda, y `patient_name` guarde el nombre y nada más. **Toca dato ya guardado, así que lo decide el Desarrollador** (`CLAUDE.md` §7): son cuatro filas ficticias, pero la migración tiene que decir qué se hace con lo que ya está escrito |
| 42 | **El servidor de mirar las pantallas no dejaba ver los cambios.** Se arreglaba solo en el archivo y el navegador seguía mostrando la versión vieja de `js/main.js` sin avisar, porque `python -m http.server` no manda ninguna instrucción sobre guardar copias y el navegador entonces decide por su cuenta guardarlas por mucho tiempo. Costó dos vueltas enteras de comprobación el 24 de agosto de 2026, buscando un error que ya estaba arreglado. Queda escrito `scripts/servidor_local.py`, que es el mismo servidor diciendo «no guardes nada», y `.claude/launch.json` ya lo nombra | Se compruebe que el programa que levanta el servidor está tomando `.claude/launch.json` cuando cambia: el 24 de agosto siguió usando el comando viejo aun después de reiniciarlo, y hubo que forzar la recarga desde el navegador. Mientras eso no se compruebe, conviene desconfiar de una pantalla que «no cambió» |

---

## Se resuelve dentro de la migración a React

Nada de esto conviene arreglar antes: la migración reescribe cada pantalla y el arreglo se tiraría.

| # | Pendiente | Se cierra cuando |
|---|---|---|
| 8 | **2.566 declaraciones de estilo pegadas al HTML**, en 772 atributos `style=`. Incluye 304 colores escritos a mano, 48 distintos, ninguno de los cuales sale de un token — contra la regla 6. **Mientras quede alguno, el modo oscuro no se enciende solo**: los tokens están listos, pero un color escrito a mano no cambia con el modo. El motivo está en `css/tokens.css`, arriba del bloque del modo oscuro. Reparto por archivo abajo | Ningún archivo tenga atributos `style=` con colores fuera de la paleta. Se saca pantalla por pantalla, al portarla |
| 9 | **No hay multiidioma.** La regla 2 pide `es-AR`, `en` y `pt-BR` desde el primer día; en el código hay un solo `toLocaleDateString('es-AR')` en `js/main.js:221` y todo el texto visible está escrito a mano dentro del HTML — incluida `acceso.html`, que nació el 24 de agosto de 2026 con sus avisos y sus mensajes de error en un solo idioma | Exista el archivo de traducciones con los tres idiomas y las pantallas lean de ahí. **Cada pantalla nueva lo encarece** |
| 11 | **La Prestadora de ejemplo está escrita a mano, 111 veces en 18 archivos.** «PresDemo» aparece en cada `<title>`, en cada encabezado, en cada pie, en los avisos legales de `panel-prestadora.html` y `postulacion-asistente.html`, y con tres grafías distintas —`PresDemo`, `PrestDemo`, `Presdemo`—. Su logotipo está enlazado como `assets/images/logo_presdemo.png` en 17 lugares, y su identificador y su UUID están escritos en el remiendo de `js/apiClient.js:55-58`. **Es la regla 2 del `CLAUDE.md`, no una prolijidad**: cuando haya cientos de Prestadoras, cada uno de esos 111 lugares muestra la Prestadora equivocada. El nombre del producto ya no es parte de este problema: se cerró | El nombre, el logotipo y el color de la Prestadora salgan de la Organización de la sesión, como ya hace `apiClient.js` con el logotipo del encabezado, y ninguna pantalla los tenga escritos. **El inventario y el plan ya están hechos**, en `docs/PLAN_PRESTADORA.md`: 32 de las 111 apariciones ya se reemplazan solas al cargar y 43 quedan sueltas. Falta que el Desarrollador apruebe, y decida las cuatro cosas de su sección 4 |
| 12 | **Las dos PWAs enlazan al directorio padre** para imágenes y para cuatro pantallas (`pwa-asistente/index.html:270, 277, 289, 315, 371`; `pwa-familia/index.html:540, 546, 548`). Su service worker no alcanza esas rutas, así que fuera de línea no cargan | Haya un solo service worker en la raíz del sitio, que es lo que emite `vite-plugin-pwa`. El problema se disuelve, no se parcha |
| 13 | **`apiClient.js` y `auth.js` están triplicados byte a byte** —1.082 renglones en tres lugares— y `styles-pwa.css` duplicado. `css/tokens.css` está en tres lugares por la misma razón: el service worker de cada PWA solo alcanza su propia carpeta, así que fuera de línea no puede leer el del padre | Existan imports reales. Desaparece solo al migrar. Mientras tanto `scripts/verificar_copias.mjs` compara las cinco copias byte a byte y falla si alguna se separó: no las une, pero avisa. Como se corre a mano, vale el pendiente 17 |
| 14 | **Dos fuentes de datos conviviendo**: Supabase y datos escritos en el HTML. Eran cuatro. El 24 de agosto de 2026 se fueron dos: el almacenamiento del navegador —el interruptor `useSupabase` valía siempre `true`, así que cada método de `js/apiClient.js` arrastraba una copia muerta que escribía en el navegador, y `js/main.js` sí escribía ahí de verdad y anunciaba éxito— y `data/cuidadores.json`, cuatro perfiles de muestra que no leía nadie. Lo que queda escrito a mano son las fichas de `perfil.html` (8 asistentes, unos 230 renglones) y las 9 tarjetas de `directorio.html` | Quede una sola. El punto 2 ya se decidió el 24 de agosto de 2026, así que estas dos pantallas se pueden tocar: el directorio se ve sin sesión y tiene que leer de la base |
| 15 | **`messages` se consulta directo desde el HTML**, fuera del cliente de datos, en `mockup-app.html:702` y `:737` | Toda consulta pase por `apiClient.js` |
| 16 | **Las imágenes traen la paleta ajena adentro.** Las nueve ilustraciones de `assets/images/` están dibujadas con los colores del sitio de origen. Cambiar las variables CSS no cambia un `.png`: las pantallas van a quedar con la paleta de Careonys alrededor y la vieja en el medio | Cada ilustración se rehaga con la paleta propia o se reemplace. Va con el mapeo del sistema de diseño, no antes |
| 17 | **Los chequeos se corren a mano.** Ya son cinco: `verificar_identidad.mjs`, `verificar_copias.mjs`, `verificar_guiones.mjs`, `verificar_trato.mjs` y `verificar_escapado.mjs`. Cada uno funciona y falla cuando tiene que fallar —se probó rompiendo a propósito cada cosa que vigilan—, pero este proyecto no tiene compilación: es HTML servido tal cual, sin `package.json`. Nadie lo corre solo. Un chequeo que hay que acordarse de correr protege hasta que alguien se olvida, que es exactamente como el nombre del producto llegó a 273 apariciones | Corra solo: enganchado a la compilación que traiga la migración a React, o antes en un `pre-commit`. **No se agrega un `package.json` ahora nada más que para esto**: el sitio se publica como archivos estáticos y meterle uno cambia cómo lo detecta el servicio de publicación |

### Reparto de los estilos pegados al HTML

Sirve para saber qué pantalla va a costar más y en qué orden conviene portar.

| Archivo | Atributos `style=` | Declaraciones |
|---|---:|---:|
| `postulacion-asistente.html` | 197 | 745 |
| `mockup-app.html` | 101 | 329 |
| `pwa-familia/index.html` | 94 | 293 |
| `formulario-integral.html` | 78 | 224 |
| `solicitar-asistente.html` | 58 | 229 |
| `index.html` | 56 | 240 |
| `pwa-asistente/index.html` | 47 | 122 |
| `perfil.html` | 42 | 120 |
| `directorio.html` | 37 | 108 |
| `panel-prestadora.html` | 32 | 85 |
| `cursos.html` | 27 | 66 |
| `soporte-remoto.html` | 3 | 5 |

Las dos PWAs tienen además 767 renglones de CSS en bloques `<style>` adentro del HTML.
